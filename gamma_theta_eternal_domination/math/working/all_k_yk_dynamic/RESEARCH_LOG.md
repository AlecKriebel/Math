# Research log: all-parameter \(Y_k\) dynamics

## 2026-07-28 PDT

- Read C-117, C-118, C-121, the accepted restoration theorem, and the
  exact-family mixed-\(P_4\) results C-067 through C-072.
- Proved simultaneous installation of the \(K_{k-3}\) singleton vertices.
- Proved that the original family-list portions on \(\{a,b,c\}\) are forced
  to be the exact \(Y_3\) pattern, without assuming that response lists are
  unchanged after installation.
- Isolated the first lift obstruction: a singleton vertex adjacent to a
  wrong base anchor forces an explicit private-buffer witness.
- Proved the clean lift: freezing a clean installed singleton clique gives
  an equality-three antineighborhood projection with exact family-list
  \(Y_3\).  C-072 yields the conditional all-\(k\) floor \(n\geq2k+6\).
- Identified the exact extra hypothesis needed to invoke C-121: every
  forbidden projected static swap must retain a defect.  Under that
  hypothesis the floor is \(n\geq2k+8\).
- Found and exactly checked a seven-vertex boundary control in which the
  singleton replacement is clean and retained but repairs a static defect,
  enlarging one static list from \(\{0\}\) to \(\{0,2\}\).  The control has
  \((\gamma,\alpha,\gamma^\infty,\theta)=(3,4,4,4)\), so it is not a
  conjecture counterexample.

The universal conjecture and arbitrary-order \(Y_k\) exclusion remain open.
