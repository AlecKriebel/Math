# Research log: full-list completion coupling

## 2026-07-28 PDT — branch spokes and the first ridge square

- Reconstructed accepted C-170 and C-171 with their hostile reviews.
- Fixed one rank-zero full-terminal corridor row
  \(S=\{u,v,t\}\), predecessor \(\{v,t,q\}\), terminal
  \(\{v,t,r\}\), secondary witness \(w_v\), and completion
  \(d\in C_{xr}\).
- If the retained C-170 branch is \(D=\{d,t,r\}\), then
  \(\gamma=3\) makes the pair \(\{d,t\}\) non-dominating.  Its entire
  common-nonneighbor set \(C_{dt}\) is nonempty.  Domination by \(D\)
  forces \(r\) adjacent to every \(e\in C_{dt}\), and the attack at
  \(e\) has the unique response \(r\to e\), retaining
  \(\{d,t,e\}\).
- If additionally \(e\) misses the full target \(x\), then
  \(\{x,r,d\}\) and \(\{x,e,d\}\) are maximum independent retained
  triples.  The initially proposed ridge-square conclusion was not new:
  \(\{x,d,t\}\) is simply missed by \(e\), and the associated activity
  split reduces to accepted C-172.  This route was explicitly terminated
  before promotion.
- The genuinely uniform statement is stronger in a different direction:
  **every** pair co-occupied in a retained triple has its entire
  non-domination witness fan retained.  C-172 alone permits an omitted
  fan on a reciprocal edge; co-occupation rules out that branch.
- In the completion setup, the cross state \(\{x,d,t\}\) dominates
  exactly when the supported fan \(W_{dt}\) avoids
  \(B=N_{\overline G}(x)\).  If the fan meets \(B\), its trapped member
  gives an independent source witnessing \(x\to t\), while fullness gives
  \(t\to x\).
- Exact equality controls realize a nondominating/reciprocal cross state,
  a dominating retained cross state, and a dominating omitted cross
  state.  Thus neither family membership nor rank follows in the
  dominating branch.
- Best-guess completion: **70%** for this completion-coupling lane,
  **54%** toward a complete parameter-three theorem, and **18%** toward
  universal resolution.  These are workload estimates, not
  probabilities.
- Best-guess completion: **25%** for this completion-coupling lane,
  **53%** toward a complete parameter-three theorem, and **18%** toward
  universal resolution.  These are workload estimates, not
  probabilities.
