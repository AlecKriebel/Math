# Research log: singleton-list endgame

## 2026-07-28 PDT

- Read the accepted no-full projection formula and terminal classification
  (C-069 and C-075), the odd physical fan exclusion (C-079), the dynamic
  cap and literal-physicalization theorems (C-082 and C-094), and the
  later all-exact-two results C-103, C-111, and C-114.
- Kept the main model boundary explicit: C-111 assumes every outside list
  has exact size two and therefore cannot be imported after a singleton
  appears.
- Exhaustively scanned all connected unlabeled graphs through order nine
  as a discovery falsifier.  Equality examples already contain thousands
  of dynamically omitted singleton incidences.  No finite claim is made
  from this scan.
- Found exact equality controls with singleton lists, all three exact-two
  types, and dynamic exact-two ports.  This refuted both naive
  physicality extensions before any proof was attempted.
- Isolated the elementary sealed-cap witness mechanism.  When a sealed
  \(i\)-positive cap has list \(\{i,j\}\), applying
  \(\gamma(G)=3\) to its pair with the third anchor forces a
  singleton-\(j\) buffer on a literal complement \(2\)-path.
- Combined that lemma with accepted C-094, C-082, and C-079.  Every
  dynamic exact-two port now has a precise alternative: a sealed
  singleton cap of the omitted color, or a sealed exact-two cap absorbed
  by the forced singleton buffer.
- Verified the six-vertex sharp control `EEv?` from a selected eight-state
  family: all parameters are three, all 24 obligations pass, and the
  sealed exact-two/buffer normal form is literal.
- Independently rebuilt the 13-vertex equality control
  `LFzJbZYhdrDZdM`: its greatest kernel has 142 states, all 1,420
  obligations pass, and its two dynamic ports lie in unpinned components
  while their sealed caps are singleton buffers.
- Recorded the exact remaining branches without claiming a proof:
  arbitrary separated-port unit chains, and unit-free residual bicycles
  whose dynamic ports cannot transport clause incidences.
- Hostile review accepted Lemma 2.1, Corollary 2.2, Theorem 3.1, and both
  controls, but correctly found that the simplified logical taxonomy had
  omitted an immediate false constant.  Revised Proposition 1.2, the
  top-level inventory, and Section 5 to keep projection-internal
  singleton-parity/fixed-collision certificates as a separate terminal
  branch, exactly matching C-075.  No graph-game proof or control changed.
