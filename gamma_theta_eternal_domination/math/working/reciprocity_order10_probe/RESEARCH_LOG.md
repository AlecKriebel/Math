# Order-ten reciprocity falsification probe

## 2026-07-28 — lane opened

- Purpose: try to falsify the open equality-specific greatest-family
  reciprocity statement before investing further in its all-order proof.
- The scan uses the already hostile-audited C-138 colored-configuration
  implementation but streams one deterministic `geng` residue at a time.
  Sixteen partitions keep every job short and resumable.
- A one-sided active orientation would be a rigorous candidate only after
  independent parameter, greatest-kernel, and exchange replay.  A zero
  scan remains `OBSERVED` until all split coverage and a second
  implementation are audited.

## 2026-07-28 — all sixteen discovery partitions complete

- All residues `0/16` through `15/16` completed without an asymmetric
  active orientation.
- The partition sum is 11,716,571 connected unlabeled order-ten graphs,
  exactly matching a separate count-only `geng -c -u 10` run.
- Of these, 61,366 pass the static
  \(\gamma=i=\alpha=3\) filter, 17,671 also have a nonempty greatest
  eternal triple kernel, and their maximum-independent endpoints induce
  458,696 active orientations.  Every one of those orientations was
  reciprocal.
- This is strong falsification evidence for greatest-family reciprocity,
  not yet a theorem: the stream partition and order-ten adaptation still
  need an independent reconstruction before the result can be promoted
  from `OBSERVED` to `CERTIFIED-FINITE`.
- Exact totals and every partition hash are frozen in `aggregate.json`.
