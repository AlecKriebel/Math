# Research log: full-list anchor restoration

- **2026-07-28 PDT.** Reconstructed accepted C-149, C-154, C-157, and
  C-163, including the exact restricted-rank convention and both C-149
  terminal gates.  Isolated the rank-zero anchor-restoration state
  \(T=\{c,r,q\}\to\{a,c,r\}\).

- **2026-07-28 PDT.** Applied arbitrary-state restoration to the terminal
  predecessor.  Proved that a nonsingleton terminal palette either contains
  the attacked anchor, or is exactly \(\{u,c\}\) and forces the attacked
  anchor into \(Q(q)\).  Explicitly kept palette omission distinct from a
  graph nonedge.

- **2026-07-28 PDT.** Classified the only alternate \(r\to a\) by the
  independent conditions \(ar\in E(G)\) and \(q\in B\).  Proved the
  shared-secondary missed-witness ladder through two unique unoccupied
  attacks.

- **2026-07-28 PDT.** Audited the accepted equality controls and the 55
  published gamma-two near-misses.  The latter realize 14 local status
  rows and show that neither domination nor retention is encoded by the
  terminal palette alone.  This catalog scan was exploratory and is not a
  promoted finite claim.

- **2026-07-28 PDT.** Searched bounded edge mutations of the accepted
  order-12 and order-16 equality controls.  Found the exact graph
  `OYifur}UO]}iTij]tpo]v`, obtained exploratorily by toggling two edges of
  the prior order-16 control.  It realizes both palette branches with the
  same dominating banned but nonretained alternate.

- **2026-07-28 PDT.** Wrote a clean ordinary-frozenset verifier independent
  of the search core.  It recomputes all five parameters, the 304-state
  greatest family, 3,952 attack obligations, all restricted ranks, and
  every named edge, nonedge, palette, state, and ban status.  Strict replay
  passed.

- **Boundary.** The local promotion
  `dominating alternate => retained alternate` is false even under full
  equality.  The control has restricted-kernel sizes \(0,150,0\), so the
  remaining route must couple the three color traces rather than analyze a
  single annihilated color in isolation.
