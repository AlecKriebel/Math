# Research log: repair-square holonomy

Date: 2026-07-28 (PDT)

- Began from accepted C-145 rather than re-deriving its five-state repair
  square.
- Observed that a path in the complement link between the endpoints of a
  one-sided active edge can be shortened by two using the opposite
  asymmetry supplied by C-145.
- Proved the resulting fixed-pivot component-separation theorem: every
  common nonneighbor places the two asymmetric endpoints in different
  complement-link components.
- Combined C-051 with the accepted parameter-two theorem to make every
  such link bipartite and isolate-free under
  \(\gamma=\alpha=\gamma^\infty=3\).
- Iterated arbitrary synchronized walks in the two components and obtained
  the exact checkerboard orientation: the original bipartition sides point
  in the original direction and the opposite sides point in the reverse
  direction.
- Checked the finite-cycle boundary.  Every repair step has a literal
  two-step return obtained by backtracking both completion edges.  This is
  a cycle of oriented pairs, not reciprocity of one physical edge.
- Checked the C-064 boundary.  The mixed repair states are not independent,
  the two polarized endpoint components have no ridge path between them,
  and the immediate backtrack has cancelling ridge transpositions.
- Proved corner-rank conservation: the omitted state of a repair square is
  literally the same reverse endpoint for the original and opposite
  asymmetric orientations.
- Applied C-146 to show that changing the independent endpoint triple moves
  this rank by at most two.
- Replayed the exact order-15 boundary control
  `NslalntvXzn^{~n||^w`.  Its link at \(w=10\) is
  \(2K_2\), the repair alternates \(0\to1\) and \(13\to6\), and the named
  reverse rank rises from one to three.  The graph has 23 dominating pairs
  and \(\gamma=2\), so it is not an equality counterexample.
- Exhausted all 3,672 labeled isolate-free bipartite link graphs through
  order six in an independent abstract closure audit.  All 60,044
  same-component oriented roots reached a forbidden shared completion,
  link edge, or reverse orientation; all 6,924 separated roots remained
  consistent and passed 27,468 checkerboard-pair checks.
- Recorded \(\overline{L(K_{3,3})}\) as the exact full-equality warning
  that gamma three does not force complement-link connectivity.
- Stopped the fixed-pivot lane at the precise new gate: couple distinct
  link components using the global every-pair common-neighbor condition,
  or derive a dominating pair from the checkerboard polarization.
