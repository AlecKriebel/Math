# Second independent audit addendum: \(k=3\) full-list slice

Date: 2026-07-26 (PDT)

## Verdict

**CONCUR WITH PASS.**

This audit was run independently and concurrently with the primary hostile
review in this directory.  It reached the same mathematical and finite
boundary:

- every statement labeled `PROVED` in the target note is sound for an
  arbitrary specified one-guard eternal family;
- the connected-unlabeled scan through order nine merits the narrow
  `CERTIFIED-FINITE` label after independent coverage replay;
- the order-12 graph is a valid positive equality control with a genuine
  full greatest-family list; and
- neither the \(k=3\) slice nor the universal conjecture is resolved.

## Frozen artifacts

| artifact | SHA-256 |
|---|---|
| target `NOTE.md` | `ebcf7a6ef902889e5d70a657baf7e79613b3dd0e278be01263cf0882033d23be` |
| target `probe.py` | `64faa7775df8c5b7145472ca8a4147c1c778e00b3a3061ecaf7d09e9db1c4eeb` |
| target `probe_result.json` | `4e97308e937506f3ad52dbe4c99e1285aa9156ba46c27194526bcf13af9ba55a` |
| second clean-room `independent_audit.py` | `f6eb1468ffd3d3847d1a787a551944580351bf68f2a0ba8358c5134acc718b5e` |
| second clean-room `independent_result.json` | `7d33ede87d31ab57bbc7f7d2ff635a20687da8ee446468dc83f59dd1f078a3da` |

The second checker imports neither the target probe nor any campaign
evaluator.  Its order-nine replay uses integer masks and complete
fixed-point deletion.  Its order-12 replay separately uses ordinary
`frozenset` configurations.

## Proof stress points

The following delicate steps were reconstructed directly.

1. **Vertex avoidance.**  From a family state avoiding the deleted vertex,
   an attack in the deleted graph cannot introduce that vertex.  The
   original one-guard successor therefore remains in the restricted family,
   and every surviving direct-swap state has exactly the same membership.
2. **Full-core extension.**  A proposed proper coloring of the full
   vertices contributes exactly the constants and units forbidding
   full--non-full collisions.  The accepted no-full-list gluing theorem
   covers every remaining edge of \(H\), so Theorem 3.1 is an equivalence.
3. **Spoke layers.**  Full direct-swap states force both opposite-anchor
   graph edges, the unique spoke response, clique spokes, and the external
   \(Y_{u,p}\) cliques.  Every invoked attack is unoccupied.
4. **Component signatures.**  Ridge covariance across adjacent edges of a
   bipartite complement link preserves the fixed \(X,U,V\) roles.  No graph
   automorphism or greatest-family assumption is used.
5. **Cross-spoke separation.**  On an even link path, the first forced
   response creates \(D_1\); domination forces the initial responder edge.
   The successive ridge transpositions map role \(v_2\) to the terminal
   role \(v_{2m}\), contradicting the terminal spoke nonedge.  The
   composition and attack domain are valid for every even length.
6. **Local coloring.**  Once the full target receives color \(w\), each
   link component uses the other two colors.  Cross-spoke separation makes
   every bad-pair set empty, so all three choices pass locally, with no
   claim of global extension.

## Independent finite results

The second replay reproduced every per-order graph-stream hash and every
count.  Totals through order nine were:

| quantity | count |
|---|---:|
| connected unlabeled graphs | 273,193 |
| raw static-full incidences | 623,732 |
| \(\gamma=\alpha=3\) graphs with a static-full incidence | 51 |
| such incidences | 61 |
| eternal-equality candidate graphs | 15 |
| equality/static-full incidences | 24 |
| greatest-family full incidences | 0 |

The family-list-size histogram on the 24 surviving incidences was exactly
\((0,24,0,0)\) for sizes \(0,1,2,3\).

This is certificate support only for connected graphs, orders one through
nine, and the stated full-family-list predicate.  The finite singleton
histogram is exact inside that universe; any extrapolation of singleton
collapse beyond it remains `OBSERVED`.

## Independent order-12 control

For the labeled record

```text
Ksv`f\knJVis
```

independently canonicalized as `K{eYptMJynEn`, the second checker obtained

\[
(\gamma,\alpha,\gamma^\infty,\theta)=(3,3,3,3).
\]

It verified all 127 dominating triples, all 1,143 unoccupied attacks, the
full list \(L_{123}(0)=\{1,2,3\}\), the eight other displayed lists, the
unique compatible anchored coloring, and the displayed clique partition.
The link is exactly \(6-8\) and \(10-11\), with spokes and external
witnesses exactly as stated.

As an additional falsifier, the local link has exactly one anchored
coloring for each prescribed color \(1,2,3\) of the full target, while the
global response-list instance has exactly one coloring overall and selects
target color \(3\).  This directly confirms the note's local/global
boundary.

The proper `FDzro`, static-versus-family `HCQebjw`, and abstract full-column
controls were also replayed independently.

## Archival qualifications

The only corrections recommended are nonmathematical:

1. explicitly close the target probe's subprocess pipes so that
   `-W error` emits no shutdown `ResourceWarning`; and
2. bind the research log to the repository result rather than a temporary
   output path.

These do not change the `PASS` verdict or any mathematical/finite count.
