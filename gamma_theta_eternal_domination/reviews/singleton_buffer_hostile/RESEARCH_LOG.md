# Research log: hostile singleton-buffer audit

## 2026-07-28 PDT

- Froze and hashed the candidate note, log, controls, discovery script,
  verifier, and manifest.
- Read the exact current statements and hostile reviews for C-069, C-075,
  C-079, C-082, and C-094.  Checked C-111 and C-114 only as scope
  boundaries.
- Reconstructed Proposition 1.1's complete list-pair inventory over all
  nonempty proper subsets of a three-color set.
- Found one exact logical omission: Proposition 1.1 allows false constants
  after fixed anchor-component substitution, while Proposition 1.2 applies
  C-075 without restoring pinned anchor variables or retaining a separate
  immediate-constant branch.
- Sent the precise defect and two minimal repairs to the lead researcher.
- Audited Lemma 2.1 directly from the no-dominating-pair property,
  response-membership edges, sealing, and list nonemptiness.  Checked every
  anchor and witness collision.
- Audited Corollary 2.2 by direct list-color properness.
- Reconstructed Theorem 3.1's dependency chain:
  C-094 supplies a literal outside length-two path, C-082 supplies an
  outside positive cap on its first edge, and C-079 at path length one
  seals the cap.  Verified every distinctness condition and confirmed that
  no clause edge is transported to the physical representative.
- Wrote a clean-room bit-mask checker with no campaign or candidate import.
  It uses a fresh graph6 decoder, exhaustive parameter routines, a separate
  coloring backtracker, and simultaneous greatest-fixed-point deletion.
- Rebuilt `EEv?`, its eight-state family, all 24 obligations, exact lists,
  sealed vertices, singleton buffer, and unique compatible coloring.
- Rebuilt `LFzJbZYhdrDZdM`, its 142-state greatest kernel, all 1,420
  obligations, exact lists, dynamic ports, unpinned components,
  physicalization paths, sealed singleton caps, and two compatible
  colorings.
- Confirmed both controls have
  \((\gamma,i,\alpha,\gamma^\infty,\theta)=(3,3,3,3,3)\).
- Recorded `CORRECTION REQUIRED` rather than an unconditional pass.  The
  graph-game theorems and controls are sound; only the advertised exact
  logical split and resulting remaining-branch ledger need repair.
- Re-audited revised candidate bytes.  The revision adds the immediate
  false-constant branch to the opening inventory, Proposition 1.2 and its
  proof, the scope disclaimer, Section 5's obstruction ledger, and the
  next-lemma list.  The branch is explicitly kept outside C-075.
- Confirmed the three terminal cases are now mutually exclusive and
  exhaustive and that no downstream sentence silently drops the restored
  branch.
- Rebound the clean-room checker to the revised note/log hashes and reran
  every finite reconstruction.  The controls and all numerical outputs are
  unchanged.
- Issued final revised-byte verdict `UNCONDITIONAL PASS`.
