# Autonomous Final K3P Paper, Proof-Certification, and Submission-Release Program

## Mission

Continue from the completed cloud K3P classification and finish the workstream locally as one exact, independently replayed, submission-ready research paper.

The mathematical outcome reported by the cloud phase is:

[
\boxed{\mathrm{K3P\text{-}SAME}}
]

On the principal positive-eigenvalue K3P domain, and separately in strict continuous time, regular full-dimensional stochastic containment among binary standard semi-directed strongly tree-child level-2 networks occurs exactly under labelled topology equivalence modulo ordinary triangle redirection:

[
\boxed{
N\preceq_{3,+}N'
\iff
N\equiv_{\triangle}N'
\iff
N\bowtie_{3,+}N'.
}
]

The final local classification reportedly contains:

* zero unresolved four-port relation orbits;
* nine polynomially separated orbits;
* five directed-rank-separated orbits;
* no new symmetric K3P move;
* no proper directed K3P containment inside the strong class;
* and two additional pre-lock sink-swap records separated by an exact quartic.

The reported sharpness theorem gives, for every (n\ge3), two networks

[
W_n,W'*n\in
W*{\mathrm{TC}}\setminus S_{\mathrm{TC}}
]

whose strict continuous-time K3P images share a common full-dimensional regular germ of dimension

[
6n-3.
]

The ordinary triangle ambiguity is realized through a common smooth rank-(14) germ in the quartic hypersurface

[
H_{14}=Z(F_{H_{14}})\subset\mathbb A^{15},
]

not through an ambient-open rank-(15) three-leaf germ.

The remaining problem is not open-ended theorem discovery. It is to:

1. reconstruct the cloud result from the attached files on the local machine;
2. independently certify every load-bearing finite claim;
3. repair the failing clean-room H21-01 automorphism/transport check without weakening it;
4. complete all mutation and regeneration gates;
5. integrate the proof into one polished mathematical paper and reader supplement;
6. create immutable, replayable release artifacts;
7. prepare publication packages; and
8. return a final honest theorem and release status.

Do not return another progress checkpoint. Complete the program.

---

# 1. Local-machine operating constraints

The machine is an Apple Silicon M1 Pro MacBook Pro with:

```text
10 CPU cores
16 GB RAM
macOS
```

Use resource-conscious defaults:

* at most two heavy symbolic workers concurrently unless benchmarks prove that more improves throughput;
* atomic per-record output;
* deterministic resumability;
* explicit peak-memory logging;
* no large in-memory accumulation when a streamed representation is possible;
* no destructive modification of supplied source packages;
* and no dependence on cloud-only paths, URLs, content references, or hidden state.

Use `caffeinate` or the local equivalent for long runs.

Every long computation must:

* checkpoint frequently;
* resume from canonical record IDs;
* reject stale records;
* preserve partial exact evidence;
* write logs continuously;
* and leave the repository in a known state after interruption.

Do not provide a future time estimate instead of performing available work.

---

# 2. Source-material rule

The attached local files are the only authoritative file inputs.

At the beginning, inventory every attachment and archive into:

```text
LOCAL_INPUT_INVENTORY.json
LOCAL_INPUT_INVENTORY.md
```

For every input record:

```text
original filename
local path
byte size
SHA-256
archive type
claimed role
detected contents
source manuscript or package
cloud-stage provenance if encoded
status
```

Do not assume that the cloud researcher’s prior directory structure exists locally.

Do not infer missing certificate contents from prose summaries.

When an expected file is missing:

1. search the attached archives;
2. search generated manifests;
3. regenerate it from primitive graph or algebra inputs where possible;
4. bind the regenerated object to exact inputs and commands;
5. record the discrepancy in the adversarial failure log.

Do not recreate a missing theorem certificate merely by copying its expected answer into a new file.

---

# 3. Create one canonical local project

Construct a clean project root such as:

```text
k3p_level2_identifiability_final/
```

Use a layout including:

```text
input_frozen/
topology/
model_domain/
three_port/
cut_recovery/
bridge_fibre/
marginals/
four_port_atlas/
restoration/
probes/
triangle_h14/
sharpness/
clean_room/
mutations/
manuscript/
supplement/
release/
submission/
history/
```

Place immutable copies of all supplied source materials under `input_frozen/`.

Create:

```text
INPUT_LOCK.json
SOURCE_PROVENANCE.md
ACTIVE_MANIFEST.json
SHA256SUMS
```

Every active theorem artifact must be reachable from `ACTIVE_MANIFEST.json`.

Move superseded, failed, or exploratory materials into `history/` with explicit status labels. They must not be discoverable as active proof evidence through ambiguous filenames.

No final command may depend on a file outside the canonical project root.

---

# 4. Freeze the claimed theorem before replay

Create:

```text
FINAL_CLAIM_LOCK.md
FINAL_CLAIM_LOCK.json
```

The lock must state the exact claims to be certified.

## 4.1 Principal positive domain

For a K3P edge spectrum

[
(1,c,g,t),
]

the principal physical domain is

[
\mathcal D_{3,+}
================

\left{
(c,g,t)\in(0,1)^3:
\begin{array}{l}
1+c-g-t>0,\
1-c+g-t>0,\
1-c-g+t>0
\end{array}
\right}.
]

Every inheritance probability is in ((0,1)).

## 4.2 Strict continuous-time domain

[
\mathcal D_{3,\mathrm{CT}}
==========================

\left{
(c,g,t)\in(0,1)^3:
c>gt,\quad
g>ct,\quad
t>cg
\right}.
]

## 4.3 Main classification

For binary standard semi-directed strongly tree-child level-2 networks on the same labelled leaf set:

[
N\preceq_{3,+}N'
\iff
N\equiv_{\triangle}N'
\iff
N\bowtie_{3,+}N'.
]

The same equivalence holds with (+) replaced by strict continuous time.

No proper one-sided regular-germ containment occurs inside the stated strong class.

## 4.4 Triangle geometry

The three ordinary triangle orientations:

* have generic normalized rank (14);
* have the same irreducible quartic image closure (H_{14});
* share a common strict continuous-time smooth rank-(14) analytic germ;
* and remain the sole generic topological ambiguity inside the strong class.

They do not fill the ambient normalized (15)-dimensional three-leaf K3P tensor space.

## 4.5 Four-port classification

The fourteen locked nontrivial orbits have final categories:

```text
Polynomially separated:
H21-01
H21-03
H21-04
H21-05
H21-06
L20-01
L21a-01
L21b-01
L23-02

Directed-rank separated:
H21-02
L20-02
L21a-02
L21b-02
L23-01
```

No new move survives.

The complete post-quadratic raw accounting is:

[
40=38+2,
]

where:

* 38 raw records form the fourteen locked orbits;
* two additional rank-(24) (\theta_3) sink-swap presentations are not graph isomorphisms and are separated by an exact eight-term quartic.

## 4.6 Weak-class sharpness

There are three-leaf networks (W_3,W'_3) such that:

* both lie in (W_{\mathrm{TC}}\setminus S_{\mathrm{TC}});
* their rooting censuses are
  [
  W_3:(5,2,3),\qquad
  W'_3:(7,2,5);
  ]
* they are nonisomorphic;
* they are not ordinary-triangle-equivalent;
* their strict continuous-time K3P maps have rank (15) at one common certified physical tensor;
* and their images share an ambient-open regular (15)-dimensional germ.

Identical K3P cherry substitution yields, for every (n\ge3), a common full-dimensional germ of dimension

[
6n-3.
]

## 4.7 Outer-class obstruction

The supplied exact tree–double-theta collision:

* gives a proper tree-to-theta local containment;
* has tree rank (9) and theta rank (15);
* has a (23)-dimensional local collision locus;
* extends to strict continuous time;
* and lies outside (W_{\mathrm{TC}}).

It is not the sharp class-boundary example.

---

# 5. Mandatory first replay: primary exact chain

Before editing the manuscript, replay every primary theorem artifact locally.

Create one command:

```bash
bash reproducibility/verify_primary.sh
```

It must verify, from primitive inputs:

1. K3P inverse-Fourier transition formulas.
2. The exact principal stochastic domain.
3. The exact continuous-time inequalities.
4. Strict isotropic near-identity subdivision.
5. Root-movement invariance.
6. Pointwise cut recovery.
7. Tree–ordinary-sunlet separation.
8. The rank-(14) ordinary-triangle calculation.
9. The exact (H_{14}) quartic.
10. Smoothness of the common triangle point:
    [
    F_{H_{14}}(q_0)=0,\qquad
    \nabla F_{H_{14}}(q_0)\ne0.
    ]
11. Rank (14) of all three triangle orientations.
12. The supplied rank-(15) double-theta collision.
13. Its exact rooting census.
14. The complete three-sector bridge action.
15. Analytic incidence-normalizer rank.
16. Physical local-product lifting.
17. Triple-sector marginal products and physical lifts.
18. All six four-port source ranks.
19. The fourteen-orbit lock.
20. All nine polynomial separators.
21. All five directed-rank separators.
22. The two pre-lock sink-swap quartics.
23. The exact (40=38+2) accounting.
24. The Krawczyk sharpness certificate.
25. Rank (15) of both weak-class maps throughout the certified box.
26. All stochastic and continuous-time inequalities throughout that box.
27. The all-(n) cherry determinant.
28. Rooting-class persistence under cherry substitution.

Use exact arithmetic or rigorous directed interval arithmetic.

A transcript saying “PASS” is insufficient unless every check is bound to exact files and hashes.

---

# 6. Repair the clean-room H21-01 failure correctly

The clean-room verifier currently stops on an H21-01 source-automorphism or double-coset transport check.

This is the highest-priority certification task.

Do not patch the verifier to accept the primary implementation’s stored witness.

Do not loosen the expected relation.

Do not skip the check.

Independently reconstruct:

1. the source mixed graph;
2. the target mixed graph;
3. all vertex types and arrowhead endpoints;
4. all physical port labels;
5. source and target incoming roles;
6. repair tags;
7. source and target automorphism groups;
8. the relevant port-permutation action;
9. the source–target double cosets;
10. all induced Fourier-coordinate transports;
11. and the raw records belonging to H21-01.

Then compare the primary and clean-room choices.

Possible acceptable outcomes:

### Outcome A — different equivalent representatives

If both implementations choose different valid automorphism or double-coset representatives:

* prove their equivalence;
* normalize them to a common canonical relation;
* verify that both induced coordinate transports carry the same target identity to equivalent source pullbacks;
* and verify all raw records in the orbit.

### Outcome B — primary transport defect

If the primary implementation transported a valid polynomial through an incorrect graph or coordinate action:

* repair the primary classification;
* rerun every affected raw record;
* determine whether the theorem still holds;
* update the orbit lock and manifests;
* and record the failure transparently.

### Outcome C — clean-room defect

If the clean-room graph-isomorphism or automorphism logic is wrong:

* fix it from graph definitions;
* add a regression test;
* preserve the failing historical version;
* and rerun the entire independent verifier.

The final clean-room verifier must independently prove:

* raw-orbit coverage;
* source/target direction;
* graph transport;
* coordinate transport;
* exact target vanishing;
* and strict source nonvanishing.

Create:

```text
H21_01_TRANSPORT_AUDIT.md
H21_01_TRANSPORT_AUDIT.json
```

---

# 7. Independently replay all fourteen orbits and two sink swaps

After repairing H21-01, run the clean-room implementation over the complete local residue.

Create:

```bash
bash reproducibility/verify_clean_room.sh
```

For every relation, the clean-room implementation must independently reconstruct:

* source and target graphs;
* parameter order;
* output-coordinate order;
* source-to-target direction;
* automorphism quotient;
* exact rank;
* exact polynomial or function-field certificate;
* and physical source evaluation.

For each polynomially separated orbit:

1. regenerate the target identity;
2. prove exact vanishing on the complete target map;
3. evaluate it at a strict rational source point;
4. prove exact nonzero value;
5. transport it to every raw relation in the orbit.

For each directed-rank-separated orbit:

1. regenerate the selected observable set;
2. compute an exact nonzero source Jacobian minor;
3. prove the target rational function-field upper bound;
4. establish the strict rank inequality;
5. verify every raw relation covered by the orbit transport.

For the two sink-swap records:

* independently verify that they are not labelled graph isomorphisms;
* regenerate the exact quartic;
* prove target vanishing and source nonvanishing.

The clean-room census must reproduce:

```text
14 canonical nontrivial orbits
38 raw records in those orbits
2 separately classified sink-swap records
0 unresolved records
```

---

# 8. Krawczyk sharpness replay

The sharpness theorem depends on a rigorous interval certificate and must receive an independent replay.

Do not rely on the primary certifier’s final Boolean.

Implement or use a separately structured rational/interval verifier that checks:

1. the exact equality system;
2. the rational box center;
3. box radius;
4. every frozen free parameter;
5. the approximate inverse or preconditioner;
6. interval Jacobian enclosure;
7. the Krawczyk operator;
8. strict self-mapping;
9. uniqueness of the real root;
10. source rank-(15) minor exclusion from zero;
11. target rank-(15) minor exclusion from zero;
12. every inheritance interval inside ((0,1));
13. every transition-probability interval strictly positive;
14. every edge eigenvalue interval inside ((0,1));
15. every continuous-time inequality bounded strictly away from zero.

Use directed rounding or exact rational interval arithmetic.

Record numerical summaries such as the reported radius (10^{-50}) and containment ratio only after reproducing them. Do not hard-code them as assumptions.

Create:

```text
K3P_SHARPNESS_KRAWCZYK_CERTIFICATE.json
K3P_SHARPNESS_KRAWCZYK_REPORT.md
```

The reader report must explain, in mathematical terms, why the interval theorem proves a genuine common rank-(15) germ.

---

# 9. Complete the three-sector global infrastructure audit

Verify that the local classification actually supports the claimed global theorem.

## 9.1 Bridge fibre

Prove and replay the complete positive normalized fibre:

[
P_v(\mathbf h)\mapsto
P_v(\mathbf h)
\prod_{e\ni v}
(a^C_{v,e})^{\mathbf1[h_e=C]}
(a^G_{v,e})^{\mathbf1[h_e=G]}
(a^T_{v,e})^{\mathbf1[h_e=T]}.
]

Verify:

* all-zero normalization;
* one independent scale per nonzero character;
* no hidden sector equality;
* no fixed-label (C,G,T) permutation gauge;
* freeness;
* no bridge-tree holonomy;
* and exact physical saturation.

## 9.2 Physical local product

Regenerate the strict serial splitting construction and verify all endpoint and residual edge triples remain in (\mathcal D_{3,+}).

## 9.3 Marginal open image

For every selected restriction used by the atlas, restoration, and probes, verify:

[
(c_1,g_1,t_1),\ldots,(c_m,g_m,t_m)
\mapsto
\left(\prod_i c_i,\prod_i g_i,\prod_i t_i\right).
]

Check:

* complete switching signatures;
* split complements;
* invisible edge classes;
* inheritance transports;
* physical surjectivity;
* parameter rank;
* image-tangent rank;
* and local openness.

## 9.4 Rank-(14) triangle contextualization

Verify that:

* the common point is smooth in (H_{14});
* each orientation submerses onto the same local (H_{14}) germ;
* all redirected triangles enter one common context-contraction map;
* one generic rank choice suffices for all orientations;
* the constant-rank theorem supplies physical sections;
* and the contextual germ is full-dimensional relative to each complete network image.

Do not replace (H_{14}) with ambient (\mathbb A^{15}).

## 9.5 Physical bridge gluing

Verify the small-coordinate principal-domain construction and the strict continuous-time isotropic construction for all bridges simultaneously.

---

# 10. Regenerate restoration and coherent probes

The final local ledger may affect restoration assignments and probe parentage.

Regenerate, from the corrected final classification:

* every fixed-full restoration parent;
* every child;
* every terminal;
* every one-port probe;
* every two-port probe;
* every parent restriction;
* and every triangle transport.

Require:

```text
zero missing children
zero duplicate children
zero unresolved leaves
zero restoration cycles
zero broken parent transports
zero incoherent triangle choices
```

Do not reuse a K2P or earlier K3P restoration manifest without exact regeneration.

Create:

```text
RESTORATION_MANIFEST.json
ONE_PORT_PROBE_MANIFEST.json
TWO_PORT_PROBE_MANIFEST.json
GLOBAL_TRANSPORT_MANIFEST.json
```

The human-readable supplement must explain:

* why restoration uses direct marginals of the original containment;
* how one-port probes locate omitted labels;
* how two-port probes recover order;
* and why all local triangle choices are globally coherent.

---

# 11. Mandatory mutation suite

Create one command:

```bash
bash reproducibility/test_mutations.sh
```

At minimum reject:

* deletion of one of the 40 post-quadratic raw records;
* duplication of one record;
* reassignment of a raw relation to the wrong orbit;
* reversal of source and target;
* corruption of H21-01 automorphism transport;
* replacement of one quartic by another valid quartic;
* deletion of one monomial of (F_{H_{14}});
* use of an H14 polynomial on the wrong marginal;
* changing one directed-rank observable set;
* changing one source-rank minor;
* weakening one target rank upper bound;
* changing a port permutation;
* changing an incoming role;
* changing a repair tag;
* merging two source embeddings;
* imposing the K2P equality (C=T);
* swapping (C,G,T) without transporting coordinates;
* deleting one transition inequality;
* accepting a boundary inheritance probability;
* accepting an interval rank determinant containing zero;
* widening the Krawczyk box until self-mapping fails;
* changing one Krawczyk matrix entry;
* omitting one restoration child;
* breaking one one-port parent;
* reversing one two-port order without transport;
* and allowing inconsistent triangle orientations.

Every required mutation must fail for the intended reason.

Record expected and observed failure classes.

---

# 12. Long-run entry points

Provide three deterministic entry points.

## Quick

```bash
bash reproducibility/verify_quick.sh
```

Checks:

* manifests;
* hashes;
* frozen theorem counts;
* primary exact certificates;
* clean-room normalized outputs;
* Krawczyk certificate;
* restoration/probe summaries;
* manuscript crosswalk;
* and mutation summaries.

## Full

```bash
bash reproducibility/verify_full.sh
```

Additionally regenerates:

* all fourteen orbit certificates;
* both sink-swap certificates;
* clean-room orbit classification;
* full restoration forest;
* all probe ledgers;
* and sharpness rank and interval evidence.

## Full regeneration

```bash
bash reproducibility/verify_regenerate_all.sh
```

Starts from primitive graph and model inputs and rebuilds all active proof evidence.

All scripts must:

* leave the tracked repository clean;
* report exact elapsed time;
* report peak memory;
* fail closed;
* and emit complete transcripts.

---

# 13. Final paper: one article only

Produce one definitive K3P article.

A suitable provisional title is:

> **Generic Identifiability and Directed Containment for Strongly Tree-Child Level-2 Networks under K3P**

A stronger conceptual alternative is:

> **Triangle Hypersurfaces and a Sharp Identifiability Boundary for Level-2 K3P Networks**

Choose the title after the manuscript is complete. Do not imply a broader theorem than proved.

The paper should be self-contained enough to read without the JC or K2P papers, while reusing and citing their model-independent topology framework rather than repeating all engineering history.

## Required structure

### 1. Introduction

Explain:

* why exact-distribution identifiability is an information limit;
* prior level-1 JC/K2P/K3P results;
* prior level-2 JC and K2P companion classifications;
* the known rank-(15) outer-class double-theta collision;
* the K3P-specific obstacle posed by three independent Fourier sectors;
* and the final K3P-SAME result.

State clearly that K3P agrees topologically with JC and K2P but has different local triangle geometry.

### 2. Main theorems

State:

* the principal-domain classification;
* generic identifiability;
* exact reconstruction;
* strict continuous-time classification;
* weak-class sharpness;
* and the outer-class directed collision as a separate proposition or discussion result.

### 3. Conventions and K3P model

Give:

* rooted and semi-directed conventions;
* strong/weak tree-child definitions;
* edge Fourier spectra;
* physical inequalities;
* continuous-time inequalities;
* observational relations;
* and triangle equivalence.

### 4. Physical domain and pointwise topology recovery

Include:

* strict subdivision;
* root invariance;
* pointwise cut theorem;
* and tree-of-blobs recovery.

### 5. Three-leaf K3P geometry

Include:

* tree model;
* tree–ordinary-sunlet separation;
* the exact (H_{14}) quartic;
* generic triangle rank (14);
* smooth common triangle germ;
* and the distinction from the ambient rank-(15) double-theta map.

### 6. Three-sector bridge fibre

State and prove:

* complete incidence action;
* analytic slices;
* physical saturation;
* and local product coordinates.

### 7. Marginal localization

State and prove:

* triple-product descriptors;
* physical lifts;
* tangent-space submersion;
* finite target selection;
* and remote-compensation exclusion.

### 8. Primitive factors and bounded classification

Explain:

* the reused cycle/four-theta topology universe;
* K3P source ranks;
* exact classification categories;
* the fourteen-orbit theorem;
* and the corrected (40=38+2) accounting.

Give a compact but complete table of all fourteen orbits and two sink swaps.

### 9. Restoration and arbitrary words

Explain:

* fixed-full restoration;
* one-port probes;
* two-port order recovery;
* coherent triangle transport;
* and maximum selected support size.

### 10. Global classification

Prove necessity and sufficiency.

The sufficiency proof must use the common smooth (H_{14}) germ, not an ambient triangle germ.

### 11. Genericity and reconstruction

Give the exact semialgebraic exceptional-set argument and terminating exact algorithm.

### 12. Strict continuous time

Prove the full transfer.

### 13. Sharpness

Present:

* the weak-not-strong base pair;
* the exact Krawczyk common point;
* rank-(15) submersions;
* strict continuous-time certification;
* cherry inverse;
* all-(n) dimension (6n-3);
* and class-status persistence.

### 14. Outer-class tree–double-theta obstruction

Explain why it lies outside weak tree-childness and why it is not the sharp boundary.

### 15. Unified Kimura-family perspective

Give a restrained comparison table:

```text
JC
K2P
K3P
```

Compare:

* number of nonzero character sectors;
* bridge scales per incidence;
* ordinary-triangle local dimension;
* weak-class ambiguity dimension;
* and final topology quotient.

A synthesis statement for the three completed companion results is encouraged.

Do not assert a theorem for arbitrary finite groups or arbitrary symmetry subgroups unless separately proved.

### 16. Scope and limitations

State:

* exact infinite-data setting;
* topology rather than all physical parameters;
* no finite-sample stability theorem;
* level two;
* strong tree-childness;
* positive eigenvalues;
* fixed state labels;
* and no claim about signed K3P unless separately audited.

### 17. Reproducibility and AI disclosure

State precisely:

* the finite machine dependency;
* primary and separately implemented replay;
* rigorous interval proof;
* mutation testing;
* no independent human specialist review;
* and author responsibility.

---

# 14. Reader supplement

Produce a concise but complete supplement containing:

1. dependency graph;
2. theorem-to-certificate crosswalk;
3. exact model-domain formulas;
4. (H_{14}) coordinate order and quartic;
5. triangle rank witnesses;
6. tree–sunlet SOS certificate;
7. three-sector normalizer matrices;
8. fourteen-orbit table;
9. every polynomial separator’s row, coordinate, and transport data;
10. every directed-rank witness;
11. the two sink-swap records;
12. restoration counts;
13. probe counts;
14. Krawczyk certificate summary;
15. sharpness rank witnesses;
16. cherry determinant;
17. clean-room architecture;
18. mutation list;
19. reproduction commands;
20. artifact hashes.

Avoid dumping raw machine output into the supplement. Put complete ledgers in the archive.

---

# 15. Adversarial manuscript audit

After drafting, run a fresh hostile review that does not assume the theorem is correct.

At minimum attack:

* use of rank (14) versus (15);
* smoothness and irreducibility of (H_{14});
* contextual full-dimensionality through a hypersurface germ;
* source-relative versus symmetric containment;
* physical versus Zariski containment;
* four-port universe exhaustiveness;
* (40=38+2) accounting;
* H21-01 transport;
* directed-rank logic;
* Krawczyk uniqueness and rank persistence;
* all-(n) cherry inversion;
* weak versus strong class membership;
* continuous-time gluing;
* genericity;
* and every claim of “sharp.”

Correct every valid objection and rerun all affected proof and release gates.

Do not use a prior favorable review as evidence.

---

# 16. PDF and source quality

Build and inspect:

* canonical article PDF;
* reader supplement PDF;
* Systematic Biology manuscript PDF;
* Journal of Mathematical Biology manuscript PDF;
* both cover letters;
* any anonymized-review variants required by the current journal instructions.

Check:

* every font embedded;
* no clipping;
* no overlapping TikZ labels;
* no unresolved references;
* no malformed equations;
* no broken hyperlinks;
* no placeholder metadata;
* consistent theorem numbering;
* consistent file paths;
* consistent title and abstract;
* and exact source-archive reproduction.

Create byte-for-byte source-archive tests for every delivered PDF.

---

# 17. Release artifacts

Create:

```text
k3p_level2_reproducibility.tar.gz
k3p_level2_reproducibility.tar.gz.sha256
RELEASE_ENVELOPE.json
RELEASE_ASSET_SHA256SUMS
FINAL_RELEASE_ENGINEERING_REPORT.md
clean_clone_transcripts/
```

The full archive should contain:

* primitive graph inputs;
* exact certificates;
* primary verifier;
* clean-room verifier;
* Krawczyk verifier;
* restoration and probe ledgers;
* mutation suite;
* manuscripts;
* source archives;
* environment files;
* and all active manifests.

Also create a compact verifier-entrypoint ZIP suitable for a manuscript-upload portal. It must clearly state that the full proof archive is external and canonical.

The release envelope must bind:

* source commit;
* tag;
* article PDF;
* supplement PDF;
* full archive;
* compact verifier archive;
* transcripts;
* and every submission package.

Do not create a self-referential archive hash inside the archive itself.

---

# 18. Zenodo and repository handling

Prepare an exact Zenodo-upload package but do not invent a DOI.

If authenticated repository access is available, create an immutable GitHub release only after every local gate passes.

Otherwise produce:

```text
ZENODO_UPLOAD_CHECKLIST.md
GITHUB_RELEASE_CHECKLIST.md
POST_DOI_REBUILD.md
```

The DOI-bearing journal package must be rebuilt after the real DOI is issued.

Do not claim a DOI that has not been minted.

Choose and document explicit licenses only with the user’s authorization. Do not infer a license.

---

# 19. Submission packages

Prepare dedicated packages for:

## Systematic Biology

Include:

* article;
* supplement;
* editable source if required;
* data/code availability statement pointing to the persistent repository;
* cover letter;
* author metadata;
* AI disclosure;
* and a portal upload map.

Systematic Biology’s code-deposit requirements must be reflected accurately.

## Journal of Mathematical Biology

Include:

* article;
* supplement;
* editable LaTeX source;
* identified supplementary materials;
* cover letter;
* code/archive citation;
* and upload map.

## Preprint or priority archive

Prepare a Zenodo-ready paper and source bundle.

If an arXiv package is desired, prepare it without assuming endorsement or submission access.

Do not create a bioRxiv package unless the current affiliation policy is independently satisfied.

---

# 20. Publication positioning

The final paper should claim the strongest supported contribution:

> The level-2 generic identifiability and directed-containment classification is the same under JC, K2P, and K3P: strong tree-childness yields topology recovery modulo ordinary triangle redirection, and weakening to weak tree-childness already permits robust full-dimensional failure.

But the K3P paper’s distinct contributions must remain visible:

* three independent Fourier sectors;
* pointwise K3P cut recovery;
* the rank-(14) (H_{14}) triangle hypersurface;
* complete fourteen-orbit classification;
* exclusion of proper directed K3P inflations in the strong class;
* rigorous Krawczyk sharpness;
* and all-(n) dimension (6n-3).

Do not market the work as a mechanical K2P extension.

Do not imply practical finite-sequence reconstruction.

Do not claim all group-based models beyond JC/K2P/K3P.

---

# 21. Final acceptance gates

Do not declare completion until every applicable gate passes.

## Gate A — local reconstruction

Every cloud artifact needed by the proof exists locally or has been regenerated.

## Gate B — primary replay

The full primary proof chain passes.

## Gate C — H21-01 repair

The clean-room transport issue is understood and resolved mathematically.

## Gate D — independent orbit replay

All fourteen orbits and two sink swaps pass independently.

## Gate E — sharpness replay

The Krawczyk certificate and rank persistence pass independently.

## Gate F — global infrastructure

Bridge fibre, marginal openness, H14 contextualization, restoration, probes, gluing, genericity, and reconstruction are all bound to active evidence.

## Gate G — mutations

Every mandatory mutation fails.

## Gate H — manuscript integrity

The final article contains no conditional theorem language, stale counts, or unsupported claims.

## Gate I — exact source reproduction

Every delivered PDF is reproduced byte-for-byte from its source archive.

## Gate J — clean runs

Quick, full, and full-regeneration suites pass from clean checkouts and leave them clean.

## Gate K — release binding

The final tag, commit, files, archive, transcripts, and manifests agree.

## Gate L — adversarial review

A fresh review finds no remaining mathematical or release blocker.

---

# 22. Required final response

Return one final completion report containing:

1. **Final theorem status**

   ```text
   K3P-SAME
   K3P-MODIFIED
   K3P-STRONG-FAILURE
   ```
2. Exact theorem statement.
3. Exact sharpness statement.
4. H21-01 diagnosis and repair.
5. Fourteen-orbit and (38+2) final census.
6. Primary replay status.
7. Clean-room replay status.
8. Krawczyk replay status.
9. Mutation-suite status.
10. Quick/full/regeneration runtimes and hashes.
11. Final article and supplement paths.
12. Full reproducibility archive path and SHA-256.
13. Submission-package paths.
14. Remaining human-only actions, such as:

    * choosing licenses;
    * minting a Zenodo DOI;
    * inserting the issued DOI;
    * and performing actual portal submissions.

Do not return a broad checkpoint.

Do not call the paper submission-ready if the clean-room verifier, mutation suite, source-reproduction test, or full-regeneration suite is still failing.

Do not reopen unrelated theorem searches unless a verifier exposes a genuine mathematical contradiction.

The completed project must leave the user with one exact paper, one exact supplement, one exact reproducibility archive, and a clear publication path.
