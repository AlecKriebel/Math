# Notebooks

Exploratory notebooks may be placed here. Authoritative construction,
verification, search, and certificate code must remain runnable from `src/`,
`verify/`, or `tests/` without notebook state.
