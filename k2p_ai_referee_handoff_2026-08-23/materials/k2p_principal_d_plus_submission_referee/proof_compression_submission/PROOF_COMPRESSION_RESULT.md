# Proof-compression result

Verdict: **`PC-PARTIAL`**.

The bounded theorem has zero unresolved mathematical records, and the frozen
principal-domain conclusion remains **`K2P-SAME`**.  The adversarial proposal
to compress the submission was therefore useful, but its strongest possible
reading is not justified: neither the restoration forest nor the probe
premises reduce to a small exact representative table.

What is proved compactly:

- the primitive completion-count formula and exact raw universe totals;
- the literal quadratic families and direction-safe high-degree families;
- the exact restoration proof-mechanism census and depth-two termination; and
- the arbitrary attachment-word theorem from one-/two-port coherence.

What remains load-bearing:

- 75 exceptional rank representatives and all direction-sensitive whole-map
  \(T_i\) evidence;
- 27 transported high-degree bodies;
- 997 restoration assignments, 2,540 member roots, 36,824 edges, and 16
  algebra transport classes; and
- 176 probe anchors, 29,964 one-port rows, 544,571 two-port rows, 67,741 exact
  transports, and 4,379 parent restrictions.

The 297 restoration archetypes compress exposition only; they are not a
proved graph-transport quotient.  Similarly, the word theorem is uniform but
does not eliminate its finite separation and coherence premises.

## Reproduction

From the project root:

```bash
.venv/bin/python proof_compression_submission/verify_compressed_release.py --check
.venv/bin/python proof_compression_submission/verify_old_new_equivalence.py --check
.venv/bin/python proof_compression_submission/run_compression_mutations.py --check
```

The first command is the minimal fail-closed check.  The second replays all
load-bearing old/new comparisons.  The third requires every targeted
corruption to be rejected, including a run under optimized Python.
