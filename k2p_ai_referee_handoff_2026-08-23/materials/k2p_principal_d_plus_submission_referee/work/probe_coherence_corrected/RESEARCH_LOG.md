# Corrected all-primitive probe coherence log

## 2026-08-21 12:49 PDT — program opened

- Scope: the clean principal-domain K2P one-/two-port coherence closure.
- Frozen equality input: 176 physical anchor records, 2,206 source sites,
  2,206 target sites, and 29,964 first-probe Cartesian pairs from the
  independently replayed root-invariant input contract.
- Bound primitive releases: corrected four-port overlay, corrected restoration
  forest v3, theta2 fixed-full closure, and cycle promotion package.
- Mandatory classifier order: exact labelled isomorphism/ordinary triangle;
  displayed quartet; direct full-map `T_i` zero/strict-sign; then only an exact
  lifted marginal or reviewed algebra certificate.
- Forbidden dependencies: rooted `triple_type`, rooted `clean_restrict`, the
  revoked 39/172-anchor census, and topology caches not invariant under exact
  semi-directed isomorphism.
- Candidate grammar: every edge of the suppressed semi-directed mixed graph;
  the two rooted halves of its artificial-root edge are one certified site.
- Success gate: complete one- and two-port raw ledgers, unique coherent parent
  restriction for every equality survivor, one global ordinary triangle,
  zero unresolved/incoherent rows, independent replay, and fail-closed
  mutations.
- Current best-guess completion toward this probe subgoal: 8%.

## 2026-08-21 13:05 PDT — corrected one-port gate

- Independently reconstructed all 176 physical equality-anchor records from
  primitive graphs and the frozen locators. Exact census: 2,206 sites on each
  side and 29,964 source/target site pairs.
- Replaced the revoked rooted restriction oracle with direct expansion of the
  five Fourier coordinates in `T_i` on each original full graph. The strict
  side is certified by exact tensor Bernstein coefficients on the full open
  physical-parameter cube; boundary incidence multihomogeneity is recorded.
- Complete one-port result: 27,758 displayed-quartet mismatches, 99 exact
  full-map `T_i` strict-sign separators, 1,915 labelled isomorphisms, and 192
  ordinary-triangle relations. Zero unresolved and zero incoherent.
- 2,107 exact equality survivors are the complete input to the two-port gate.
  Runtime 166.7 s; peak RSS 449,593,344 bytes.
- Best-guess completion toward the complete probe-coherence release: 45%.

## 2026-08-21 13:10 PDT — two-port run launched

- The two-port run enumerates every mixed attachment site only above the 2,107
  exact one-port equality parents. It also reconstructs the reverse-order
  one-port marginal for every equality leaf and requires membership in the
  same base anchor's complete one-port equality universe.
- Single-process execution is intentional for the referee package: projected
  30–35 minutes on the local M1 and observed RSS about 0.36 GiB during the
  first 100 parents.

## 2026-08-21 14:54 PDT — complete corrected probe closure

- Final optimized build PASS. One port: 29,964 raw pairs = 27,758 displayed
  quartets + 99 direct full-map `T_i` strict signs + 1,915 isomorphisms + 192
  ordinary triangles. It retains 2,107 exact equality parents.
- Two port: 544,571 raw pairs = 511,266 displayed quartets + 576 direct
  full-map `T_i` strict signs + 30,969 isomorphisms + 1,760 ordinary
  triangles. All 32,729 equality leaves have an exact reversed one-port
  marginal above the same base anchor.
- Zero unresolved records and zero incoherent mappings. Every relation map
  restricts its parent vertices and attachment site. Every triangle map uses
  the inherited base triangle and common reticulation.
- Exact theorem optimizations: child relation matching is attempted only at
  the unique parent edge-map image site, and child quartet tests inspect only
  quartets containing the new label after exact equality of the parent deck.
  These are proof consequences, not heuristic topology caches.
- Build payload:
  `674853fa730c4f54b9ba264d539a51591c8b926ad444195e68df086c26f83825`.
  Runtime 2,762.68 s; peak RSS 451,903,488 bytes.
- Independent streaming replay PASS: payload
  `b5be07041c7e52cce4828982f295032894c351f41a97cd85f8c753709fe1a8d2`,
  runtime 16.65 s, peak RSS 71,942,144 bytes. It independently replays all 118
  strict-polynomial Bernstein tensors and validates 156 `T_i` relation
  certificates, 638 quartet certificates, 67,741 exact transports, 4,379
  parent restrictions, both Cartesian ledgers, and every reverse/global-T
  reference.
- Fail-closed mutations PASS: 15/15 rejected, plus a clean nondefault
  `PYTHONHASHSEED=12345` replay. Mutation payload
  `320197b72a2320b50ce3f52a13ff0119a94340e51778bb929ce30311a9170f13`;
  peak RSS 73,596,928 bytes.
- During mutation development a temporary-case certificate was mistakenly a
  symlink, so summary-only mutations altered the live summary. None of the
  primary ledgers changed. The deterministic resealer reconstructed the
  summary from those ledgers and primitive anchors and recovered the exact
  pre-incident logical payload `674853fa...`; the mutation driver now copies
  the summary and mutation tests confirm source immutability.
- Best-guess completion toward the probe-coherence subgoal: 95%, pending the
  separate adversarial primitive-graph regeneration and global-theorem
  consumer audit.

## 2026-08-21 15:12 PDT — independent site-map partition sealed

- A separate streaming verifier reconstructed the base site maps from the
  frozen 176-anchor input contract and every one-port-parent site map from the
  public exact mixed-edge transport records. It did not consume the builder's
  compatibility flag.
- One port partitions exactly into 2,206 compatible and 27,758 incompatible
  site pairs. Compatible pairs are 1,915 isomorphisms, 192 ordinary triangles,
  and 99 direct full-map `T_i` separations; every incompatible pair is a
  displayed-quartet mismatch.
- Two port partitions exactly into 33,305 compatible and 511,266 incompatible
  site pairs. Compatible pairs are 30,969 isomorphisms, 1,760 ordinary
  triangles, and 576 direct full-map `T_i` separations; every incompatible
  pair is a displayed-quartet mismatch.
- The audit has payload
  `a1f80d5ed090a1c6e31067123f0739392384d5124e75296cc76035430710497c`,
  runtime 4.62 seconds, and peak RSS 50,806,784 bytes. Zero unresolved rows.
- Best-guess completion toward the probe-coherence subgoal: 98%, pending only
  the separate adversarial primitive-graph/global-theorem consumer audit.

## 2026-08-22 12:29 PDT — operational telemetry removed from certificate bytes

- A detached full regeneration reproduced all 574,535 probe rows and the
  logical payload, but the downstream independent replay bound the byte hash
  of `probe_coherence_certificate.json`, whose embedded wall-clock runtime was
  necessarily different. This caused a fail-closed logical-comparison stop
  after the 2,949.341-second primitive build.
- Removed operational runtime from the authoritative certificate bytes and
  print it only to stderr. The mathematical payload remains
  `674853fa730c4f54b9ba264d539a51591c8b926ad444195e68df086c26f83825`;
  the stable certificate SHA-256 is now
  `93de7b0dd3aa581bdf12288eae8cb9ac42f20a9d9bb3eab35eee8ef9a759d390`.
- The deterministic ledger resealer reproduces the stable certificate exactly.
  The refreshed independent replay passes with payload
  `971d0fdc3b84cfd4001574ba80d272841eb809213b24984009dda4556ebc6261`,
  and all 15 targeted mutations plus the nondefault hash-seed replay pass.
- Best-guess completion toward the probe-coherence subgoal remains 100%; the
  remaining work is transitive release rebinding and a fresh clean full replay.
