# Exact first-child closure for the 997 K2P restoration parents

## Finite statement

Fix the corrected v4 four-port release and its primitive completion grammar.
Every one of its 997 canonical `restoration_parent` records expands into its
member presentations by correlating target attachments with the same
`target_index` and `port_match`.  There are exactly 2,540 such members.  Their
dummy-role multiplicities are

\[
568\cdot 1+1260\cdot 2+712\cdot 3=5224.
\]

For each role, the restored source port can subdivide any of the seven frozen
non-root, non-leaf source edges.  Hence the complete physical first-child
layer has

\[
5224\cdot 7=36{,}568
\]

labelled source-to-target presentations.  The certificate retains the parent
ID, restored role, new label, source edge, target dummy attachment, port
match, and all remaining roles on every row.

Exact labelled mixed-incidence deduplication reduces these presentations to
2,240 directed relation classes.  The complete proof census is

| terminal certificate | raw children |
|---|---:|
| displayed-quartet mismatch | 35,758 |
| strict tree--sunlet sign | 646 |
| exact K2P quadratic | 148 |
| lifted exact `F_(2,112)` quartic | 16 |
| labelled mixed-graph isomorphism | 0 |
| ordinary triangle quotient | 0 |
| unresolved | 0 |

For every algebraic row the target pullback is the zero polynomial and the
source pullback is nonzero over characteristic zero.  A rational strict
`D_+` source value is also recorded.  Every polynomial is multihomogeneous in
the two K2P incidence sectors, so its zero/nonzero conclusion is preserved by
the physical bridge scaling action.

The 16 quartic rows are not inferred from a modular zero.  The verifier loads
the independently certified four-port `F_(2,112)`, applies the recorded port
permutation, embeds its four Fourier characters into the selected quartet of
the five-port tensor, and expands both graph-derived pullbacks exactly.

## Conditional restoration consequence

Suppose the paired K2P marginal open-image/restoration lemma has the following
form: a full source-to-target containment whose selected four-port marginal is
one of these parents induces, after restoring any actual omitted physical
role, a direct five-port containment for its recorded target attachment and
for one of the seven possible source insertion edges.  Then no frozen parent
can arise from a full containment.  Indeed, its induced child is one of the
36,568 rows, while every such row has an exact source-to-target separation
certificate.

This implication terminates at depth one.  The 712 three-role roots are real
and must not be erased from the census, but none reaches a six- or seven-port
state because its first restored child is already separated.  Consequently
the generated forest has no cycle, missing child, or unresolved leaf.

## Claim boundary

The certificate proves the finite child premise.  It does not, on its own,
prove that the frozen four-port parents exhaust every topology/rank case, nor
does it reprove the marginal open-image/restoration lemma, coherent global
probe theorem, bridge gluing, or generic reconstruction theorem.  Those are
separate load-bearing gates and must be cited or replayed before claiming the
final K2P classification.
