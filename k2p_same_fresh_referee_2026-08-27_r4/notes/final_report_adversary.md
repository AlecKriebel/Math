# Final adversarial audit of the integrated referee report

**Audit date:** 27 August 2026  
**Report audited:** `reports/FRESH_ADVERSARIAL_REFEREE_REPORT_2026-08-27.md`  
**Completion estimate:** 100% of the assigned final report/evidence consistency audit.

## Disposition

The integrated scientific recommendation should remain **HOLD**. The status
lines are coherent as written: Mathematics **PASS**, computational evidence
**HOLD**, reproducibility **FAIL**, and human metadata/release **PASS**.

- **ACCEPT is not justified** because the supplied compressed-ledger replay
  accepts a conflicting duplicate member after a coherent layer reseal, and
  documented portable production entry points do not all reject optimized
  Python. The latter was shown to turn a target-zero assertion failure into a
  false successful separator record.
- **REJECT is not justified** because neither attack falsifies a clean sealed
  row, the central finite census, or a mathematical implication. Fresh quick
  (23/23), full (41/41), and release-mutation (25/25) runs passed, and the
  clean evidence received substantial independent checking.
- **HOLD exactly matches the requested decision rule:** no counterexample was
  established, but two specific fail-closed/reproducibility blockers remain.

No substantive verdict change is required.

## Eight-section protocol coverage

The report contains the required eight numbered sections in the requested
order: verdict, separate statuses, claim matrix, numbered findings, execution
ledger, independent attacks/mutations, scope/literature audit, and required
actions. It now also supplies or points to:

- exact article/supplement locations for every theorem layer;
- a mathematical/computational/provenance evidence-class map;
- exact authoritative hashes and the complete submitted C01--C13
  producer/replay registry;
- fresh top-level and 23/41-child command ledgers;
- all requested finite censuses;
- expected versus observed mutation mechanisms and honest independence
  qualifications;
- exact defect reproducers, severity, theorem effect, remedy, and recursive
  reseal scope;
- a complete scope exclusion and primary-literature audit; and
- a precise legacy-name/current-command mapping with no unrun current release
  gate.

The obsolete `SUBMISSION_BINDING.json` five-dependency vocabulary is now
handled explicitly. The current outer manifest declares three supplemental
execution dependencies, not five. I verified their exact live rows and tag
bytes:

| Current supplemental dependency | Bytes | SHA-256 |
|---|---:|---|
| `output/referee/README.md` | 2,791 | `d7cd6562816484e1070e28294e248a15312baaaacb2132ff98d3c0112ef4b707` |
| `output/referee/REFEREE_BUNDLE_CONTENTS.json` | 76,995 | `6fd50449e450d45176346b47e19a06f667fd29a1c190f66032793df562eaec6f` |
| `output/referee/build_referee_bundle.py` | 7,401 | `a0e0c6007dabf761dc443fff051e6058754d0e64a132b5aecbf9303f8850234a` |

All three are intentionally outside the 406-file inner closure, occur in the
84-file outer submission partition, match annotated tag
`k2p-same-biorxiv-v1.0.3`, and have individual omission mutations. The content
ledger is necessarily not its own member and is required by
`build_theorem_artifact_crosswalk.py:18`.

## Live consistency checks

1. **Report hashes.** The final report contains 56 full 64-hex hash values.
   Forty-eight are exact hashes of retained live files. The remaining eight
   are declared payload/content roots or the deleted-temporary mutant gzip:
   all eight were independently recomputed from their retained source JSON,
   manifests, or deterministic mutation. No unexpected unmatched hash
   remains.
2. **Submitted crosswalk.** All 137 unique path/hash/byte rows across the 13
   claims' authoritative, producer, replay, and mutation artifact arrays
   exist and match live files. The 62 producer/replay path-hash appearances in
   `EVIDENCE_REGISTRY.md` also match exactly.
3. **Harness child ledgers.** Every name, return code, elapsed time, stdout
   hash, and stderr hash in the 23-row quick table and 41-row full table equals
   the corresponding fresh JSON report. There were zero mismatches.
4. **Top-level outputs.** All 77 retained files in `logs/` have their exact
   SHA-256 indexed in the report, registry, or execution ledger. The current
   quick, full, mutation, PDF, archive, independent-math, boundary, duplicate,
   and optimized-attack hashes were spot-checked directly.
5. **Key source/evidence files.** Every hash in the report's authoritative
   artifact table, the five source files, both PDFs, release lock, theorem
   crosswalk, three review finding artifacts, mutation patches, rechecker,
   and all five specialist notes matches the live file.
6. **Counts and runtimes.** The printed raw4, theta2, cycle, restoration,
   probe, direct-degree, archive, PDF, and child-layer counts agree with their
   fresh JSON evidence. External versus internal harness runtimes are not
   conflated.

At the end of this audit, the three integrated files had these hashes:

| File | SHA-256 |
|---|---|
| `reports/FRESH_ADVERSARIAL_REFEREE_REPORT_2026-08-27.md` | `2f7b667816cbb8ecdb1d431c7ae7dad2405f773b8e92e3a8f3fca7798c128a7f` |
| `EVIDENCE_REGISTRY.md` | `3ee488db6f1478b4fc4fc920f139795ea027b91d59b9c37b7dd82dbf95c9c629` |
| `EXECUTION_LEDGER.md` | `e7869ec1b72d81c2dae54b8428ef2e09685d112ea04871ac2a181806dea7f12f` |

The report hash should be recomputed if the root referee makes any later edit.

## Corrections applied during this adversarial pass

I made only report/ledger corrections; I did not edit submission source or
change the substantive recommendation.

1. Replaced an unsupported approximate authority-audit runtime in
   `EXECUTION_LEDGER.md` with the explicit unavailable marker.
2. Corrected the producer/verifier-registry wording: the complete submitted
   registry is the theorem-artifact crosswalk; the review registry indexes it.
3. Distinguished generated-input build omissions from bibliography rejection
   by the outer manifest mutation suite.
4. Corrected the primitive-audit independence description. The review script
   imports the submitted grammar and calls its slow/fast descriptor pair for a
   disclosed 17-record consistency sample; those descriptors are not used to
   decide the independent structural predicates or census. The earlier claim
   of no submitted canonicalizer call was factually false and has been
   removed.
5. Added exact retained SHA-256 values for the optimized semantic mutation's
   atlas patch, input-lock patch, and independent false-certificate rechecker.
6. Made explicit that the symbolic-rank suite includes a production-verifier
   mutation substituting sampled-rank evidence for a symbolic upper bound.
7. Corrected the PDF omission/reseal scope: the changed release-lock hash is
   printed at `supplement.tex:775`, so the supplement source, both submitted
   PDF builds/build report, the exact five-source bioRxiv archive, and their
   applicable sidecars must be regenerated.
8. Replaced the semantic mutation pair's unsupported `bounded` runtime label
   with `not retained`; no runtime was inferred.
9. Added the restoration suite's cycle attempt and invalid-`D_plus` mutation
   to the nested coverage table.
10. Replaced `published` with the accurate `supplied` for the probe verifier.
11. Resolved an internal contradiction that said there was no presentation
    finding immediately after reporting the harmless underfull-box finding.
12. Added the exact supplement-log path, line 590, source-line range, and log
    SHA-256 for that nonblocking warning, together with its no-reseal remedy.
13. Added the current three-dependency outer/inner partition, exact hashes,
    byte counts, tag binding, and content-ledger crosswalk dependency.
14. Added the reconstruction theorem's at-most-nine-port and
    `O(|\mathcal X|^9)` bounded-restriction count.
15. Added the weak-sharpness zero-based rows, named columns, two exact rational
    rank-nine determinants, cherry determinant/inverse, rank-four induction,
    and graph-pruning preservation under extension.
16. Made the genericity row explicitly cover finite topology/reticulation/
    vertex bounds and componentwise properness.
17. Made the paired-marginal row explicitly cover tensor-invisible parameters,
    and the localization row explicitly record that no abstract marginal is
    lifted and no target deletion map is inverted.
18. Added strict physical witnesses to the certificate-semantics evidence
    description.
19. Corrected the optimized-entrypoint count to five unguarded Python commands
    plus the unguarded shell runner, rather than an ambiguous five portable
    commands.
20. Clarified that a legacy mapping may be a command set, not necessarily one
    command.
21. Added exact internal/external runtimes and peak RSS for the duplicate-name
    attack and the exact Fraction-only boundary and corrected cherry-inverse
    evidence supplied by the root integration.

## Residual disclosures, not further blockers

- Several short reviewer operations and the semantic normal/`-O` pair have
  unavailable wall/RSS cells. The ledger marks these as not sampled or not
  retained rather than inventing values. This is a measurement limitation,
  not an unrun theorem gate.
- The semantic mutation's large disposable APFS clone was discarded, but the
  full mutated files, unified patches, normal/optimized outputs, false record,
  and independent pullback rechecker remain. The conclusion is reproducible
  from those retained artifacts.
- The review-owned primitive audit is independent at the predicate/census
  level, not at the grammar-construction level. The report now says so.
- The review-owned probe check reconstructs registry joins, not graph maps.
  The complete graph-map transport/restriction replay is a separate
  submission-supplied implementation, independent of the decisive classifier.
- No second all-family orbit partition avoiding both atlas and canonicalizer,
  and no second symbolic engine expanding every higher-degree body, was
  supplied or run. These are disclosed stronger-audit boundaries, not current
  release gates.
- Optional authoritative `--write`/reseal forms were not run in the pristine
  copy. Their check paths ran, and all writing was confined to disposable or
  review-owned locations.

These disclosures are already reflected in the report and do not require a
different scientific recommendation. I found no remaining factual
inconsistency that changes the HOLD disposition.
