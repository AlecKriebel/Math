VERSION 1.0 LOCAL RERUN NOTE — NONCANONICAL ENVIRONMENT PROVENANCE
Date: 2026-08-09

The stable report contains only mathematical output and the hashes of a
closed explicit source allowlist. Interpreter, platform, elapsed-time,
temporary-path, and transcript data are deliberately outside that report.
Every top-complex certificate is independently validated before it enters a
fixed digest, and reproduce.sh compares two new canonical reports with the
committed golden file without overwriting it.

Pre-release local rerun environments:
  CPython 3.11.8 on macOS, arm64
  CPython 3.14.6 on macOS, arm64

Command:
  ./reproduce.sh

Outcome under each interpreter:
  45 unit tests passed.
  Two regenerated reports were byte-identical.
  Reports were byte-identical across both interpreter versions.

Canonical verification_report.json SHA-256:
  72d0ab306d61f95f7fd469cdd0e334f97b1b63b4101d87f82242f6bca4e1b9e1

The canonical evidence is verification_report.json plus reproduce.sh. This
human-readable note is excluded from the report source allowlist and makes no
claim of external or independent expert human review. The tagged clean-clone
replay is recorded separately under validation/.
