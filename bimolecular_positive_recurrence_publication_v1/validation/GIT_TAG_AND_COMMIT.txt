VERSION 1.0 GIT PROVENANCE
Date: 2026-08-09 (America/Los_Angeles)

Release tag name:
  bimolecular-positive-recurrence-v1.0

Fully validated corrected release-content commit:
  d8b18ad43b28b3d01956224a4e9cec2a65d37ef3

Parent main commit used for the release-content commit:
  5435f79c1e6d8b3de6320e3a1f6a3feb8a5ee21b

Temporary annotated tag object used for the recorded clean-clone replay:
  c0b9eb11f8283e957991167ec1c9dcd3ccd1752f

The complete initial replay is in CLEAN_CLONE_TRANSCRIPT.txt. It cloned the
temporary annotated tag, created a fresh Python 3.14.6 virtual environment,
installed the Version 1.0 package in editable mode, passed all 45 tests,
reproduced byte-identical canonical reports, rebuilt all three PDFs byte for
byte with Tectonic 0.16.9, and verified the complete candidate manifest.

After the final independent audit repairs, the corrected release-content
commit recorded above was checked out from the local Version 1.0 tag in a new
sparse clean clone and fresh Python 3.14.6 virtual environment. That replay
again passed all 45 tests, matched the canonical report, rebuilt all three
PDFs byte for byte, verified both manifest/report copy sets, and confirmed
that all 60 durable ZIP members were safe and byte-identical to the tagged
package tree.

After this record and the regenerated final manifest are committed, the public
Version 1.0 annotated tag is placed on that final metadata commit and the
entire replay is repeated once more. A Git commit cannot literally contain its
own hash: adding the hash would change the commit. Accordingly, the immutable
public tag is the authoritative resolver for the final release commit. Verify
it with:

  git rev-parse bimolecular-positive-recurrence-v1.0^{commit}
  git show --no-patch --format=fuller bimolecular-positive-recurrence-v1.0

No GitHub Release or DOI was created for Version 1.0.
