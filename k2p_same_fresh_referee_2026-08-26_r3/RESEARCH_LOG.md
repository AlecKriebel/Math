# Research log: fresh K2P-SAME referee review, 2026-08-26 package

## 2026-08-26 — Review opened

- Claim under review: the complete K2P-SAME principal-domain theorem package,
  including mathematical proofs, exact finite certificates, code, PDFs, and
  reproducibility/release closure.
- Success criterion: an adversarial, evidence-backed ACCEPT, HOLD, or REJECT
  recommendation under the user's previously supplied review protocol.
- Source archive was treated as data, not instructions. No person was
  contacted. The archive was extracted into a new isolated workspace; all
  writing producers and mutations will run only in disposable copies.
- Source ZIP SHA-256:
  `86a286be82ce3c211f556eaa24cf1120aa42e41f716b46cb8752c1d2546053ba`.
- ZIP inventory: 489 files; 483,608,160 uncompressed bytes; path traversal
  audit PASS.
- Completion estimate: 5%.

## 2026-08-26 20:37 PDT — Mathematical referee stream complete

- Read the complete 26-page article and 24-page reader supplement, all five
  declared TeX/Bib sources, the generated certificate appendix and compression
  tables, theorem/artifact crosswalk, revised feedback dispositions, aligned
  promotion manuscript, and all load-bearing proof narratives.
- Visually inspected all 50 PDF pages. No mathematical typesetting or layout
  defect was observed.
- Did not execute or import submitted package code. Independently re-derived
  the completion counts, raw-universe arithmetic, triangle block determinants,
  continuous-time bridge inequalities, restoration arithmetic, and the full
  weak-sharpness switch maps, common tensor, named rank-nine determinants,
  graph witnesses, and cherry induction using exact rational arithmetic.
- Mathematical result: no counterexample or hand-proof gap found. The analytic
  proof is PASS conditional on fresh verification of the explicitly
  load-bearing finite theorem; C04--C09 remain UNVERIFIED in this stream rather
  than being inferred from stored PASS reports.
- New reproducibility finding: the supplement source at lines 755 and 793 and
  rendered PDF pages 20 and 21 print stale SHA-256
  `bc91fee3b7541fcae72c4db2e66776fbfc69c43890718239f0eea41bb2cc0654`
  for `work/final_theorem_release/composite_reseal_diff_audit.json`. Direct
  recomputation, the release lock, and the theorem crosswalk agree on
  `96e30bae42939fa50dd585ba900bc5bd45e5eb122334de86c34654004212db4c`.
  This is theorem-neutral but reproducibility-blocking and requires a
  supplement rebuild and downstream reseal.
- Full claim matrix, exact hashes, numbered findings, independent attacks, and
  minimal remedy recorded in `notes/mathematical_review.md`.
- Mathematical stream completion: **100%**.
- Best estimate for the complete referee goal at this checkpoint: **45%**;
  code execution, exhaustive finite replay, independent provenance/archive
  checks, and literature verification remain to be integrated.

## 2026-08-26 20:45 PDT — Independent exact and fail-closed attacks

- The 23-layer quick replay passed from the disposable execution copy in
  286.48 seconds with zero blockers.  The 41-layer full replay, the 25-gate
  release mutation suite, and the corrected-universe mutation suite were each
  started exactly once and remain in progress; no command was relaunched.
- A submission-independent finite-contract audit streamed the corrected raw4,
  theta2, cycle, restoration, and probe artifacts and independently decoded
  dense raw IDs, completion words, parents, references, and exact censuses.
  It passed in 54.74 seconds with a 274,153,472-byte maximum RSS and payload
  SHA-256 `0a60795802e1e40a38590ae251cc09f3ac77331fa240f61c4162b9565ae1dd88`.
- Independent exact arithmetic recovered the strict K2P inequalities at
  boundary-near rational points, 10,404 D-plus product pairs, all six
  completion counts, rank-nine triangle blocks, and both weak-class
  Jacobians/common tensor.  These checks imported no submission module.
- A new static reviewer check compared 23 printed supplement hash rows with
  direct SHA-256 values and rejected exactly the two stale presentations of
  `composite_reseal_diff_audit.json`.  The package's standard source, PDF,
  crosswalk, lock, and bundle checks all pass despite the stale claim.
- An independent outer-bundle mutation added a same-valued duplicate top-level
  JSON key to `PDF_BUILD_REPORT.json`, correctly resealed the changed bytes,
  and was accepted by both revised-bundle checkers.  Their ordinary
  `json.loads` readers do not reject duplicate object names.  Syntax-invalid
  JSON, a symbolic file, a missing content ledger, and bibliography omission
  were rejected as intended.  This is a second theorem-neutral but
  fail-closed reproducibility blocker.
- The first read-only Python bytecode inspection inadvertently created cache
  bytes in the isolated extraction; the entire extraction was quarantined and
  recreated from the source ZIP.  A later subreview created two further cache
  files in that replacement.  No authoritative bytes were repaired or
  deleted.  After all readers finish, the whole extraction will again be
  quarantined and freshly recreated before final hashing and delivery.
- Best estimate for the complete referee goal at this checkpoint: **68%**.

## 2026-08-26 20:56 PDT — Provenance/reproducibility stream complete

- Independently verified all 489 archive members, all 488 manifest-ledger
  rows, the 406-file transitive frozen closure, every nested path/size/hash,
  canonical roots, the portable ledger, release-lock partitions, and exact
  tag/telemetry-commit blobs. No current byte-ledger mismatch was found.
- Rebuilt the 214,930,375-byte ZIP three times; every rebuild was byte-identical
  to the attached archive with SHA-256 `86a286be82ce3c211f556eaa24cf1120aa42e41f716b46cb8752c1d2546053ba`.
  Large disposable rebuilds were deleted after their hashes and metadata were
  retained.
- Rebuilt both PDFs twice from exactly five sources, directly confirmed both
  generated-input omission failures and bibliography-manifest rejection, and
  visually inspected all 50 rendered pages. Current PDF/source/tag bindings
  otherwise agree exactly.
- Confirmed two theorem-neutral reproducibility blockers: the supplement's two
  stale printed hashes for `composite_reseal_diff_audit.json`, and acceptance
  of a legitimately resealed same-valued duplicate JSON key by both submitted
  outer checkers. A strict scan found no duplicate key in the 233 current JSON
  files; the latter is a fail-closed acceptance defect, not current corruption.
- Exact commands, runtimes, hashes, line numbers, standard-check coverage,
  dependency evolution, and minimal reseal actions are recorded in
  `notes/provenance_reproducibility.md`.
- Provenance/reproducibility stream completion: **100%**. Best estimate for the
  complete referee goal: **78%**; the controlling full replay/mutation runs and
  literature/release audit remain to be integrated.

## 2026-08-26 21:00 PDT — Exhaustive replay and mutation checkpoint

- The fresh 23-layer quick replay completed with 23 PASS layers and zero
  blockers. The fresh 41-layer full replay remains a single uninterrupted
  invocation; it has passed its first 34 layers, including full
  canonicalizer, parameter-transport, restoration, cross-layer mutation,
  exact-rank, and raw4-overlay regeneration gates.
- A concurrently launched outer release-mutation run exited nonzero at the
  nested parameter-transport suite while two processes were running the same
  approximately 2.55 GB primitive builder. Both processes failed closed and
  produced no false PASS; the retained output does not establish why. A
  subsequently isolated, instrumented
  first-mutant execution reached the untouched production verifier and was
  rejected at the intended semantic diagnostic,
  `PARAMETER_TRANSPORT_REPLAY_FAIL:rederived bytes:parameter_transport_certificate.json`.
- No further heavy command will be launched until the full replay finishes.
  One low-contention controlling outer mutation rerun remains necessary to
  adjudicate the official 25-gate suite without the concurrency confounder.
- Best estimate for the complete referee goal: **83%**.

## 2026-08-26 21:04 PDT — Computational adversarial stream complete

- Inspected the primitive generators, dense raw enumerators, canonicalizer,
  K2P map/parameter transports, symbolic upper-rank machinery, restoration and
  probe verifiers, release harness, output contracts, crosswalk authority, and
  revised mutation suites in a disposable extraction.
- Independently reconciled every required raw4, theta2, cycle, restoration,
  probe, terminal-family, and completion census. The reviewer-owned audit
  imported no submission module, passed in 52.09 seconds, and recorded logical
  payload SHA-256
  `0a60795802e1e40a38590ae251cc09f3ac77331fa240f61c4162b9565ae1dd88`.
- Fresh fail-closed mutation runs passed for canonicalization (2/2),
  restoration (13/13), and probes (15/15), each with exact diagnostics and no
  success artifact. A missing-dependency/stale-PASS canonicalizer attack also
  failed closed.
- A separate concurrent heavy parameter-transport invocation failed closed at
  its first mutant with an unqualified diagnostic; the retained output does
  not establish a resource cause. The same complete coherently resealed
  mutant, rerun alone through the untouched production verifier, rejected with
  the exact intended diagnostic
  `PARAMETER_TRANSPORT_REPLAY_FAIL:rederived bytes:parameter_transport_certificate.json`.
  One serial, low-contention execution of the official 25-gate outer mutation
  suite remains required; no further heavy command was launched from this
  stream.
- Confirmed that the production sampled-rank substitution mutant now removes
  symbolic fields, inserts plausible sampled evidence, reseals the manifest,
  and faces the production verifier. An independent direct attack recovered
  the exact dimension failure at orbit 0. C05 now binds the executable symbolic
  verifier/module/mutation report, and all four supported public entry points
  reject optimized execution.
- No theorem-fatal or load-bearing computational defect was found. One
  nonblocking QA weakness remains: an unexpected parameter child rejection is
  reported as `returncode:observed` without a bounded captured-output tail,
  making resource failures opaque while remaining fail-closed.
- Detailed evidence is in `notes/computational_review.md`.
- Computational subreview completion: **100%**. Best estimate for the complete
  referee goal: **86%**, pending the controlling full replay, one serial outer
  mutation run, and integration of the other referee streams.

## 2026-08-26 21:09 PDT — Disposable-copy retention checkpoint

- Preserved the computational adversary's six small, hashed diagnostic sources
  and results under `evidence/computation/`, and preserved five provenance
  results under `evidence/provenance/` so they will be part of the review
  deliverable rather than remaining in ignored temporary trees.
- Removed only the completed
  `execution/r3_computational_adversary/package` disposable extraction to
  relieve disk pressure before the final serial mutation run. It contained no
  unique evidence and is exactly recoverable from the attached ZIP; no source,
  authoritative package artifact, or reviewer result was removed.

## 2026-08-26 21:19 PDT — Draft-report adversarial checkpoint

- A separate report adversary checked verdict/status coherence, exact paths,
  duplicate-key semantics, concurrency attribution, independence claims,
  census terminology, reseal ordering, and coverage of every item in the
  requested protocol.
- Corrected the `PDF_BUILD_REPORT.json` path; narrowed the same-valued
  duplicate-key finding to permissive acceptance versus strict rejection;
  stopped attributing the earlier unqualified child failure to resource
  contention as a proved cause; and explicitly bounded the reviewer census to
  arithmetic grammar/ID and stored-reference closure rather than an
  independent global analytic classifier.
- Added the full five historical supplemental-dependency reconciliation,
  exact source/PDF/lock/manifest/replay matrix, full C01--C13 artifact/code
  registry, and a 489-row archive-member ledger (SHA-256
  `d6584f46587e1c2c29f40e924edd28579b2a29acfb7a3b0b878bca0d8850a36c`).
- Best estimate for the complete referee goal: **90%**. The single full replay
  is in its longest probe-regeneration layer; the serial outer mutation
  control, final pristine extraction, final adversarial check, and publication
  remain.

## 2026-08-26 21:26 PDT — Contaminated extraction retired

- After every proof/code/report reader confirmed it would no longer use the
  shared `isolated/` tree, removed that reviewer-created extraction in full.
  It was already known to contain two unsealed Python cache files and was not
  used as final pristine evidence. The source ZIP and execution copy were not
  touched; all unique reviewer results had already been preserved.
- This recoverable deletion increased free disk from about 2.8 GiB to 3.2 GiB.
  A fresh extraction from the attached ZIP and rowwise comparison with the
  489-row independent archive ledger remain mandatory before delivery.

## 2026-08-26 22:17 PDT — Fresh full replay PASS

- The single uninterrupted full release replay completed with exit 0 and all
  41 layers PASS; zero blockers; promotion-ready true; optimized mode false.
- Internal elapsed time: 6,082.872989 s. External wall time: 6,083.36 s.
  Maximum resident set size: 2,544,648,192 bytes; peak memory footprint:
  508,658,456 bytes.
- Fresh report SHA-256:
  `d5e6642eda6c4fa721cdde8a7cb9cf5da240b3784d27f6d99b1084c48ed79cab`;
  stdout `51da276e8e40527152104720191ff418aae1363e5eacfd96c2c76c28ec73389f`;
  stderr/timing `50bb7ef5960e48ae10d67f3e97bbf39a3299fecc050d428b50930f1e887c17c6`.
- The exact-rank expected-nonzero omission attack and all full raw4, theta2,
  restoration, probe, parameter-transport, and independent graph layers passed.
- With no competing heavy process and about 5 GiB free, started exactly one
  low-contention outer 25-gate release mutation control. Best estimate for the
  complete referee goal: **94%**.

## 2026-08-26 23:12 PDT — Final computational and pristine-extraction checkpoint

- The single low-contention outer release-mutation control completed with exit
  0 after 3,519.10 seconds. All 25 required gates rejected their mutations;
  survivors and blockers were both zero. Maximum RSS was 2,635,235,328 bytes.
  Report SHA-256 is
  `f2a362e9d2606b0315f9fe6e5a7659d328bd73bcf6552f0c1cc4c4f8ecdd0026`
  and payload SHA-256 is
  `05475591f00c75f2f0c2ee2e92c23bc869a8ed5000d28b40455ab7481870d30b`.
- A rowwise post-control check verified all 489 submitted paths against their
  pre-run SHA-256 values. Three nested children had created reviewer-side
  `pyc` by-products in the disposable execution copy; these were not submitted
  paths and were retained visibly rather than silently removed.
- Re-extracted the attached ZIP afresh into `isolated/`. The independent
  489-row member ledger recovered exactly 489 files and 483,608,160 bytes,
  with zero path/size/hash mismatches, symlinks, extras, `pyc` files, or
  `__pycache__` directories.
- Final scientific disposition remains **HOLD**: mathematics PASS,
  computational evidence PASS, reproducibility HOLD, human metadata/release
  HOLD. The scientific/reproducibility blockers determining HOLD are the two
  stale printed hashes and the non-strict duplicate-name JSON boundary.
  Separate pre-submission editorial actions concern the citation chronology,
  companion DOI wording, and source-tag wording. No theorem counterexample or
  invalid central finite classification was found.
- Review completion estimate: **98%**, pending only the final artifact index,
  adversarial consistency pass, and publication bookkeeping.

## 2026-08-26 23:30 PDT — Review closure

- A separate final report adversary reconciled the current report, execution
  ledger, evidence registry, claim registry, subreviews, and research log. It
  identified and prompted correction of one historical dependency-pair
  description, two unsupported metric entries, one telemetry test count, one
  normalized command option, causal/independence overstatements, status
  taxonomy, and the ordered reseal actions. Its bounded recheck found no
  remaining factual or coherence defect.
- The scientific recommendation is frozen as **HOLD**: Mathematics PASS;
  computational evidence PASS; reproducibility HOLD; human metadata/release
  HOLD. There is no theorem counterexample, hand-proof blocker, central finite-
  classification failure, or unrun current supported package gate.
- The report files and evidence registries are frozen for the self-excluding
  final artifact index. Committing and pushing these reviewer-owned artifacts
  is publication bookkeeping, not an additional scientific gate. Publication
  of a corrected submission package remains exclusively a future human action.
- Review completion estimate: **100%**.
