# Final source/polar coverage reconciliation

2026-09-14 local audit date. Bounded review complete: 100%. Read-only review of frozen Lean sources and `COVERAGE.md`; no Lean/Lake commands, `.lake` access, or protected-source/document edits were performed. Final compiler acceptance remains the integration owner's clean `check.py` receipt. The owner reports these source endpoints compiled and their axiom checks passed.

This addendum supersedes the remaining source-spectrum and strategy qualifications in `SOURCE_POLAR_SEMANTIC_AUDIT.md`.

- **Relative spectrum closed:** `source_relative_full_simple_spectrum` proves the actual spectrum of `sourceRelative y` equals the full equality-root range, with every corresponding eigenspace of complex dimension one. `source_relative_charpoly` also proves the exact characteristic polynomial. These statements hold for every d≥2.
- **Bob spectrum closed:** `sourceBob_full_simple_spectrum` proves the literal coefficient matrix has exactly the full dth-root spectrum, again with every eigenspace of dimension one. Its proof transfers the actual transpose's weighted-cycle eigenspaces through a proved unitary intertwiner, then transfers eigenspace dimensions through transpose by rank-nullity. It does not infer simplicity merely from the dth-power identity. The earlier full-spectrum gaps are closed.
- **Source strategy closed:** `sourcePhysicalStrategy` constructs actual positive-state/PVM data. `sourcePhysicalStrategy_encodings` identifies source Z/X, each literal Bob coefficient matrix, and extra Z†. `sourcePhysicalStrategy_attains` proves the actual first augmented value `2/sin(pi/(2*d))+1`; `source_reduced_expectation` separately proves the reduced value. These justify the corresponding coverage rows without a substitute witness.
- **Literal inverse formula now explicit:** `sourceModulus_eq_relative_modulus` identifies H with the positive square root defining |1+W|, and `sourceBob_transpose_inverse_formula` proves the manuscript's `B^T = G(1+W†)Z†` with G a verified two-sided inverse of H. This supplements the already audited actual positive-modulus factorization and removes any possible reading that source identification consists only of a definitional adjoint-transpose equality.

The remaining proof-method boundaries in `COVERAGE.md` are accurate. The arbitrary-Hilbert polar lemma proves the identity for a supplied polar factor satisfying its defining factorization and initial-isometry equations; it does not construct arbitrary canonical polar decompositions, their uniqueness, or their strong-limit/von Neumann algebra representation. Kernel safety is proved without assuming a unitary polar extension. The specific source attaining factor and inverse are explicitly constructed, so this general existence boundary does not leave its source identification conditional.

The computational-MUB coverage is also accurate. `MUBSpace_representation` identifies the literal Fourier/diagonal-conjugated Fourier operator space; `computational_MUB_spectral_obstruction` proves actual real spectral values strictly above and below a computational eigenvalue unless the operator is scalar; `computational_PVM_coefficientwise_saturation` proves every separately bounded coefficient is scalar when the computational PVM saturates on Phi. Its arbitrary unit-phase diagonal specialization includes the manuscript phase vector. The stronger constant-diagonal positivity proof establishes the proposition and its scoped consequence, while the manuscript's individual wraparound/Toeplitz/SVD intermediate calculations are not independently formalized. It makes no broader settings no-go claim.

No overclaim was found in the final source-spectrum, source-strategy, polar-boundary, or MUB wording of `COVERAGE.md`. No additional mathematical repair is requested by this reconciliation.
