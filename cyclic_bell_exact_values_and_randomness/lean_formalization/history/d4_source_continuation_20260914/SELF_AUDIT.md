# Continuation self-audit — not an independent-agent certificate

The same assistant wrote and inspected this source. No separate agent, Lean
compiler, or external quantum-information reviewer validated it in the cloud.

## Inspected points

The physical definitions contain positivity, normalization, and projective
measurement conditions only. Both scalar functionals are the actual source
formulas. The generic PVM-to-unitary and inverse-encoding scripts allow zero
outcomes. The universal bounds have arbitrary local dimension parameters and
arbitrary mixed states. Their module import closure has no concrete-witness
module; no equality spectrum or saturation is assumed.

The first d=4 SOS uses polynomial factors and exact scalar identities. Its
algebra passed an alternate reduction check allowing only U/B_y commutation,
with A/B_y and same-party commutations explicitly not imposed. The second SOS
uses its source normalization 1/8 and sourceLambda, not a fitted vector.
Both are separately instantiated in the physical tensor model.

Every witness PVM is explicit through a complete orthonormal basis. Observable
encoding fixes the weighted-shift orientation. The literal swap, actual
exponential embedding, Bob conjugation, source D_l, and coefficient formulas
have separate source bridges. Attainment is evaluated independently of either
universal bound. The target PVM objects are shared by the two strategies and
the physical Born calculation. Maximality is proved by all-competitor comparison.

The one-dimensional Eve instrument is positive and complete, and its
conditional states are tied to an actual adjoined state, sandwich, and partial
trace. The witness AB state is pure. Only fixed guessing success, not global
adversarial optimality, is concluded.

## Mechanical checks actually executed

The source inventory verifies unique declaration names, balanced delimiters,
namespace structure, exact pins, all required imports, and a query for every
named declaration. It is not a Lean parser or dependency analyzer. It caught
and enabled repair of two extra closing parentheses. Reporting tests check
missing/custom/native-trust axioms, missing audit imports, unaudited added
declarations, Unicode names, and hidden comment/string tokens. All current
compiler-free checks passed; reports explicitly say kernel_checked=false.

## Outstanding risk

No script has been elaborated. API spelling, simplifier behavior, coercions,
matrix/finite-sum rewrites, and proof-search budgets may fail. A script may
need substantial proof restructuring or additional lemmas. Actual #print
axioms output is absent. Expanded statements are authored self-audit examples,
not independent confirmation of manuscript correspondence. The source-writing
milestone must not be represented as a verified theorem milestone.
