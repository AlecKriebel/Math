# Adversarial review of the compiled value/model endpoints

2026-09-15 UTC. Scope: independent statement-to-paper review of the four exact-value triple endpoints, their physical model definitions, and the commuting upper-bound route. This supplements the broader independent semantic audit; it does not certify unreviewed paper claims.

## Findings

1. **The supremum sets are actual physical sets.** `Qq` existentially quantifies finite local carrier types, their finite instances, an arbitrary PSD trace-one density, and actual complete orthogonal PVMs. No bound, saturation, phase, Fourier-flatness, support-rank or witness-table condition occurs in membership. `Qqc` quantifies complete complex Hilbert spaces, normalized vectors and actual PVMs with every cross-party effect pair commuting. It imposes no finite-dimensional instance. Its fixed Lean carrier universe is a size convention, while the underlying Hilbert-bound theorems are universe-polymorphic. This matches the paper's PVM convention (main.tex lines 215–239).
2. **No empty-set supremum shortcut.** `bellSupremum` is literally real `sSup (f '' S)`. `bellSupremum_of_attained_bound` establishes image nonemptiness from the same exhibited witness used for attainment and boundedness from the universal inequality. No conclusion is obtained from the default value of an empty or unbounded real supremum. The physical state normalization also rules out zero-dimensional local realizations automatically; no nonempty model is silently assumed.
3. **The closure is genuine.** `Qqa` is `closure (Qq ...)` in the ordinary product topology on real behavior arrays. The finite sums defining both Bell functionals have proven continuity. The closure bound follows from a closed scalar sublevel set, not an assumed closedness of a quantum set. The supplied development does not prove the paper's contextual inclusion `Qqa ⊆ Qqc`; this gap does not weaken the four exact-value conclusions, which follow independently from bounds and finite witnesses.
4. **The normalization/signs match.** Outcomes are `ZMod d`; the encoded observable uses positive characters. `probabilityCorrelator` is `sum chi(a+b) p(a,b)`, with a proved trace-correlator bridge and no unintended Bob conjugate. Family I uses the real part of `(A0 + chi(y) A1) By`. Family II uses conjugate actual `generalLambda(l)` and the positive-character Bob Fourier transform. Augmentation adds the aligned input `(0, none)` with coefficient 1. Reduced scenarios genuinely remove the extra Bob input via an explicit input pullback.
5. **No circular upper-bound assumptions.** First-family arbitrary-Hilbert bounds use unitarity and cross-party commutation only, matching main.tex Theorem `thm:exact`. The C*-algebra SOS constructs continuous half-polar factors and proves their gap. It does not assume a Bell bound as a structure field or invoke finite matrix algebra for an infinite Hilbert space. Second-family matrix and C*-algebra SOS identities use the exact `1/(2d)` prefactor; their coefficient normalization is separately proved for the manuscript coefficients. The generic second SOS actually requires less commutation than the physical model, which strengthens rather than weakens the upper bound.
6. **Finite-to-commuting membership is constructive.** The inspected bridge uses the actual positive square-root factor of the density, vectorization into Euclidean Hilbert space, explicit identity environment, and proved equality of every Born probability. `matrixCLM` acts on `EuclideanSpace`, avoiding the incorrect supremum-norm function-space Hilbert identification. Neither Born equality nor a universal embedding is postulated.
7. **Quantifiers and boundary cases remain correct.** The four exact-value triple endpoints quantify arbitrary `d` with explicit `2 ≤ d`, and arbitrary physical competitors through the supremum sets. Finite witnesses exist in dimension `d`. The radical table gives actual evaluated values at d=2,3,4,5,6, and the d=4 augmented result is attached to all three actual suprema.

## Evidence and remaining boundary

`GeneralCorrelationValues` and `GeneralExactValues` compiled without source changes once repaired dependencies were available. Actual expanded endpoint declarations and `#print axioms` are recorded in `correlation_value_axioms.log`; the four triple-value endpoints and d=4 radical endpoint use only `propext`, `Classical.choice`, and `Quot.sound`. Representative first/second C*-algebra, Hilbert and physical-probability axiom outputs are in `general_first_axioms.log` and `general_commuting_axioms.log`.

The original polar-decomposition intermediate identity, general contextual `Qqa ⊆ Qqc`, and other separately identified manuscript coverage gaps must still be accounted for in the overall full-paper assessment. The compiled exact-value endpoints themselves do not exhibit a semantic weakening or vacuity issue in this review.

Checkpoint: this bounded semantic review of the four compiled exact-value/model routes is complete (100%); no claim about overall paper completion follows from that percentage.

## Binary benchmark extension

The compiled `binary_values_q_qa_qc` also uses the actual three physical sets and an explicit attaining qubit strategy. Its arbitrary-Hilbert upper bound is proved by a C*-algebra SOS, avoiding any finite-matrix-to-infinite-space inference. The score coefficients are literally `1,-2,2,2`, with target `3 sqrt(3)`.

`PurifiedStrategyOn` contains only a normalized finite matrix amplitude and physical local PVMs. `BinaryPrivacyAt` quantifies over every compatible finite local/Eve carrier, all corresponding finite instances, and every compatible purification; its conclusion is the actual conditional Eve matrix `reducedE/4` for each output pair. This is not merely observational uniformity or privacy of the exhibited witness. The definition is vacuous for unrealizable arrays, as universal compatibility predicates usually are, but `binary_two_input_private_achievable` explicitly requires `p ∈ Qq` and supplies an attaining strategy. `purifyStrategy` separately shows every finite mixed strategy has a compatible finite purification. Thus the achievement theorem is nonvacuous.

The one-input impossibility statements construct compatible finite purifications and complete guessing effects with success one, then contradict operator uniformity. They are scoped to actual one-input nonsignalling behavior structures and available target inputs. No infinite-dimensional privacy conclusion is inferred from these finite matrix results.

These endpoints and both all-dimensional three-model counterexamples have printed only the three standard axioms; see `final_foundation_axioms.log`. `ModelBoundaryCheck.lean` independently compiles explicit model-nonemptiness examples and an `IsEmpty (StateOn Empty)` proof.
