# Focused completion plan before describing the archive as a complete formalization

The reviewed archive should remain unchanged as evidence. Make any revision in the active Lean source and produce a new archive with a new checksum. The following work is about explicit manuscript correspondence; the independent review found no incorrect principal endpoint.

1. **Complete the second-family first-harmonic endpoint.** For every d≥2, permutation κ, and l,y in ZMod d, prove from the actual `secondPermutationStrategy` that `<A_l B_y> = generalLambda l * chi (-(l*y))`, `<A_l B_none> = if l=0 then 1 else 0`, and every Alice/Bob local first moment vanishes. Assemble the full d-by-(d+1) matrix's independence of κ. The generic two-Alice-setting theorem alone is not that endpoint. Existing `secondAlice_encoding`, `permutationBob_some`, `weighted_entry_conjugate`, `phi_weighted`, `sum_permuted`, `polarTransform_shift`, `polarTransform_compression`, character orthogonality and weighted trace lemmas supply the proof. See permutation_review.md for concrete signatures.

2. **Close the convention-transport statement.** Prove that negating a PVM's outcomes sends its encoded observable to the adjoint; prove the full Born behavior transforms by b↦−b. Apply to the second-family strategy. Define and transport the alternate convention's Bell functional as well—changing Bob outcomes alone does not preserve an arbitrary fixed functional. State target nonuniformity/guessing preservation under that relabeling.

3. **Expose the all-dimensional second SOS annihilation if claiming every theorem clause.** State each concrete second-family residual applied to Phi_d equals zero, using the already proved operator compression and unitary vector identity. Attainment plus the independently proved universal SOS already suffices for maximality; this is an explicit theorem-clause bridge, not a discovered error in maximality.

4. **Choose an honest scope for standard analytic infrastructure.** The general polar positive-factor identity is proved from supplied polar factorization and initial-isometry equations, including kernels. Main bounds are proved unconditionally by a separate route. The arbitrary canonical polar decomposition's existence/uniqueness, von Neumann algebra membership, and strong-limit construction are not formalized. Either keep those boundaries explicit and call the release a formal companion to the principal results, or formalize the additional analytic infrastructure before claiming every argument in the paper is checked. The source attaining matrices already have an explicit canonical-polar construction. Reproducing the paper's cosecant and Toeplitz intermediate derivations is unnecessary for the results whose alternate proofs are already complete.

5. **Reconcile reviewer documentation.** Remove/qualify stale claim-ledger language about absence of Qqa⊆Qqc and source canonical-polar identification; add mappings for the new second-family statements. The abbreviation “all harmonics” should always be explained as all *first-harmonic entries*, not every Fourier order.

6. **Rebuild and repack.** Include every new proof-bearing declaration in the axiom inventory, regenerate manifests, run the clean Lean/axiom/control checks on the newly extracted archive, verify the manuscript hash and unchanged physical definitions, and independently inspect the final theorem assumptions. Accept only the same ordinary Lean axioms (`propext`, `Classical.choice`, `Quot.sound`), no placeholders or native trust shortcuts. Preserve a new exact-archive receipt.

This plan is not an email and does not authorize external contact. The user's requested follow-up email remains conditional on the revised package meeting its intended verification claim.
