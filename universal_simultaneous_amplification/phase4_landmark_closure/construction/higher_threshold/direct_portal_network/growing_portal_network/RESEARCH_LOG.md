# Research log: growing direct portal networks

## 2026-08-08 02:00 PDT — Growing-Q cycle opened

- Moved outside the proved fixed-`Q,T` scope by taking `Q_s->infinity` with
  `Q_s=o(s)`.
- Began with complete portal graphs and then isolated the exact local
  hypothesis needed for the same limit: regular total portal load and
  maximum individual portal edge tending to zero.

## 2026-08-08 02:12 PDT — Correct nested branching map

- Derived the exact finite-`Q` portal-count chains for both update rules.
- Found that the correct portal episode is itself a branching process.  Its
  PGF satisfies a quadratic equation with an `F^2` term; treating the episode
  as one protected portal would be incorrect.
- Derived the parent lifetime PGFs after this nested portal episode.
- Proved that arbitrarily many exchangeable blade types collapse to the same
  scalar law because every parent row is identical after time rescaling.

## 2026-08-08 02:25 PDT — Exact all-r obstruction

- Bd's complete establishment test factors exactly by `B+H-1`: it exceeds
  the complete-graph limit exactly when the total portal degree `d=B+H`
  exceeds one.
- When `d>=1`, the dB test reduces to an affine polynomial in the portal load
  `H`.  Both physical endpoint values are manifestly positive for
  `1<r<2`, forcing strict dB suppression.
- This excludes the entire diffuse regular growing-portal route for every
  beneficial fitness, including `r=3/2` and `r=31/20`.
- No construction lead survived establishment, so no unsupported
  post-establishment fixation claim was made.

