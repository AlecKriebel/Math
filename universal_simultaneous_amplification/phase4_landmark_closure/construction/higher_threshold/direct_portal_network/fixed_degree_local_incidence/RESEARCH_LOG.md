# Research log: fixed-degree local portal incidence

## 2026-08-08 02:40 PDT — Spatial branch opened

- Moved outside the diffuse theorem by keeping portal degree fixed and each
  portal edge at order-one weight.
- Allowed blade types and seeding distributions to be tied to portal
  neighborhoods.

## 2026-08-08 02:52 PDT — Exact local recursion

- Derived the full subset-lattice Bd and dB recursions in terms of the number
  of mutant portal neighbors at each vertex.
- Derived the reverse local incidence kernel for parent seeding and the exact
  multitype parent survival maps.
- Identified the infinite high-girth limit as an interacting finite-set
  process on the regular tree, not a scalar branching process.

## 2026-08-08 03:04 PDT — Cavity closure refuted exactly

- Showed at the generator level that a portal child retains an order-one edge
  to its parent, altering both recovery and outward-birth rates before any
  graph cycle is encountered.
- Rebuilt the complete 63-state six-cycle episode with exact rational rates.
- At both `r=3/2` and `r=31/20`, and for both update rules, the true singleton
  PGF has a nonzero rational residual in the diffuse quadratic.  The natural
  independent-descendant recursion is therefore falsified.

## 2026-08-08 03:12 PDT — Knife-edge theorem and search verdict

- Found the exact product solution `F_A=r^(-|A|)` for Bd at the establishment
  test whenever `B+H=1`, for every regular portal topology.
- This makes Bd exactly neutral against the large-complete baseline and
  excludes strict simultaneous amplification on that hypersurface.
- Exact sparse searches on cycles through `C_8` and the cubic portal graph
  found no simultaneous-establishment lead at either requested fitness.
- Away from `B+H=1`, the infinite-tree sign remains open.  No fixation claim
  was made because no establishment lead survived.

