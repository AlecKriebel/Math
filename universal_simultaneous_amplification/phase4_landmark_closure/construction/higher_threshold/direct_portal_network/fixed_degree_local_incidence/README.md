# Fixed-degree local portal incidence

This folder treats fixed-degree portal graphs, where portal parent and child
retain an order-one shared edge.

- `SPATIAL_PORTAL_RECURSION_AUDIT.md` derives the exact multitype spatial
  recursion, proves the `B+H=1` Bd obstruction, and records the precise open
  infinite-tree sign.
- `verify_spatial_recursion_counterexample.py` gives an exact 63-state audit
  and refutes the diffuse/cavity quadratic at both requested fitnesses.
- `search_spatial_portal.py` is a numerical hostile-search tool only.
- `RESEARCH_LOG.md` records this bounded cycle.

No universal threshold claim is made.
