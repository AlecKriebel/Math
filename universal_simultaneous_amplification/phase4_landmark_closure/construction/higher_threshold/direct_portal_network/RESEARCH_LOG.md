# Research log: direct portal networks

## 2026-08-08 00:55 PDT — Branch opened

- Began from the hostile-audited higher-rank no-direct-portal trace.
- Allowed a fixed finite symmetric portal graph on the episode scale.
- Did not reopen the no-direct-portal theorem and used no literature search
  or external contact.

## 2026-08-08 01:05 PDT — Exact trace and endpoint searches

- Re-derived the full `2^Q-1` labelled portal-subset transforms directly
  from the Bd and dB event rules.
- Re-expressed the parent lifetime maps in portal loads `B_a`, degrees
  `d_a`, and row fractions `f_at`.
- Ran full-trace searches at `r=31/20` for `Q=2,T=2`, `Q=2,T=3`, and
  `Q=3,T=2`.  No simultaneous establishment candidate was found.  The best
  recorded minimum gaps were approximately `-0.03232544` and `-0.03209860`.
- Ran a relaxed hostile search over independent row marks and parent weights
  through `Q=4`.  It did not refute the inherited affine-map conjecture.
  This remains numerical evidence only.

## 2026-08-08 01:24 PDT — Exact Q=2 scalar separator discovered

- Specialized the direct trace to two unequal portal loads and one blade
  type, retaining the direct edge and all three nonempty labelled subsets.
- Defined the exact baseline-test margins
  `T_B=1/r^2-D_B(1/r^2)` and
  `T_D=(2-r)/r-D_D((2-r)/r)`.
- Numerical optimization suggested a constant weighted separator rather
  than separate portalwise inequalities.
- At `r=31/20`, found and exactly certified
  `T_B+(2/5)T_D<0` with a seven-box rational Bernstein cover.

## 2026-08-08 01:43 PDT — Full interval theorem

- Found that the single rational weight `81/200` gives
  `T_B+(81/200)T_D<0` uniformly on `3/2<=r<=2`.
- Compactified the three positive graph parameters and represented fitness
  directly on `[0,1]`.  The exact numerator has multidegree
  `(14,11,11,11)` and 7,323 terms.
- A fixed 11-box de Casteljau cover of maximum depth five certifies the sign
  exactly.  No floating-point interval decision is used.
- Consequence: arbitrary fixed unequal loads and an arbitrary fixed positive
  edge between two portals cannot cross the simultaneous `3/2` barrier in
  the one-blade-type strong-pair trace.
- Higher-rank direct networks remain open; no universal claim is made.

