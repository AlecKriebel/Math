# Mesoscopic module search log

All times are America/Los_Angeles.  No literature search or external contact
was used.

## 2026-08-02 03:35 — exact attachment reduction and bounded search

- Rebuilt the exact weak repeated-module trace including the macro fixation
  factor; this rejects branching-only portal scores.
- Proved that the attachment optimum for both the repeated-module common
  fixation value and the separated center-degree window has support on at
  most two module vertices.
- Implemented the reduction and screened weighted modules of orders six,
  seven, and eight at `r=1.51` using randomized weight-scale starts and local
  mutations.  No candidate survived isolated dB establishment.  This is
  numerical reconnaissance, not a finite-order impossibility theorem.
- Tested structured `3+3`, `2+4`, and four-vertex-plus-helper singular
  modules.  None repaired the Bd--dB handoff mismatch.
- Tested the exact load-aware early-migration derivative at order six.  The
  best balanced numerical coefficient remained negative.
- Proved a class no-go for arbitrary weighted-regular modules over arbitrary
  weighted-regular macrographs with uniform complete-bipartite replacement
  blocks: the full graph is weighted-regular and ties the Bd baseline exactly.
- No result beyond the existing lower bound `R_sim>=3/2` is claimed.

