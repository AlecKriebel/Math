# Research log

## 2026-08-02 (America/Los_Angeles)

* Derived the exact absent-state identity `q pi_i^0 = eta_i R_0` and the
  direct target `E_(pi_i^0) h_i <= E_(eta_i) g`.
* Split the target exactly through the zero-biased post-clock law.  The left
  slack is `E_eta[u(1+g)]-1`; the right slack is
  `-Cov_eta(u,g)`.
* Adversarial search found no symmetric failure through five vertices, but
  this remains numerical evidence only.
* Found and exactly certified a four-state directed failure of the left
  split.  The full component-odds inequality remains positive there.
* Found and exactly certified symmetric `K_4` failures of both proposed
  intermediate `R_0g` comparisons.
* Exactly closed generic association and scalar-order shortcuts.  Neither
  failure decides the special stationary covariance.
