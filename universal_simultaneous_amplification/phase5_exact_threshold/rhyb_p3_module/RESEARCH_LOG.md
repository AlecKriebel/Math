# Research log: `R_hyb` BDM on `P_3`

## 2026-08-13

- Chose the first non-complete bounded-module target, the unweighted
  three-vertex path, rather than opening a module search.
- Solved both six-state labelled chains and their reciprocal-fitness copies.
  Recovered the exact centre/leaf singleton atoms of both stationary OR
  duals by type complementation.
- Expanded the denominator-cleared BDM gap simultaneously in gate odds and
  arbitrary portal loads.  Symmetry leaves three portal coefficient
  quadratics.
- Proved all three quadratics strictly positive at `R_hyb`: their endpoint
  coefficients are positive and their discriminants are negative by exact
  Sturm certificates on the hybrid root isolating interval.
- Result: strict BDM for `P_3` with every nonzero nonnegative portal vector.
  The weighted path and the universal higher-rank stationary inequality
  remain open.

