# Next executable step

Last updated: 2026-08-08 13:05 PDT.

## Immediate command

After the determinant and hostile branches freeze their current checkpoint,
replay:

```text
cd /Users/alec/Documents/Math-universal-amplification
git status --short --branch
```

Then run only the exact verifier belonging to each new claimed milestone.

## Immediate proof step

The collision/promotion implication has been audited and repaired.  The next
upper-bound step is to prove or refute the finite-time harmonic lemma

\[
 \nu_KK_P^tH\ge\nu_KH\qquad(t\ge0),
\]

whose Cesaro limit proves the true collision target.  Pointwise rank cuts,
PGF order, likelihood-ratio order, individual excursion packets,
complete-ray convexity, and colour-count monotonicity are unavailable.  The
first executable subproblem is now the exact fixed-count coefficient
`b_(t,2)`: the antisymmetric sector is proved for every order and lag, while
the standard and symmetric two-channel rank recurrences remain open.  Seek
a weighted cumulative/Fejer certificate for those two recurrences, not a
packetwise or radial-eigenmode sign.  Then determine whether the same
grouping extends to higher colour counts.  In parallel, continue the direct
subset-tree sign

\[
 m_KZ_P(1)-Z_P'(1)\ge0
\]

for every reversible loopless replacement kernel.  Root stability,
log-concavity, and levelwise domination are unavailable; seek a centered
two-tree exchange, transfer-current identity, or complement-reversal
cancellation at `t=1`.  The exact triangle `t=3` increment and all-order
antisymmetric Hessian are the current algebraic footholds.

The next lower-bound step also starts outside the separated strong-pair
normal form, which is now rigorously obstructed on `[R_pair,2]`.  In priority
order:

1. derive the exact fast law when portal events compete with resolution of a
   discordant pair or star--reservoir interface;
2. test a clique-reservoir plus vanishing star-antenna module, retaining the
   complete uniform-initialization correction;
3. derive a growing-rank recurrence in a singular portal regime where the
   fixed-order remainder is not uniform;
4. derive inter-module density corrections rather than isolated module
   responses.
