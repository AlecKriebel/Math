# Phase-5 research log

## 2026-08-08 08:48 PDT — state recovery

- Read the exact-final-closure mission in full.
- Confirmed `main=origin/main` at `91d1f03b` before phase-5 edits.
- Preserved all pre-existing untracked discovery packages.
- Replayed the optimized dilute hybrid certificate: PASS.
- Replayed the exact marked `r=2` structural verifier: PASS.
- Replayed the exact promotion corpus: PASS, with the universal sign still
  explicitly OPEN.
- Created the six required live state files plus this log.
- Started independent determinant-proof, hostile-counterexample, and
  lower-to-two response-cone branches.

## 2026-08-08 08:55 PDT — logical audit opened

The prior marked-lift note states that stationary promotion is equivalent to
the universal collision bound.  The displayed equations visibly establish

```text
promotion => collision target,
```

because the two-step value is at least the complete value.  The converse is
not displayed.  Phase 5 therefore treats promotion as a proved sufficient
named lemma until an exact converse is supplied.  This distinction is now a
mandatory hostile-audit item rather than an assumed equivalence.

## 2026-08-08 09:00 PDT — logical audit resolved

The implication audit is exact:

\[
\sum g\ge\sum\mathcal T_P^2\mathbf1
\quad\Longrightarrow\quad
\sum g\ge|\mathcal Y|,
\]

because the two-step SOS proves
`sum T_P^2 1>=|Y|`.  No converse is known.  The phase-4 note and all live
ledgers were repaired.  The exact collision theorem now has its own weaker
tree coefficient

\[
\sum_y\tau_y(H(y)-1/m_K)\ge0,
\]

whereas promotion replaces `1/m_K` by the larger graph-dependent two-step
value `a_2`.  This opens a genuinely new direct determinant route that the
prior handoff had incorrectly hidden.
