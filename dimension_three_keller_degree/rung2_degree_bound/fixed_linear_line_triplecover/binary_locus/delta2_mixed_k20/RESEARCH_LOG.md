# Research log: mixed fixed-linear \(\delta=2\), \(\{2,0\}\)

All timestamps are UTC.

## 2026-07-25T15:28:00Z

- Identified \(b_1=0\) as the exact twice-divided-gradient branch:
  \(N=(P_q,Q_q,R_q)/(pq)\) forces Hilbert--Burch shape \(\{2,0\}\).
- The \(r\)-part of its linear multiplier is killed universally by a
  curvature polynomial that cannot vanish without making \(A,B\)
  non-coprime.
- Exhausted the two endpoint charts.  All binary contacts vanish except
  \(a=0,bc=1/3\) and \(a=0,bc=4/3\); the latter has gcd degree at least
  three.
- The apparent \(bc=1/3\) survivor was caught by the mandatory gcd audit
  before promotion: all three minors share
  \(pq(3p^2+bq^2)\), so it has gcd degree at least four.  The exact
  component therefore closes already at \(E_6\).
- Retained the nonzero \(E_5\) coefficient on that higher-gcd boundary
  only as a regression check, not as the proof.
- Recorded the residual zero-multiplier plane-field exit and packaged
  dual-CAS checks, a hostile checklist, and a priority audit.
