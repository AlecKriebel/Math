# Hostile review: complete fixed-linear exact \(\delta=1\)

- Re-derive the power-fibre/height-two dichotomy from the abstract
  Hilbert--Burch lemma.
- Prove the marked/unmarked split is exhaustive and invariant under the
  residual source and target actions.
- Starting before normalization, derive (1) and the nonzero
  \(p^3\)-coefficient of \(B\).
- Audit the zero-pattern partition:
  \(a_2=0\); \(a_2\ne0,c_0=0\) with both \(b_1,c_2\) endpoints;
  \(a_2c_0\ne0,b_1=0\); and \(a_2c_0b_1\ne0\).
- Independently redo the generic contact saturation, including \(d=0\),
  \(a=0\), and both pivot divisors.  A flat resultant alone is
  insufficient.
- Reconstruct both literal linear kernels with every nominally free lower
  coefficient retained.
- Replay the marked component and power fibre rather than importing their
  conclusions by filename.
- Replay the zero-tangent all-binary block and the plane-plus-shear exit
  over the required characteristic-zero coefficient field.
- Reject any conclusion beyond this exact-\(\delta=1\) stratum.
