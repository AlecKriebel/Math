# Hostile review checklist — mixed \(\{1,1\}\), exact \(\delta=2\)

Status: independent hostile audit required before promotion.

1. Re-derive the normal form (2) without dividing by a parameter that can
   vanish on \(b_1\ne0\).
2. Verify that \(b_1=c_2=1\) can be imposed simultaneously and that the
   two \((a_3,b_3)\) charts exhaust the residual source/target quotient.
3. Check that the change matrix (4) really identifies the full
   Hilbert--Burch \(\{1,1\}\) component on exact gcd \(pq\), rather than
   merely two syzygies on a dense open.
4. Recompute the \(y=0\) elimination with saturation at \(b\) and all
   denominators in (13), (15); try to produce a missed endpoint.
5. Independently verify that \(D=0\) and \(G=0\) put a genuine extra
   factor into all three original minors, including the denominator
   boundary in (22).
6. Recompute the lifted kernel and Veronese equation without using the
   supplied pivot rows.
7. Confirm that every possible lower homogeneous coefficient is
   retained in the top-only \(r^2E_5\) argument.
8. Recompute at least one of the resultants (26) by a method other than
   the supplied SymPy elimination, and check content/primitivity.
9. Audit \(c=8\) and \(c=16\) directly before any denominator clearing.
10. Challenge the zero-contact exit: verify the shifted syzygy block is
    injective and that the plane-field theorem is unconditional and
    applies over \(\mathbb C(r)\).
11. Search for scope creep: this excludes one leading-form stratum, not
    all quartic Keller maps.
12. Mutate one sign, one denominator, and one endpoint in each verifier
    and confirm that the strict suite fails.
