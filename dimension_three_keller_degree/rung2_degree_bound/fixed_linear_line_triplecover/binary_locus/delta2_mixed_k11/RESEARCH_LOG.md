# Research log — fixed-linear mixed \(\{1,1\}\), exact \(\delta=2\)

## 2026-07-25T16:05:16Z

- Reconstructed the two degree-\((2,2,1)\) divided-gradient syzygies and
  their determinant-\(4pq\) change matrix.
- The first residual endpoint chart has no projective contact.
- On the second chart, the \(y=0\) contact ideal has exactly two
  components; both create an additional common factor in the original
  minors.
- The genuine \(y\ne0\) contact locus is the curve \(V(b,c)=0\), away
  from two higher-gcd pivot divisors.
- The \(r^2\)-coefficient of \(E_5\) is top-only even with all lower
  coefficients retained.  Two parameter resultants force any common
  zero to \(c=8\) or \(c=16\); the former is higher-gcd and the latter
  has a direct nonzero remainder modulo its contact quadratic.
- Exact SymPy and independent PARI/GP suites pass.  Promotion is
  withheld pending a hostile normal-form, saturation, and plane-exit
  audit.
