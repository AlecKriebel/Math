# Hostile review: generic unmarked contact decomposition

- Derive the normalization \(a_2=c_0=b_1=1,b_2=0\) with its exact
  residual stabilizer.
- Recompute the \(d=0\) lex basis and the three literal higher-gcd
  factors.
- On \(d\ne0\), split \(a=0\) before using the last equation.
- Audit both cleared pivots
  \(D=32ac+3a+24b\) and \(E=24a-16c-9\); verify their two half-family
  points directly.
- Recompute the open resultant (5) in an independent exact algebra
  system.  Prove that the quadratic factor has empty contact fibre and
  that \(a=13/20\) forces \(d=0\).
- Check the cubic component by a literal common divisor, not pairwise
  resultants.
- Check that the \(a=1/2\) family has exact open
  \(d(64d-1)\ne0\), and replay its complete lower obstruction with every
  free coefficient retained.
- Inject each omitted pivot branch separately; the audit must fail if any
  one is removed.
