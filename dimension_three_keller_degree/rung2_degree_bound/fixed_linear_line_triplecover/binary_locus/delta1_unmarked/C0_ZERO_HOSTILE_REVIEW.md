# Hostile review: \(c_0=0\) unmarked boundary

- Split \(c_2=0\), \(c_2\ne0,b_1=0\), and \(b_1c_2\ne0\) before
  normalizing.  Verify the \(R=0\) exit and the literal extra line on the
  \(b_1=0\) endpoint.
- Justify the open-subchart normalization
  \(a_2=b_1=c_2=1,b_2=0\) from the residual stabilizer, without importing
  any gauge that divides by \(c_0\).
- Prove that exact \(\delta=1\) excludes \(d=0\), then replay the contact
  elimination and its saturation by \(4a-1\).
- Check the nonzero resultant in (4) and independently verify the unique
  orbit (5).
- Verify exact gcd one from the three full homogeneous forms in (6),
  including the two projective endpoints.
- Replay the legal target/source/target gauge sequence leading to
  \(u_3=v_3=t_0=t_2=0\).
- Reconstruct \(E_6,E_5,E_4\) with
  \(\ell_{13},\ell_{23},\ell_{32},x_2,y_2,u_0,v_2,w\) free.
- Check literally the third row of (12).  Mutating the coefficient
  \(64w^2/81\) in \(\ell_{31}\) must destroy it.
- Replay the zero-tangent plane exit.  Restrict the conclusion to the
  \(a_2\ne0,c_0=0\) boundary.
