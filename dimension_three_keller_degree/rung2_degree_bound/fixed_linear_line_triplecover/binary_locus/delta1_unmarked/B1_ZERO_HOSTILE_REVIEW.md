# Hostile review: \(b_1=0\) unmarked boundary

- Derive the residual source/target stabilizer and justify
  \(a_2=c_0=1,b_2=0\) without dividing by \(b_1\).
- Recompute the unmarked common-root conditions before imposing the
  boundary; ensure no second boundary parameter was silently discarded.
- Reconstruct every contact coefficient and verify (2)--(5) with the
  stated Jacobian signs.
- Check the two cases \(c=0\) and \(c\ne0\) division-free.  In particular,
  do not infer \(b=0\) by cancelling \(c\).
- Verify the literal factor \(q(2p+3aq)\) in all three minors, including
  \(a=0\) and \(c=0\).
- Replay the zero-tangent lower block on this boundary and the
  plane-plus-shear automorphism exit.
- Restrict the conclusion to \(a_2c_0\ne0,b_1=0\).  The \(a_2=0\) and
  \(c_0=0\) charts are separate.
