# Hostile review checklist: \(h=pq\), two-simple-contribution
\(\delta=2,\{1,1\}\) exclusion

1. Recompute the exact gcd and both \(A=0,B=0\) mutations.
2. Verify that the displayed two tangents span the complete \(E_7\)
   kernel and that the lifted contact matrix has rank exactly four.
3. Recompute the kernel ratio \(1:7/5:1\) and the Veronese sign
   \(Y^2-XZ=24/25\).
4. Confirm that zero tangent forces \(x_5=y_5=0\), rather than leaving
   a pure quadratic-\(r\) contact.
5. Replay the complete constant \(E_6\) block and audit the all-binary
   field/descent exit.
6. Keep the theorem scoped to (1) until the remaining fixed-divisor
   orbits are handled.

This is a mathematical audit checklist.  Exact CAS output is not peer
review.
