# Hostile review checklist: \(h=p(p+q)\), two-fixed-root
\(\delta=2,\{1,1\}\) exclusion

1. Recompute the generic gcd and all three boundary gcds
   \(B=0,A=B,A=-4B\), without using the displayed tangent basis.
2. Verify the fresh boundary \(E_7\) ranks and confirm that every
   boundary is routed to \(\delta=3\), not included silently.
3. Check both displayed tangents against the signed \(E_7\) row and
   verify that the decisive minor makes them a complete basis on the
   exact open.
4. Rebuild the lifted \([r]E_6\) matrix from the determinant.  Check
   the rank-four minor, the full kernel vector, and the sign in
   \(Y^2-XZ=24B^2(A-B)^2\).
5. Confirm that the Veronese miss forces all four genuine variables
   \(s,t,x_5,y_5\) to vanish, rather than only the tangent parameters.
6. Replay the complete constant \(E_6\) block and audit the all-binary
   field/descent exit.
7. Keep the theorem scoped to (1)--(2) until the remaining incidence
   leaves are closed.

This is a mathematical audit checklist.  Exact CAS output is not peer
review.
