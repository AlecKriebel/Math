# Hostile review checklist: \(h=p(p+q)\), fixed-\(p\) plus
ramification-contact \(\delta=2,\{1,1\}\) exclusion

1. Recompute the generic \(pq\) gcd and the two boundary gcds
   \(C=0,C=-T\) without using an open-chart tangent basis.
2. Verify both boundary \(E_7\) ranks are five and keep the boundaries
   routed to \(\delta=3\).
3. Check the generic tangent basis and the complete factorization of
   the lifted contact determinant, including signs and powers.
4. At \(16C=9T\), discard the generic basis and independently rebuild
   the fresh basis, \(E_7\) rank minor, and full contact determinant.
5. At \(12C=-7T\), independently rebuild the fresh basis, rank-four
   minor, complete kernel, and the sign of the Veronese obstruction.
6. Confirm the three charts cover the exact open, including \(T=0\),
   and that no intersection of the two internal divisors is missed.
7. Replay the constant \(E_6\) block and audit the all-binary
   field/descent exit.
8. Keep the theorem scoped to (1)--(2) until the remaining leaves are
   closed.

This is a mathematical audit checklist.  Exact CAS output is not peer
review.
