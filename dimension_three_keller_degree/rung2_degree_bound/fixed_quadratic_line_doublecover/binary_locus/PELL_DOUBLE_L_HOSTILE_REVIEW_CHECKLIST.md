# Hostile review checklist: \(h=p(p+q)\), doubled-\((p+q)\)
\(\delta=2,\{1,1\}\) exclusion

1. Treat this leaf independently; do not import the doubled-\(p\)
   answer through an unproved marked-row symmetry.
2. Recompute the generic gcd, both \(\delta=3\) boundary divisors, and
   the exact-\(\delta=2\) pivot \(A=B\).
3. Verify both contact minors in (5).  Check that no claim rests on the
   quartic factor appearing in a different single minor.
4. Reconstruct the \(A=B\) tangent basis from scratch and replay (7).
5. Verify the constant \(E_6\) determinant and the all-binary
   field/descent exit.
6. Keep the theorem scoped to this doubled-\((p+q)\) leaf.

This is a mathematical audit checklist.  Exact CAS output is not peer
review.
