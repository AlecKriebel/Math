# Hostile review checklist: \(h=p(p+q)\), fixed-\((p+q)\) plus
ramification-contact \(\delta=2,\{1,1\}\) exclusion

1. Recompute the generic \(q(p+q)\) gcd and the boundary gcds
   \(C=0,C=5B\) without using the generic tangent basis.
2. Verify both boundary \(E_7\) ranks are five and keep the boundaries
   routed to \(\delta=3\).
3. Check the generic tangent basis and every factor, exponent, and sign
   in the lifted contact determinant.
4. At \(B=-16C\), discard the generic basis and independently rebuild
   the fresh tangent basis, \(E_7\) rank minor, and contact determinant.
5. At \(5B=4C\), independently rebuild the fresh basis, rank-four
   minor, full kernel, and Veronese obstruction.
6. Check that the two internal divisors do not meet on the exact open
   and that the charts include \(B=0\).
7. Replay the constant \(E_6\) block and audit the all-binary
   field/descent exit.
8. Do not infer this leaf from an unproved marked-row symmetry with
   the fixed-\(p\) companion.

This is a mathematical audit checklist.  Exact CAS output is not peer
review.
