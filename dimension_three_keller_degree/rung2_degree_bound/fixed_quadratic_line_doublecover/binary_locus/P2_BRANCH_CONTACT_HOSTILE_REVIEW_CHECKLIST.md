# Hostile review checklist: \(h=p^2\), branch-contact
\(\delta=2,\{1,1\}\) exclusion

1. Recompute the gcd on \(D\ne0\) and the \(D=0\) mutation.
2. Verify the generic lifted-contact determinant and independently
   reconstruct the \(\Lambda=0\) tangent basis; do not divide by
   \(\Lambda\).
3. At \(C=0,A\ne0\), check both the rank-four kernel and the sign of
   \(Y^2-XZ\).
4. At \(A=C=0\), prove that the two-dimensional lifted kernel meets the
   Veronese cone in exactly the single line used in (11).
5. Start from the full determinant at \(R=Dq^3\) with every binary
   lower coefficient and all entries of \(L\).  Recompute the rank-six
   \(E_6\) solve and the \(E_5\) compatibility \(3Dkv_0^2\).
6. Verify that the \(E_4\) coefficient comparison really forces both
   \(M_0\) and \(M_3\), and that \((k,0,-v_2)\) is nonzero.
7. Audit the all-binary \(k=0\) exit over the correct plane function
   field, including generic-degree descent and the birational Keller
   theorem.
8. Keep the result scoped to this one incidence family until the other
   \(\{1,1\}\) leaves are separately eliminated.

This is an adversarial mathematical checklist.  Exact CAS output is not
peer review.
