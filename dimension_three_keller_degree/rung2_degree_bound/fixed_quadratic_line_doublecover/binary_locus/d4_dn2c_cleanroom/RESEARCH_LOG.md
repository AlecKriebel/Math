# Research log

## 2026-07-26T05:57:51Z

- Began an independent clean-room derivation for frozen family
  D4-DN-2C without reading `d4_dn2c_full_rebuild`.
- Constructed the three exact \(E_7\) coefficient matrices.  Their
  ranks are \(2,3,4\), giving nullities \(0,2,4\).
- Restored all 18 lower variables that enter \(E_6\).
- Found two lower-free \(r^3\) coefficients forcing the two
  \(r^2\)-contact parameters to vanish.
- Computed the constant-rank \(r^1\) augmented-minor ideal.  Its reduced
  zero set is a pair of conjugate planes over
  \(\mathbb Q(\sqrt{-2})\).
- Removed the constant rank-three image in the \(r^0\) equations and
  computed an explicit quotient atlas.  The quotient ranks are \(2,1,0\)
  off the plane intersection, on its punctured intersection, and at the
  origin.  Augmented ranks agree on every chart.
- Scope decision: bank this only as a full-lower \(E_6\) contact
  theorem.  No D4-DN-2C exclusion is claimed.

## 2026-07-26 lower descent

- Closed both contact-plane interiors at \(E_5\) with three selected
  lower-free coefficients whose quadratic coefficient determinant is
  \(-768(-22+\sqrt{-2})\).
- On the punctured intersection, recorded exact \(E_6,E_5\) pivots and
  the \(F,G\) and conjugate quadratic factorization of the \(E_5\)
  compatibility ideal.
- Found \(E_4\) forces \(X=t_0-t_1+t_2=0\), coalescing the conjugate
  quadratic branches.  Verified the \(G\) branch forces \(F\) by a
  square.  The reverse \(F\)-only implication remains the explicit gap
  in this clean-room audit.
- Independently closed the \(F=G\) overlap at \(E_3\): all four binary
  coefficients share a factor \(C\), \(\det L\) is divisible by \(C\),
  and the quotient equations triangularly force \(C=0\).
- Closed the origin at \(E_4\) by four lower-free coefficients, forcing
  the literal six-coefficient \(r\)-collapse.
