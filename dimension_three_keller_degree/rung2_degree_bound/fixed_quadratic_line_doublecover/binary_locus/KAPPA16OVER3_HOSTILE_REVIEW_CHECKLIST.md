# Hostile-review checklist: \(\kappa=16/3\) exclusion

1. Verify that \(h=(p+q)(3p+q)\) has orbit invariant \(16/3\), and that
   the chosen fixed-root/contact orientation represents all stabilizer
   orbits claimed.
2. Reconstruct the exact open condition
   \(b(a-b)(a+3b)\ne0\) from local gcd orders.
3. Reconstruct the \(\mathcal M_1\) column
   \((4p+q,-3q,a-b)\), both \(\mathcal M_0\) multiples, and the factor
   \(k/2\) in the integrated \(E_7\) family.
4. Recompute \([r^3]E_6\) and the complete \([r]E_6\) endpoint argument.
   In particular audit \(a=-2b\) separately.
5. Check that the two rank-four minors in the constant \(E_6\) solve
   really cover every \(b\ne0\) point.
6. At \(\lambda=0\), verify the rank-two \(E_5\) cover and literal zero
   third column of the linear part.
7. At \(\lambda\ne0\), audit the degree-at-most-four claim after
   triangularizing \(F_3\), base-change/descent of generic degree, and
   the birational Keller conclusion.  Reject any appeal to full
   two-dimensional JC.
8. Fault-inject both CAS paths and the strict transcript wrapper.

State explicitly whether the candidate theorem is safe to promote.
