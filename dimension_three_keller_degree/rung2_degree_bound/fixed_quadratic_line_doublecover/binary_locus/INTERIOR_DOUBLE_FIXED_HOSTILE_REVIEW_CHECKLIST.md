# Hostile review checklist: squarefree-interior doubled-fixed-root
\(\delta=2,\{1,1\}\) exclusion

1. Recompute the generic \(L^2\) gcd and the three deeper-incidence
   boundary gcds in (4).  Keep \(w=0,w^2=1\) routed to the correct
   fixed-divisor boundary orbits.
2. Verify the complete generic \(E_7\) tangent basis, including the
   \(D^2\) scaling and the sign of the decisive rank minor.
3. Rebuild the lifted contact matrix and the four selected maximal
   minors.  Do not replace the projective rank-drop analysis by a
   multivariate polynomial gcd.
4. Independently recompute all four resultants in (11) and their monic
   gcd.  Check the variable eliminated is \(a=A/B\).
5. Audit the missing projective endpoint \(B=0\), including the
   univariate gcd \(w^2\).
6. Treat \(D=Aw+B=0\) in a fresh tangent chart.  Verify that this is
   the triple-root case \(R=L^3\), not a routed mutation.
7. Check that the factors \(w^2=3,1/3\) on the fresh chart are exactly
   the two excluded branch contacts.
8. Replay the constant \(E_6\) block and audit the all-binary
   field/descent exit.
9. Keep this theorem scoped to the doubled-fixed-root interior leaf;
   the other squarefree-interior incidence types remain separate.

This is a mathematical audit checklist.  Exact CAS output is not peer
review.
