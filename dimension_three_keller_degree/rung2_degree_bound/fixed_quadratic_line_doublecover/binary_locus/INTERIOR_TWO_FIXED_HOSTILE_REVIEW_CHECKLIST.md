# Hostile review checklist: squarefree-interior two-fixed-root
\(\delta=2,\{1,1\}\) exclusion

1. Recompute the generic \(LM\) gcd and all four boundary gcds in (3).
   Keep \(w=0,w^2=1\) routed to the appropriate fixed-divisor orbits.
2. Verify both displayed tangents against the signed \(E_7\) row and
   check that the rank-six minor makes them a complete basis.
3. Rebuild the lifted contact matrix from the determinant and verify
   the rank-four minor without assuming the kernel formula.
4. Recompute all five entries of the primitive kernel and check that
   the entire \(6\times5\) matrix kills it.
5. Check the sign in
   \(K_Y^2-K_XK_Z=24E_M^2(w^2-1)^2E_L^2\).
6. Confirm that a nonzero scalar multiple of the kernel cannot satisfy
   the Veronese relation, including over the algebraic closure.
7. Replay the constant \(E_6\) block and audit the all-binary
   field/descent exit.
8. Keep this theorem scoped to the two-fixed-root interior leaf.

This is a mathematical audit checklist.  Exact CAS output is not peer
review.
