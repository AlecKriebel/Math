# Research log - exact-\(\delta=4\) umbrella

## 2026-07-26T06:45:00Z - assembly started

- Froze the bridge from the six immutable atlas IDs to the six independently
  packaged family exclusions.
- Added a fail-closed manifest comparison against the certified
  \(19+6+1\) high-incidence denominator.
- Recorded the strict scope: this would close \(6/26\) fine families and the
  complete exact-\(\delta=4\) stratum, while leaving the global
  \(4/14\) certified, \(4/14\) provisional, \(6/14\) open count and the
  universal total-degree floor four unchanged.
- Withheld promotion pending a final hostile assembly audit, a complete
  aggregate run, and rendered-paper inspection.

## 2026-07-26T06:58:00Z - canonical-ledger repair

- A hostile audit found that the first bridge pointed to the superseded
  primary \(17+6+1\) ledger even though the reconciliation wrapper also ran
  the canonical \(19+6+1\) ledger.
- Rebound `FAMILIES.json` directly to the blinded canonical denominator,
  adopted its six canonical IDs, and made the aggregate execution plan come
  from the validated manifest instead of a second hard-coded list.
- A second hostile mutation showed that path and theorem-scope strings were
  not yet bound.  The verifier now hard-binds the narrow exact-\(\delta=4\)
  scope, canonical path, schema, PASS status, textual scope, and exact
  \(19/6/1/26\) counts before emitting its canonical marker.

## 2026-07-26T07:25:07Z - certified release

- The revised dynamic aggregate completed with all six distinct family
  terminal markers and
  `EXACT_DELTA4_SIX_FAMILY_EXCLUSION_STRICT_PASS`.
- The standalone hostile wrapper independently repeated the aggregate,
  replayed the immutable fourteen-row taxonomy, confirmed that the containing
  row remains open, and rejected mutations of the canonical ID, alias,
  proof path, marker, denominator source, theorem scope, and counts.
- Its final marker was
  `EXACT_DELTA4_HOSTILE_UMBRELLA_AUDIT_STRICT_PASS`.
- The exact-\(\delta=4\) structural theorem is promoted.  It excludes
  \(6/26\) frozen fine families but changes neither the global
  \(4/14,4/14,6/14\) status nor the universal total-degree floor four.
- The final five-page `paper.pdf` was regenerated from `paper.tex`, rendered
  page by page at 130 dpi, and visually inspected.  No clipping, overlap,
  missing glyph, unresolved citation, or layout warning was found.
