# Hostile review checklist: \(h=p^2\), simple-fixed-root
\(\delta=2,\{1,1\}\) exclusion

1. Recompute the exact gcd on \(BC\ne0\), and verify that \(B=0\) and
   \(C=0\) really route to \(\delta\ge3\), while \(A=0\) remains inside.
2. Reconstruct the two Hilbert--Burch tangents independently and verify
   that the signed-minor vector spans the complete lifted \(E_6\)
   contact kernel on \(BC\Delta\ne0\).
3. Check that \(Y^2-XZ\), rather than a sign- or factor-altered conic,
   is the correct Veronese equation for \((s^2,st,t^2)\).
4. Replay \(\Delta=0\) with a fresh basis.  Do not infer it by division
   from the generic chart.
5. Verify explicitly that the stabilizer sends every point of
   \(256AC+11B^2=0\), \(BC\ne0\), to
   \((-11,16,1)\), including the induced scaling of the third target.
6. Start from the full determinant with every binary \(H_3,H_2\)
   coefficient and all nine linear entries.  Confirm the rank-six
   constant \(E_6\) solve and that no lower coefficient can alter
   \([r^2]E_5\).
7. Audit the \(k=0\) branch separately and verify every hypothesis of
   the degree-four plane-field, descent, and birational Keller exit.
8. Confirm that the theorem is scoped to one exact-\(\delta=2\)
   incidence mechanism and is not presented as closure of the entire
   \(\{1,1\}\) row.

This checklist is for a hostile mathematical replay.  Passing exact
computer algebra is not peer review.
