# Hostile-review checklist: doubled-root \(\kappa=4\) exclusion

1. Reconstruct the exceptional coefficient equation, the exact open
   \(b(3a-2b)\ne0\), and the kernel
   \((6p+4q,-2q,6a-b)\).
2. Verify completeness of the integrated \(E_7\) family, including the
   \(k/2\) factor and every lower coefficient.
3. Recompute \([r^3]E_6\) and both branches of the \([r]E_6\) solve,
   especially \(6a+11b=0\).
4. Recompute the rank-four constant \(E_6\) solve without dividing by
   \(6a-b\).
5. At \(\lambda=0\), check the rank-two \(E_5\) cover and zero third
   column.
6. At \(\lambda(6a-b)\ne0\), audit the plane-field degree bound,
   base-change/descent, and birational Keller exit for hidden plane-JC
   inheritance.
7. At \(a=b/6\), reconstruct the rank-six \(E_5\) solve and
   \([r]E_4=6b\lambda^2(p+2q)^3\).
8. Run fault injection against both CAS scripts and the strict wrapper.

The final report should also cross-check that the three-piece list in
`DELTA2_K20_UMBRELLA.md` is exhaustive before promoting the umbrella.
