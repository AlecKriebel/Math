# Research log: independent bridge audit for Q2-E0-A2-B2-D2-N1

All timestamps are UTC.

- 2026-07-26T09:26:31Z — Created this dedicated audit directory.  Before
  this checkpoint, the only mathematical sources read were
  `FROZEN_TAXONOMY_v1.md` and `frozen_manifest_v1.json`.  In particular, no
  proposed `BRIDGE_Q2_E0_A2_B2_D2_N1*` file and no working conic proof or
  checker had been opened.
- 2026-07-26T09:26:31Z — Clean-room derivation: the frozen tuple forces a
  primitive canonical quadratic pencil and a degree-two conic embedding.
  The latter is the complete binary quadratic linear system, so a
  simultaneous source-target conjugation gives the leading form
  `(p^2,p*q,q^2)` without choosing or dividing by any frozen coefficient.
- 2026-07-26T09:26:31Z — Clean-room double-line analysis: a pencil with two
  distinct double lines is generated by their squares and makes `p/q` a
  square of a rational function.  Its relative closure is therefore
  strictly larger than `C(p/q)`, contradicting `N1`.  Examples show that
  both zero and exactly one double-line member are compatible with the
  relative-closure condition.
- 2026-07-26T09:36:00Z — After the clean-room note was saved, opened
  `WORKING_CONIC_TYPE_22.md`, its SymPy and PARI checks, and then the
  proposed bridge for comparison.  Tightened the pivot statement using the
  fact that the three conic components are linearly independent:
  `C15`--`C44` are empty.  Also recorded the exact two-double-line boundary
  destination `Q2-E0-A1-B4-D2-N2`.
- 2026-07-26T09:36:00Z — The existing PARI regression passed.  The default
  system Python could not import SymPy; the independent replay uses the
  available SymPy 1.14 environment when invoked directly and is designed
  to fail closed if SymPy or PARI is unavailable.
- 2026-07-26T09:43:40Z — Completed exact reconstruction of the missing
  completeness assertions.  For both unique-double-line pencil forms the
  degree-seven matrix is `36 x 18`, has rank 12, and has kernel exactly
  `M(p,q)`; its compatibility ideal is exactly
  `<alpha*ell_y,alpha*ell_z>`.  The degree-six linear matrix is `28 x 9`,
  has rank 6, and has kernel exactly the arbitrary-first-column space.
  Both displayed degree-six compatibility ideals and the full sole-residue
  affine solution also match.
- 2026-07-26T09:43:40Z — The independent verifier passed, including the
  existing SymPy and PARI implementations.  The strict wrapper then passed
  all six required-failure mutations and the optimized-Python guard:
  `INDEPENDENT_Q2_E0_A2_B2_D2_N1_STRICT_PASS`.  Final verdict: `PASS`,
  recommend promotion of exactly this frozen row.
