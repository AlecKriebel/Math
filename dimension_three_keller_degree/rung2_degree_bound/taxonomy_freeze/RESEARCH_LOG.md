# Taxonomy-freeze research log

## 2026-07-25T20:24:52Z — version-one denominator frozen

- A blinded derivation and reconciliation independently recovered one
  rank-one and thirteen canonical rank-two leading leaves.
- The historical 68-bucket proposal was rejected because it was not disjoint
  or independently complete; it is not part of the denominator.
- The first hostile replay found no missing leaf but blocked the release
  artifact on proof compression, terminology, and fail-closed verification.
- After correction, the hostile re-audit passed the normalization, rank-one
  cone argument, relative-closure pencil, polynomial factorization,
  canonicity, tuple enumeration, and 45-piece pivot partition.
- Mandatory SHA-256 hashes and the repaired verifier pass.  Version one is
  frozen at 14 inclusive leaves.  The seven exclusion status labels remain
  logically separate from this completeness certificate.

## 2026-07-25T19:30:28Z — freeze gate opened

- Halted the active doubled-nonbranch leaf calculation and all other
  quartic leaf assignments.
- Defined stable row/leaf/chart identifiers and the freeze-violation
  protocol.
- Reconstructed the leading-row denominator independently of exclusion
  status: one rank-one row and thirteen rank-two curve-image rows.
- The provisional global row score is seven audited exclusions out of
  fourteen rows.  This is not yet the certified F1 denominator because
  the incidence and boundary manifests are incomplete.
- Launched a clean-room agent with no conversation history and an explicit
  prohibition on reading the project taxonomy or exclusions.
- Launched separate primary-source correction and monodromy-obstruction
  tasks.

The next root task is to inventory the incidence invariants and every
boundary chart for the seven open rows, then check that the seven excluded
rows have archived completeness manifests rather than merely terminal
theorem statements.

## 2026-07-25T19:30:28Z — first candidate-manifest correction

- The initial 65-leaf draft used the Hilbert--Burch lists only for
  nonzero normal cubics and thereby omitted the terminal \(R=0\) branch
  in each of the three binary line-cover rows.
- Added stable candidate leaf `L00` in the common-factor-free quartic line
  cover, fixed-linear triple cover, and fixed-quadratic double cover.
- The candidate denominator is now 68.  This correction occurred before
  certification and is evidence that the freeze gate was necessary.
