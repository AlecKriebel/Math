# Research Log — 2026-07-23

All times UTC.

## 18:39:53 — Program initialization

- Created a clean worktree on `main` at `/Users/alec/Documents/Math-kissing5`
  because the original worktree contained unrelated uncommitted research on
  branch `codex/h668-theory`.
- Left the original worktree and all of its changes untouched.
- Established the accepted baseline \(40\leq\tau(5)\leq44\).
- Started independent construction and higher-order-bound research agents
  without declaring a favored exact value.
- Created the mechanism-based approach registry and claims ledger.

## 18:39:53 — Focused primary-source baseline

Sources inspected:

1. C. Bachoc and F. Vallentin, *New upper bounds for kissing numbers from
   semidefinite programming*, JAMS 21 (2008), 909–924,
   <https://doi.org/10.1090/S0894-0347-07-00589-9>.
   The method uses positive-semidefinite three-point matrix kernels and
   polynomial nonpositivity on the full feasible triple-inner-product domain.
2. H. D. Mittelmann and F. Vallentin, *High accuracy semidefinite programming
   bounds for kissing numbers*, Experimental Mathematics 19 (2010), 174–178,
   <https://doi.org/10.1080/10586458.2010.10129070>.
   It reports that dimension 5 is narrowed from possible values 40–45 to 40–44.
3. F. Szöllősi, *A note on five dimensional kissing arrangements*,
   arXiv:2301.08272, <https://arxiv.org/abs/2301.08272>.
   It records a third non-isometric 40-point configuration and explicitly warns
   against uniqueness assumptions.
4. H. Cohn and I. Rajagopal, *Variations on Five-Dimensional Sphere Packings*,
   Discrete & Computational Geometry (2026),
   <https://doi.org/10.1007/s00454-026-00841-x>.
   It proves at least four non-isometric 40-point configurations and confirms
   that the published upper bound remains 44 as of July 2026.

Imported hypotheses and normalization details still requiring a deeper source
audit are tracked in `literature/PRIMARY_SOURCES.md`.

## 18:39:53 — First exact certificate

- Encoded all 40 unnormalized \(D_5\) roots as integer vectors with exactly two
  nonzero entries, each equal to \(+1\) or \(-1\).
- Added a verifier that checks count, dimension, distinctness, norm squared 2,
  and every one of the 780 unordered pair inequalities exactly.
- Added negative tests for duplicate, wrong-norm, and boundary-violating data.

No upper-bound advance is claimed at this checkpoint.

## 19:12:31 — Exact ordinary-LP barrier and higher-order redirection

- Constructed an exact positive atomic measure of mass 41 with nontrivial
  support
  \[
  -77/100,\quad -11/25,\quad -9/100,\quad 499/1000
  \]
  and ordered-pair counts \(176,262,652,550\).
- Verified its normalized dimension-five Gegenbauer moments through degree 53
  using exact rational recurrence.  The smallest is
  \(M_2=1027/16000>1/16\).
- Proved all higher moments positive using the Gegenbauer integral
  representation and an explicit \(O(k^{-3/2})\) tail estimate.  This proves
  that ordinary Delsarte LP, even with an absolutely convergent nonnegative
  Schoenberg expansion, cannot give a strict upper bound below 41.
- The exact proof and independent standard-library checker are in
  `proofs/two_point_lp_barrier.md` and
  `verifiers/verify_two_point_barrier.py`.  A separate adversarial agent is
  auditing the normalization and analytic constants.
- Identified the fixed-cardinality three-point marginal identity
  \(\sum_{u,v}x(u,v,t)=41x(t,t,1)\), which is linear when the target
  cardinality is fixed.  This is the strongest higher-order candidate now.

### Discovery-only numerical input

- Retrieved the public numerical \(N=41,d=5\) code from
  `https://spherical-codes.org/code/5/41`.
- Retrieval date: 2026-07-23.
- SHA-256:
  `c54b38d8216bf76a79c57119fc46245811188e1de05c840c68a33cec9b7fe1b0`.
- Its computed maximum inner product is approximately
  `0.514994652512166`; it is therefore a near miss, not a code at threshold
  \(1/2\), and not a certificate of any theorem.

### Public second-level Lasserre implementation trial

- Downloaded the public data archive associated with de Laat--Leijenhorst--de
  Muinck Keizer, *Optimality and Uniqueness of the \(D_4\) Root System*,
  DOI data record `10.4121/74ce1c25-6fca-4680-8a36-e9c18e7e9594`.
- Archive SHA-256:
  `2f806fac5cf043e90bebfb6c1aab82a3d27572a2da64bb848faf2fd4b58f96d2`.
- Used official Julia 1.10.11 archive SHA-256
  `bd5706c10dceffeebe56d2d3f371e3d8d4b27195cad7489f22f6292af907f05b`
  in an isolated temporary depot.  The resolved stack included
  `ClusteredLowRankSolver 2.1.1` and `Nemo 0.54.2`.
- Command attempted a low-degree dimension-five call
  `las2(5, [-1,1//2], 2; precision=128, eps=1e-8, delta=2, d2=2)`.
- The run failed before SDP construction with `UndefRefError` in
  `sqrt_factors` while generating the four-point symmetry-adapted basis.
  This is recorded only as a reproducibility/environment issue.  It gives no
  mathematical bound and no solver evidence.

### Adversarially rejected rank shortcut

- For any five-dimensional code, the entrywise kernel
  \(K_{ij}=(G_{ij}+1)(G_{ij}-1/2)\) has diagonal one, nonpositive
  off-diagonal entries, rank at most \(1+5+14=20\), and at most one negative
  eigenvalue.
- The tempting generic lemma `rank(K) >= N/2` is false.  Applying the same
  construction to the exact 60-point \(D_6\) root system gives a matrix of
  rank at most \(1+6+20=27<30\), still PSD minus one rank-one term.
- Any viable rank route must therefore use information special to dimension
  five beyond this sign and inertia pattern.

## 19:32:06 — Construction checkpoint and rank-kernel audit

### Construction track

- Completed a numerical search over \(N=41,42,43,44\), using multistart
  soft-max and hinge objectives, epigraph SLSQP, insertion/deletion cascades,
  perturbations of public basins, and surgery on four known exact 40-point
  configurations.
- No candidate met the kissing threshold.  The nearest inspected 41-point
  numerical basin has maximum inner product about
  `0.514994652512166`, still about `0.014994652512166` above \(1/2\).
- All numerical outputs are labeled `NUMERICAL EVIDENCE ONLY`; their failure
  is not used as an upper bound.
- Proved exactly that fixed \(D_5\) is saturated: every added unit vector has
  inner product at least \(\sqrt{2/5}>1/2\) with a root.  This is explicitly
  not a global nonexistence theorem.
- Reproducible scripts, seeds, versions, hashes, and contact/Gram diagnostics
  are in `experiments/construction_round1.md` and
  `experiments/random_codes/`.

### Independent audit of the ordinary-LP barrier

- A separate adversarial agent checked the Gegenbauer recurrence, all exact
  moments through degree 53, the integral representation and Gaussian
  constant, every tail inequality, and the infinite-series interchange.
- Verdict: the ordinary two-point barrier theorem is correct.
- Narrowed one overbroad sentence: the zero-contact witness survives parity
  and contact upper bounds permitting zero, but does not by itself address a
  universally valid positive contact lower bound or rowwise contact theorem.
- Pfender-generator compatibility remains outside the certified theorem until
  its normalization receives a separate source audit.

### Exact rank counterexamples and surviving constraint

- Formalized the \(D_6\) polynomial-kernel spectrum
  \[
  -20^1,\ 4^5,\ 2^{15},\ 5^6,\ 0^{33},
  \]
  proving exact rank 27 for an order-60 kernel.
- Constructed an independent Cayley-graph example on \(\mathbb F_7^2\).
  Its matrix \(M=I-A/2\) has spectrum
  \((-14)^1,0^{30},(7/2)^{18}\); a specified order-41 principal submatrix
  has rank 19 and exactly one negative eigenvalue.
- The graph example even satisfies
  \(M+(3/10)J\succeq0\) of rank 19 and diagonal \(13/10\).
  Hence sign, inertia, entry range, low rank, and the combined
  PSD-minus-rank-one form do not suffice.
- What survives for an actual dimension-five Gram source is the separate
  harmonic-factor structure.  For
  \(R=(4/5)H_2+G/2\), Ky Fan gives
  \[
  \sum_{i=1}^5\lambda_i(R)\geq N/2,\qquad
  \sum_{i=1}^{14}\lambda_i(R)\geq4N/5.
  \]
- Exact proof and standard-library verifier:
  `proofs/rank_kernel_barriers.md` and
  `verifiers/verify_rank_kernel_barriers.py`.

## 20:30:00 — Exact local, three-point, semialgebraic, and graph barriers

### Fixed-cardinality three-point relaxation

- Derived the exact measure decomposition
  \[
  x=\delta_{111}+\int(\delta_{1qq}+\delta_{q1q}+\delta_{qq1})\,d\alpha(q)
  +\nu
  \]
  for \(N=41\), with \(\alpha(I)=40\), \(\nu(D)=1560\), and
  \(\pi_u\nu=\pi_v\nu=\pi_t\nu=39\alpha\).
- Constructed positive rational pseudo-distributions satisfying the full
  closed triple domain and all fixed-size marginals.  The strongest exact
  certificate makes every radial harmonic matrix positive semidefinite for
  \(0\leq k\leq16\), and all pair Gegenbauer moments positive through degree
  100.
- This is a rigorous feasibility barrier, not a code.  It rules out a
  low-degree fixed-\(N\) three-point proof through the certified degrees.
- A separate degree-two Tverberg argument at the exact threshold
  \(41=2(19+1)+1\) forces three disjoint measures with matching first and
  second moments.  An exact rank-five 18-point code formed from three regular
  simplices realizes this conclusion, so it is insufficient by itself.

### Local links and contact audit

- Proved the exact conditional projection lemma.  Links of contact
  \(k\)-cliques reduce to \(A(5-k,1/(k+2))\), giving common-neighbor bounds
  \(15,7,4,2,0\).
- Verified the \(S^3\) auxiliary LP polynomial and exact rational
  seven- and fourteen-point calibration codes.
- Constructed an exact 26-point inclusion-maximal code with empty contact
  graph:
  \[
  \{\pm e_i\}_{i=1}^5\cup
  \{\varepsilon/\sqrt5:\prod_i\varepsilon_i=1\}.
  \]
  This decisively rejects unrestricted contact-richness or jamming premises.

### Compact semialgebraic reduction

- Proved \(A(4,9/16)\leq32\) by an exact degree-six LP polynomial.
- Every hypothetical 41-code consequently has frame operator
  \(S\succ(9/25)I\), and Cauchy--Binet supplies a maximum-volume five-point
  basis with Gram determinant greater than
  \(6488829/7318339843750\).
- In that basis, all 180 coefficients lie in \([-1,1]\), and all 749,398
  coefficient minors are bounded by one.  Existence of a 41-code is
  equivalent in both directions to a compact rational system with 190
  variables and 36 norm equalities.
- An \(11^5\)-cell rational coefficient cover has universal cell capacity one.
  The remaining obstruction is the continuously varying ten-variable basis
  Gram matrix and a 154-dimensional intrinsic search.

### Antipodal and deep-pair graph consequences

- Proved by an even Gegenbauer polynomial that every antipodal
  five-dimensional kissing code has at most 40 points.  The apparent
  42-point equality case would require an impossible row count \(64/5\).
- For a hypothetical 41-code, the graph on pairs with inner product
  \(<-1/2\) therefore has independence number at most 20.  It is triangle-free
  by the squared norm of a three-vector sum.  An exact cover argument then
  forces at least 23 unordered deep pairs; the abstract graph bound is sharp
  for \(C_5\sqcup18K_2\).

### Four-point deep-wedge inequalities

- For \(a>3/4\), every two deep neighbors of a center force a pair with inner
  product at least \(b=2a^2-1\), and that high pair has at most one deep common
  center.  Hence
  \[
  Q_b\geq2\sum_x\binom{d_a(x)}2\geq8D_a-20N.
  \]
- An independent audit found that strictness is essential: at \(a=3/4\), an
  exact \(K_{2,2}\) example has two common centers.  A general cap argument
  supplies the appropriate multiplicity envelope below that threshold.
- For \(a>1/\sqrt2\), residual projection proves \(d_a(x)\leq5\).
- These inequalities eliminate the first exact ordinary-LP mass-41 witness.
  A rank-deficit refinement using the singular \(5\times5\) residual Gram
  matrix has also been derived.  A new rational mass-41 witness surviving the
  aggregate family is undergoing exact packaging and independent audit; no
  nonexistence conclusion is claimed.

### Discovery-only second-level Lasserre trials

- Patched two source-compatibility issues in a temporary copy of the public
  `LasserreSphericalCodes` archive; no upstream or repository source was
  modified.
- With 192-bit arithmetic, the low-degree numerical objectives were
  approximately 90 at \(d_1=d_2=\delta=4\), and 48 at degrees 6 and 8.
  Each run reached a small numerical primal-dual gap, but none is an exact
  certificate.
- The primary paper reports that the much larger
  \(d_1=14,d_2=\delta=16\) computation did not improve the dimension-five
  integer bound 44.  This prevents misreading the low-degree progression as a
  likely short route to 40.

## 20:43:15 — Local-hybrid audit and Lasserre triage

- Independently rechecked the direct Pfender row proof, including the
  nonnegative residual Gram estimate and the strict
  \(-1/\sqrt2\) boundary.
- Rechecked every multiplicity in the rank-deficit inequality: directed
  deep-edge excess is counted at most once per oriented incidence, while
  strict \(a>3/4\) makes each forced endpoint pair belong to at most one
  deep wedge.
- The exact verifier confirms that the strengthened mass-41 pseudo-measure has
  positive Gegenbauer moments in every degree and passes all 18 rational event
  cells, with rank-deficit margin \(337/1000\).
- Stopped the discovery-only degree-10 public Lasserre run after more than
  thirty minutes in basis generation. Lower degrees had plateaued at objective
  48, and the primary paper already reports no dimension-five integer
  improvement at substantially higher truncation. No solver conclusion or
  mathematical claim is inferred from the interrupted run.

### Construction round 2

- Reproducible asymmetric layer, higher-root-map, projective-line, and
  sharp-deep-graph searches covered \(N=41,42,43,44\).
- The best independently generated unrestricted 41-point candidate came from
  an \(E_6\) map-surgery seed and has recomputed maximum inner product
  \(0.5155570516153127\), with rank-five Gram spectrum and active graph stored
  in the round-2 JSON log.
- A constrained \(C_5\sqcup18K_2\)-motivated ansatz reached only
  \(0.5416720642083640\), and unrestricted release reached
  \(0.5205893864137946\). Positive optimization slack is not an
  infeasibility proof.
- No 41--44 point construction was found. Exact antipodal and negative-tail
  theorems were separated from the numerical discovery artifacts.

## 21:51:06 — Infinite three-point barrier and first incidence separator

### All harmonic degrees

- Upgraded the fixed-\(41\) rational pair/triple pseudo-distribution from a
  degree-16 check to a proof covering every harmonic degree and arbitrary
  radial degree.
- For \(1\leq k\leq505\), all active kernel blocks have exact positive
  rational LDL pivots.  After parity normalization, the boundary limits have
  rigorous minimum-eigenvalue lower bounds exceeding \(0.428\) and \(0.402\).
  Rational entrywise Chebyshev bounds make the interior perturbation smaller
  than the limit for every \(k\geq506\).
- The same pair measure has every ordinary dimension-five Gegenbauer moment
  positive: exact recurrence covers the finite prefix and an integral
  estimate covers the tail.
- This certifies feasibility of the entire fixed-cardinality two/three-point
  formulation recorded here.  It is a limitation theorem, not a 41-point
  configuration, because four-point consistency and rank five are absent.
- An independent adversarial pass rederived the parity normalizations,
  endpoint and determinant-zero contributions, same-parity recurrence,
  rational perturbation norms, pair-moment Gaussian tail, and
  \(H=V^{\mathsf T}W_kV\) factorization.  It found no mathematical objection.
  The verifier must be run without Python's `-O` flag because it intentionally
  uses assertions.

### Degree-three residual-vector separator

- Constructed a second exact 41-vertex labeled pseudo-Gram object that retains
  the six-label pair distribution, every \(3\times3\) principal determinant,
  the local deep-wedge constraints, and every BV block through total degree
  two.
- Its first harmonic failure has the elementary form
  \[
  \frac1{41}\sum_i\left\|\sum_{j\ne i}
  \left(g_{ij}-\frac83g_{ij}^2\right)
  (x_j-g_{ij}x_i)\right\|^2
  =-\frac{105027064094021}{15375000000000}.
  \]
  The left side is nonnegative for every genuine common Gram source.
- Exact motif accounting locates most of the negative contribution in 1,056
  deep--middle colored wedges.  Their closure labels, rather than four-cycle
  counts, are the immediate compatibility bottleneck.  A universal
  continuous-label closure bound is not yet proved.

### Construction round 3

- Completed 152 deterministic unrestricted Riemannian
  augmented-Lagrangian trials for \(N=41,42,43,44\), with Gaussian, public
  benchmark, higher-root projection, coordinate projection, and round-2 warm
  starts.
- No result reached \(1/2\).  Eleven independent Gaussian 41-point starts
  reproducibly approached a basin near \(0.515557052\); the better imported
  basin remains \(0.5149946525251737\).  Both have full rank-five numerical
  Gram diagnostics and stored active graphs, but neither supplies a rigorous
  bound.
- Exact reconstruction was correctly skipped because every candidate had a
  positive feasibility gap.

## One-sided cap bound and robust origin depth

- Found and exactly certified a degree-11 Delsarte polynomial over
  \(\mathbb Q(\sqrt3)\) proving \(A(4,1/\sqrt3)\le33\), with objective
  approximately \(33.97371538<34\).
- A self-contained projection/reflection argument, including the height
  \(z=1/2\) self-reflection boundary, combines this with the rigorous
  \(\tau(5)\le44\) baseline to prove the new exact one-sided bound
  \(B(5)\le38\).
- Therefore every open hemisphere through the origin contains at least three
  points of a hypothetical 41-code.  The origin remains in the interior of
  the convex hull after any two deletions.  The exact directional interval
  \(\max(0,8-r(u))\le b(u)\le r(u)+3\) yields
  \(d_{\rm contact}(x)+d_{<0}(x)\ge7\) and
  \(d_{\rm contact}(x)\le d_{<0}(x)+2\) at every vertex.
- An independent adversarial audit recomputed every algebraic coefficient and
  objective norm, checked projection and reflection boundary cases, and
  verified the Tukey-depth and contact/sign deductions.  No mathematical
  objection was found.  These are necessary conditions, not a 41-point
  exclusion.

## 22:35:58 — Rank-five spectral cuts and improved frame conditioning

- Proved the sharp centered spectral inequality
  \[
  20D^2\le9V^3
  \]
  for every PSD matrix of rank at most five.  The compact
  Lagrange-multiplier proof includes zero eigenvalues, and the exact
  11-point code \(\{\pm e_i\}\cup\{(1,1,1,1,1)/\sqrt5\}\) attains equality.
- Translated the inequality exactly to fixed-41 pair/triple moments.  It
  rejects both the all-harmonic pseudo-distribution and the new integral
  triple pseudo-incidence by strictly positive rational margins.  Separate
  reoptimization and an explicit \(D=0\) spectral family show that this is a
  witness separator, not an upper bound.
- Derived the sharp centered fourth-moment interval, Hankel determinant
  constraints, the centered sixth-moment Newton identity, and the exact
  partition of \(\operatorname{tr}(G^4)\).  The only term not fixed by
  pair/triple measures is the all-distinct four-cycle moment; controlling it
  jointly is now the precise rank-aware bottleneck.
- Certified a new degree-11 \(S^3\) polynomial proving
  \(A(4,7123/12877)\le30\).  Closed-slab projection gives the strict frame
  floor
  \[
  \sum_i x_ix_i^{\mathsf T}\succ(15059/40000)I_5,
  \]
  improving \(9/25\).  An adversarial sign check found and repaired the
  necessary numerator sign split before dividing by the projected
  denominator lower bound.
- Constructed an exact integral triple pseudo-incidence that passes triangle
  PSD, all total-degree-two BV blocks, the deep-wedge/Pfender patterns, and
  the first residual-vector square.  It fails an explicit degree-three radial
  square and the rank-five spectral cut.  This prevents silently treating the
  first residual separator as universal.
- Ten new targeted exact tests pass using only rational standard-library
  arithmetic.

## 22:49:43 — Sparse deep graphs and stronger exact barriers

- Classified the low-edge deep-pair graph exactly.  At 23 edges it must be
  \(C_5\sqcup18K_2\); at 24 edges exactly three component types remain.
  The proof uses only triangle-freeness, \(\alpha\le20\), component
  cyclomatic bookkeeping, and exact small-component bounds.
- Proved incident antipodal-deviation and odd-cycle frustration inequalities,
  the exact two-edge weight bound \(49/64\), and residual-PSD row envelopes.
  In the 23-edge case these bound the shallow projective-energy deficit but do
  not contradict harmonic positivity.
- Built an exact rank-20 countermodel from 18 orthogonal antipodal pairs and
  an orthogonal regular pentagon.  Its deep graph is the sharp 23-edge graph
  and \(h[G]-J/28\succeq0\), so it passes every subset projective-kernel
  inequality.  This certifies that the next graph step must use rank-five
  cross-component compatibility.
- Built an independent exact 41-row split-feature counterexample satisfying
  the quadratic-kernel sign and rank, the separate rank-5/rank-14 PSD factors,
  their exact traces, and both Ky Fan bounds.  It violates the nonlinear
  common-source entry range.  The \(D_5\) covering lemma proves that no choice
  of the new quadratic feature repairs that range while leaving the old
  \(D_5\) factors fixed.
- Found an exact integral fixed-41 triple pseudo-measure on the five-node
  local-hybrid support that passes all fixed-cardinality BV blocks through
  total degree three and every certified wedge event-cell capacity.  Its
  first pure BV failure is a displayed degree-four direction; independently,
  the sharp rank-five spectral inequality rejects it by an exact positive
  rational residual.
- Seven targeted tests for the three new certificate families pass.

## Split-kernel interval barrier and effective root stability

- Strengthened the split-feature counterexample to an exact cyclic
  Fourier construction of order 41 satisfying the full genuine
  off-diagonal interval for \(R\) and \(K\), with a strict \(1/2000\)
  buffer.  Directed rational Machin/arctangent and cosine enclosures verify
  every entry.  The construction still fails the kissing entry bound on its
  linear factor and the nonlinear common-source Hadamard-square identity.
- Audited the sparse-graph zero-slack proof and made its closed core-code
  hypothesis explicit.  Determinant rounding with
  \(\eta=1/7200\), rank-five minor vanishing, ADE classification, and a
  collision-transfer frame estimate prove the effective projective-energy
  gap
  \[
  A+B<-\frac1{1658880000}.
  \]
  A robust near-antipodal inequality is also exact, although this deliberately
  crude constant is far too small to close the current global margin.
- Proved elementarily that \(D_5\) with any two root lines removed is
  projectively saturated: every compatible extension line is one of the two
  deleted lines.  This excludes an induced five-cycle of extensions when the
  18-line base has exactly zero projective penalty.
- Found a degree-three BV/wedge pseudo-measure that also satisfies the sharp
  rank-five cubic inequality with strict exact slack.  Adversarial graph
  realizability exposed an independent failure: its five color-degree
  covariance matrix has variance \(-570/41\) in direction
  \((2,-1,0,0,1)\).  The next search now includes this full covariance block
  rather than silently treating aggregate triple counts as one colored
  graph.
- Tested the most direct attempt to combine the all-degree three-point
  witness with the rank-feasible degree-three witness.  At mixture weight
  \(3/5\), the rank inequality is repaired exactly, but a degree-six radial
  interpolation polynomial annihilates the old support and gives a negative
  \(H_{3,9}\) form on every positive mixture.  This is an exact scoped
  separator, not evidence that an independently reoptimized union support is
  infeasible.
- Reoptimized after adding the full color-degree covariance block.  A new
  exact integer triple pseudo-measure passes it, every degree-three BV block,
  C047, all wedge capacities, exact graphical degree sequences for each
  color, and nonnegative same-color three-vertex motif counts.  Its next
  exact failure is the scalar degree-four block \(H_{4,4}<0\); simultaneous
  five-color realization is still not asserted.

## 17:43:23 PDT — Exact one-sided bound 35 and degree-11 numerical lead

- A fixed rational degree-10 cap-SDP certificate now proves exactly
  \[
  B(5)\le35.
  \]
  Its off-diagonal bound is \(F\le-19/20\), its diagonal bound is
  \(F(u,u,1)\le33\), and its exact objective is
  \(679/19<36\).  The standard-library verifier rebuilt 2,483 exact
  Bernstein leaves (1,090 determinant-infeasible and 1,393 proved), and
  all three tests passed, including independent factor-payload and tree
  manifest tampering.  This implies that every hypothetical 41-code has at
  least six points in each open side of every origin hyperplane, Tukey depth
  at least six, and deletion-five robust interior containment of the origin.
- A separate degree-11 run is **NUMERICAL EVIDENCE ONLY**.  It was generated
  deterministically with Python 3.14.6, CVXPY 1.9.2, NumPy 2.5.1, and
  Clarabel 0.11.1 by
  ```sh
  .venv/bin/python experiments/search_one_sided_cap_sdp.py \
    --degree 11 --grid 10 --random 5000 --audit 200000 \
    --seed 511041 --solver CLARABEL --tolerance 1e-5 \
    --psd-floor 1e-4 --full-boundary-grid 31 \
    --output /tmp/capd11_target_b34.npz
  ```
  Clarabel returned `optimal_inaccurate`; the sampled objective was
  `33.925068534521`.  The subsequent 200,000-sample audit, which also checks
  both determinant sheets per random height pair, found off-diagonal maximum
  `-0.969696042612` at
  `(0.08512185065245303, 0.09540487485825522,
  -0.8858141033147785)`, dense diagonal maximum `32.929914391132` at
  height `0.93900000`, and rescaled objective `34.959006682570`.
  The generated NPZ has SHA-256
  `cf68777ed2e1310afe155a8086227e077f4b4371f9e948138e463c68b9f4eb72`.
  It is not a certificate, is not used by the \(B(5)\le35\) proof, and does
  not establish \(B(5)\le34\).  A later exact-certification attempt should
  start with rational targets \(F\le-969/1000\) and
  \(F(u,u,1)\le1647/50\), whose formal objective would be
  \(11303/323<35\).

## 18:48:12 PDT — Exact degree-11 one-sided bound \(B(5)\le34\)

- The first degree-11 rationalization passed the target diagonal inequality
  but failed the off-diagonal target at the explicit exact feasible point
  \[
  (u,v,t)=\left(4791/65536,5/64,-113/128\right),
  \]
  where exact evaluation gives \(F>-969/1000\).  Numerical localization
  exposed a missed interior ridge on the symmetry plane \(u=v\), rather than
  a Bernstein-enclosure artifact.
- A distinct adjusted search added a dense interior mesh on that symmetry
  plane.  The successful scalar-formulation command was
  ```sh
  .venv/bin/python experiments/search_one_sided_cap_sdp_adjusted_d11.py \
    --seed 511041 --random 5000 --grid 10 --full-boundary-grid 31 \
    --symmetric-u-grid 151 --symmetric-alpha-grid 21 \
    --diagonal-grid 201 --audit 100000 \
    --audit-symmetric-u-grid 501 --audit-symmetric-alpha-grid 301 \
    --tolerance 1e-5 --psd-floor 0.0001 --formulation scalar \
    --output /tmp/capd11_adjusted_symmetry.npz
  ```
  The source NPZ has SHA-256
  `e0a2fb82f03a7189d89ed5daccc4b9c5898e36e1b08adaec01ff1ad3f149a156`.
  Its solver status and all sampled diagnostics remain discovery metadata
  only.
- After applying the recorded discovery scale \(997/1000\), the matrices
  were converted to denominator-\(10^9\) rational Gram factors.  The final
  exact certificate proves
  \[
  F(u,u,1)\le1647/50,\qquad F(u,v,t)\le-969/1000
  \]
  on the complete closed domain.  The diagonal audit uses 3 leaves.  The
  full audit uses 5,995 leaves (2,848 determinant-infeasible and 3,147
  proved), maximum depth 31, and leaf digest
  `3ffd08afa66bcd12e52399e392c09fda237f8bab18fc1af9a8090e76f1f81f65`.
  The Gram-factor digest is
  `723d5521951ce45d236116016a69e7e8e510b8e7ba1f0338f7c1d6fffe507257`.
- Ordered-pair summation gives the exact objective
  \(11303/323<35\), hence the newly certified theorem
  \[
  B(5)\le34.
  \]
  A frozen-file verifier rerun passed, and all 3 unit tests passed, including
  factor and tree-manifest tampering.  For a hypothetical 41-code this
  yields origin Tukey depth at least 7, deletion-6 robust interior convex
  containment, strictly negative vertex degree at least 7, and strictly
  positive neighbor degree at least 6.

## 18:08 PDT — Exact audit of the degree-11 lead

- Exact evaluation refuted the first degree-11 candidate's proposed
  off-diagonal target.  The dyadic point
  \[
  (u,v,t)=\left(\frac{4791}{65536},\frac5{64},-\frac{113}{128}\right)
  \]
  satisfies the full \(3\times3\) Gram determinant condition, but gives
  \(F>-969/1000\).
- A denser search found a still worse symmetric ridge near
  \(u=v=0.0914976845,\ t=-0.877290785\), where
  \(F\approx-0.962913072\).  Thus the original random and boundary audit
  missed an interior feature, and the sampled objective must not be cited as
  evidence for \(B(5)\le34\).
- Two enlarged batched Clarabel attempts failed before producing a candidate.
  A third scalar formulation with 11,990 off-diagonal constraints, including
  a dense symmetry-plane mesh, remains a discovery calculation.  The stable
  exact degree-10 certificate is independent and unchanged.

## 18:09 PDT — Higher-harmonic rank cuts and fixed-support closure

- Proved for every real symmetric rank-\(r\) matrix the sharp centered-skew
  inequality
  \[
  r(r-1)D^2\le(r-2)^2V^3.
  \]
  Padding by zero eigenvalues covers rank deficiency, and the compact
  Lagrange-multiplier argument covers \(V=0\), \(r=2\), and repeated-root
  cases explicitly.
- Applied the theorem to arbitrary signed combinations of spherical harmonic
  Gram matrices.  Exact \(H_2\) and \((H_0+5H_1)/6\) instances reject the
  strongest degree-four five-node pseudodistribution by displayed negative
  rational residuals.
- Added an exact \(19\times19\) rational dual proving that no 41-code can have
  the historical five-node support and ordered pair counts
  \((170,6,262,652,550)\).  The dual uses only necessary outer rank bands,
  degree-five BV forms, colored-degree covariance, and previously proved
  threshold/clique cuts.  Its exact lower bound for \(-z\) is positive
  (approximately \(2.87505769098\cdot10^{-4}\)).
- An independent adversarial audit rederived the eigenvalue extremum,
  harmonic ranks and trace normalizations, checked all outer-band directions
  and LP-dual signs, tested determinant-zero support boundaries, and found no
  proof defect.  Exact \(D_5\) sanity and certificate-tampering tests were
  added.
- The theorem is deliberately scoped: it eliminates one fixed real support
  and pair-multiplicity vector.  There is no reduction of an arbitrary
  continuous 41-code to those data.

## 18:10 PDT — Barrier probes and construction rounds 4--5

- The all-harmonic mass-41 witness passes every nontrivial quadratic
  low-harmonic frame-potential inequality obtained from subsets of degrees
  \(0,1,2,3\) whose total feature rank is below 41.  This exact barrier shows
  that quadratic trace/rank information alone is too weak; the cubic
  centered-skew constraints carry genuinely new information.
- Added exact sparse-graph probes: a five-vector hypercube \(C_5\) has edge
  inner product \(-3/5\), chord inner product \(1/5\), cross magnitudes
  \(0\) or \(\sqrt{2/5}\) against all \(D_5\) root lines, and projective
  \(h\)-energy \(39/250\).  A planar family attains \(3/2\), but the proposed
  universal \(C_5\) energy upper bound remains numerical only; local triples
  permit \(49/32\), and the tested degree-four SOS ansatz failed.
- Construction round 4 completed 36 asymmetric deletion/reinsertion,
  contact-stress basin-hopping, release, and SQP runs for \(N=41,\ldots,44\).
  Construction round 5 completed 30 unrestricted population,
  inverse-chord-energy, aligned-crossover, splice, mutation, and SQP runs.
  Neither found a feasible code.  The best round-5 maxima were approximately
  \(0.51499465,0.51824116,0.52472448,0.52747119\).
- The best 41-point near miss has a numerically enumerated shallowest
  hyperplane split \(11/19/11\), so it does not challenge the exact
  depth-six necessary condition.  These are reproducible search outcomes,
  not nonexistence evidence.
- Checkpoint completion estimate: **18%** toward a complete exact resolution.
  This reflects audited exact infrastructure and stronger necessary
  conditions, not proximity to a theorem; the universal continuous-support
  gap remains the dominant uncertainty.

## 18:50 PDT — Independent \(B(5)\le34\) audit and four-point cuts

- A separately implemented degree-11 audit imported neither cap verifier.
  It rebuilt the 650-term rational polynomial, all Gram factors, the
  three-leaf diagonal proof, and the full 5,995-leaf tree with the same
  digest.  It also checked the former exact failure point, a dense exact
  symmetry-ridge grid, pole/contact/determinant boundaries, and exact
  \(D_5\) cap instances.  All three independent tests passed; no
  mathematical objection was found.
- Proved the universal common-neighbor lemma: a pair with inner product at
  most \(-11/25\) has at most one common neighbor whose two incident products
  are at least \(499/1000\).  Two such points would have inner product at
  least \(109001/140000>1/2\).
- The strongest degree-four five-node witness requires 243 low/high/high
  triangles but has only 219 eligible low base edges.  A separate exact
  enumeration found 3,213 labeled Gram-PSD colored-\(K_4\) patterns in 198
  orbits and reduced the same failure to the one-row covariance value
  \(-27216/131\).  This is an exact fixed-support obstruction, not a global
  reduction.
- Anchoring the exact cap kernel on every negative code-point neighborhood
  gives another universal pair/triple inequality.  Exact evaluation showed
  that both the degree-four and all-harmonic witnesses pass every natural
  threshold and eligible support-subset variant with strict slack.  This
  records a valid but blocked route.
- Construction round 6 completed 26 Riemannian active-bundle/facet-escape
  trajectories for \(N=41,\ldots,44\).  It reproduced the previous best
  maxima and found no feasible code.  The best 41-point basin still has a
  35-point active core and six rattlers; all \(\binom{41}{4}\) numerical
  hyperplane supports give minimum strict-side occupancy 11.
- The depth-seven theorem forces two disjoint inclusion-minimal positive
  circuits in any hypothetical 41-code.  Exact \(D_5\) subconfigurations
  already realize all 15 possible unordered circuit-size pairs
  \(2\le m\le n\le6\), and a 41-point \(D_6\) subset satisfies the same
  entry and circuit conditions at rank six.  Thus the circuit inventory and
  two positive kernel vectors alone cannot replace global rank-five
  compatibility.
- Checkpoint completion estimate: **21%** toward a complete exact resolution.
  The cap and local four-point advances are fully audited, but the decisive
  continuous-support rank-five exclusion or a 41-point construction remains
  absent.

## 19:12 PDT — Exact-suite checkpoint and construction round 7

- The complete exact regression suite passed 90/90 tests in 698.121 seconds.
  This includes both independent implementations of the degree-11 cap
  certificate, all boundary/tamper tests, and every previously released exact
  verifier.
- The round-6 portfolio checker independently recomputed the four stored
  maxima and the complete 41-point halfspace-depth histogram.
- Construction round 7 completed 20 \(S^5\)-to-\(S^4\) covariance-collapse
  paths.  Four exact \(D_6\) subsets and one independently relaxed numerical
  \(S^5\) code were used for each \(N=41,\ldots,44\).  Every path first
  crossed above \(1/2\) at collapse weight \(3\), although the final sixth
  covariance fraction was at most \(1.42\cdot10^{-12}\).  Best final maxima
  were \(0.5207137808832133,0.5254288787731305,
  0.5284653908755059,0.5331536680973610\).  An independent checker and four
  tests passed.  This falsifies only this construction attempt.
- A proposed axisymmetric row-energy SDP was reduced before a large solve to
  the exact C039/C067 barrier check: pure fixed-cardinality three-point BV
  kernels plus the \(H_1\) frame inequality are already satisfied by the
  all-harmonic pseudo-measure.  Any viable row-energy continuation must add
  genuinely new rank or four-point information.
- Checkpoint completion estimate remains **21%**.
