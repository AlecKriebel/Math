# Research log

All timestamps are in America/Los_Angeles.

## 2026-07-23

- 23:30 — Opened this dedicated folder for a construction round.  Kept
  `../construction_active_search/` frozen and treated its \(N=41,\ldots,44\)
  numerical records as read-only inputs.
- 23:30–23:40 — Developed three independent mechanisms in parallel:
  cardinality homotopy from four exact 40-codes, direct rank-five
  factor/slack active deflation, and history-biased population annealing.
- 23:40 — The first combined thermal production process was terminated by
  machine memory pressure during dense polishing.  Replaced that stage by
  single-threaded, all-pair constraint-generation epigraph solves and ran
  cardinalities separately.  Every reported objective continued to be
  decided by a literal scan of all pairs.
- 23:46 — Completed and consolidated eight deterministic thermal runs.  All
  inherited near-maximum graph overlaps reached zero before quenching; none
  improved a record.  The independent standard-library verifier and seven
  tests passed.
- 23:52 — Completed 32 split-homotopy paths and 16 selected endpoint
  polishes.  No endpoint improved a record.  All 16 polished
  source/cardinality endpoints were distinct from the 438 stored arrays
  scanned in the repository inventory.  This is an inventory statement, not
  a global novelty claim.  The independent verifier and six tests passed.
- 23:56 — Completed 40 factor/slack runs: 400,000 epochs, 120
  deflation/re-entry events, and 80 successful all-edge polish solves.  No
  run improved a record.  The independent standard-library verifier and
  seven tests passed.
- 23:59 — Re-ran the three audit suites independently, checked the
  \(N=41,\ldots,44\) maxima by full coordinate scans, confirmed branch
  `main`, and wrote the round-level synthesis.  Final classification:
  **NUMERICAL EVIDENCE ONLY**.  No construction with maximum inner product
  at most \(1/2\) was found, and no nonexistence conclusion is licensed.
