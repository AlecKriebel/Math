# External Numerical Inputs

Files in this directory are discovery inputs, never exact certificates.  Each
import must have a source URL, retrieval timestamp, and SHA-256 recorded in the
research log or an adjacent manifest.
