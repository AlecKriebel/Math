# Mutation-sensitive convention tests

The independent verifier rejects all eight required semantic mutations:

1. use exhaustive cleanup to admit a root-created parallel preimage;
2. drop the LSA condition;
3. confuse one tree-child rooted presentation with strong semi-directed
   tree-childness;
4. infer degree two from two boundary vertices in the literal 2-sub-blob
   definition;
5. admit the simple double-triangle `K4-e` core as strongly tree-child;
6. make broad cleanup alter one of the twelve primitive supports;
7. make broad cleanup alter the displayed weak-Theta presentation; and
8. admit the parallel `(1,1,2)` theta as a simple mixed graph.

The mutation names and zero-survivor result are serialized in
`convention_certificate.json`.
