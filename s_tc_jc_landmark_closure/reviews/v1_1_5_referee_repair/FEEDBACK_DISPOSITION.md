# Referee-repair disposition

Status: **VERIFIED AFTER CORRECTION; no theorem weakening**

The August 20 referee found one material certificate-exhaustiveness defect,
four proof-text gaps, and three auxiliary reproducibility/documentation
defects.  Each criticism was valid.  None produced a counterexample or
required narrowing Theorem 1.1.

| Referee item | Disposition |
|---|---|
| Arbitrary cut words omitted by the five-word compiler palette | Replaced the false direct-exhaustion claim by a proof: a word with three runs has an immediate path-order obstruction; every other word restricts to the direct or singleton-doubled palette.  A standalone exhaustive reduction and a separately implemented graph/switching replay are authenticated. |
| Central-arm normalization | The complete central singleton-signature class is identified and normalized before evaluating $\Delta$ and $\Gamma$.  The corrected counts are 67 strict-$\Delta$, 2 strict-$\Gamma$, 7 nonordinary weak-$\Gamma$, plus the ordinary endpoint. |
| Primitive-core and noncut-compression scope | Both statements now explicitly concern factors induced by $S_{\mathrm{TC}}$ networks. |
| Smooth branch in localization | The proof now selects a rank-$d_N$ source preimage and one smooth source-image branch before taking product coordinates. |
| Contextual triangle gluing | The representative slice is correctly one-dimensional after quotienting the three locally free incidence directions; one projective coordinate plus three effective boundary scales retain all four triangle-tensor directions.  Physical sections give the lower rank bound, while a direct three-leg normalization map gives the matching upper bound.  The proof does not invoke bridge-cut extraction when two triangle terminals reconnect inside one theta. |
| Raw-mask submersion diagnostic | Replaced by zero-sum split/complement normalization, deletion of invisible rows, an actual rational Jacobian, exact elimination, and a recorded nonzero minor for all 42,908 completions. |
| Tautological probe collision statistic | Removed.  The certificate now records 372 expected one-port ambiguities and relies on the load-bearing two-port parent-transport gates. |
| Four-port canonicalization levels | Documentation now separates the 192 normalized survivor presentations from their 3 direct and 57 nonretaining quotient classes. |
| Row-level closure provenance | Three content-addressed closure streams now bind every theorem row through all restoration states to compact paths and through every direct residual anchor to all one-/two-port relations, graphs, transports, witnesses, and polynomials.  Twenty-four in-bundle mutations attack these links and the sealing commitments; a separate seven-attack regression checks stage comparison, excludes ignored working-tree injection, excludes `PYTHONPATH/sitecustomize` injection, and rejects nonisolated outer invocation. |
| Regeneration freshness and source provenance | The regeneration driver deletes every declared output before invoking producers.  The builder refuses a dirty tracked tree, exports the exact recorded Git object, runs the builder contained in that detached export under isolated Python with startup-path variables removed, rejects any path/byte/executable-bit difference from the requested stage, archives only the detached reconstruction, and records both the 40-hex source commit and a recomputable payload commitment.  Ignored, untracked, and Python-startup-injected working-tree files cannot enter the archive.  Active sharpness inputs and replays are isolated under `sharpness/`, not imported from development audit/history trees. |

The Zenodo DOI remains intentionally pending.  It must be inserted only by
the human depositor after reserving the DOI for the final sealed archive.
