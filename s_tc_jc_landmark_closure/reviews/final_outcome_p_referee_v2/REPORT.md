# Final independent adversarial referee report: Outcome P

Reviewed mathematical/release commit:
`e1fd6ede986cd866a310cd9b0f9e7d6d13c8318c`

Observed checkout at certificate time:
`2ea6324816fc518e58061017efb4e990c20fc1ce`, a descendant differing from the
reviewed state only by the project research log.  Dirty historical files were
excluded from every theorem input.

Review date: 2026-08-13

## Terminal verdict

**VERIFIED**

No load-bearing mathematical gap remains in Outcome P.  The candidate proves,
under the locked standard semidirected convention and open JC parameter cube,
that one-sided full-dimensional regular containment between binary strongly
tree-child level-2 topologies occurs exactly when their labelled reduced bridge
trees agree and every corresponding blob is labelled-isomorphic or differs by
ordinary triangle redirection `T`.  Consequently there are no proper generic
one-sided containments, and the symmetric observational equivalence is exactly
the quotient by `T`.

The frozen weak-but-not-strong family supplies the sharp converse: for every
`n >= 4`, nonisomorphic and non-`T`-equivalent weakly tree-child level-2
topologies have a common full-dimensional regular JC germ.  Thus strong
tree-childness is the sharp boundary proved by the manuscript.

Outcome P is submission-ready as mathematics.  The remaining defects listed
below are mechanical release packaging, not proof obligations.

## First exact blocker

None.

The previous final review had two first-order blockers.  Both are now closed by
independent graph generation and exact algebra, rather than by an enlarged
search or by accepting a frozen `PASS`:

1. The three-outgoing decorated relation universe is independently generated
   from primitive supports, target completions, incoming-boundary modes and
   relative assignments before the primary claim is opened.  The independent
   and primary raw multisets both contain `10,826` relations and have SHA-256
   `73835cd5212046fb3a41f7fd36259bd3165a184a9cf36321b0ba0d13d794c3b5`.
   Their merged multisets both contain `10,466` relations and have SHA-256
   `389d574c6fc238c7cd89360e8ef3766a1a9121e53e9d1dfab394af0c9c7bf0dd`.
   Six universe/transport mutations are rejected.
2. The `62` direct residual anchors are no longer inferred from the `144`
   restoration-terminal families.  Their own graph-derived closure contains
   `2,642` one-port and `18,224` two-port directed relations.  Exactly `314`
   and `2,032`, respectively, survive as labelled isomorphism or ordinary
   `T`; all remaining `18,520` relations have exact generic-polynomial or
   strict open-cube separators.  All twelve binding, direction, transport,
   deletion and witness-assignment mutations are rejected.

## Theorem-by-theorem verdicts

| Claim | Verdict | Adversarial conclusion |
|---|---|---|
| Locked `S_TC` class and no-omnian condition | **VERIFIED** | The active definition uses the reticulation-preserving simple `sd_0` convention.  The exact mixed-graph criterion is that every tail of a retained reticulation edge is incident with two undirected edges.  It excludes both a tree vertex with two reticulation children and a reticulation with a reticulation child. |
| Automatic at-most-one-triangle statement | **VERIFIED** | The structural cycle/theta reduction leaves the two putative multi-triangle theta length patterns; one is nonsimple and the other admits no tree-child rooting.  No extra one-triangle hypothesis is used in the final theorem. |
| Pointwise cut characterization | **VERIFIED** | A true cut has the required low Fourier-flattening rank throughout the open cube; every wrong split, including the two-active-endpoint crossing, has an exact nonzero/strict witness. |
| Both cut inclusions under `preceq_JC` | **VERIFIED** | Pointwise rank separation prevents a source-open model from lying in a target wrong-cut exceptional locus.  Applying the argument in both source/target directions gives the same labelled bridge tree. |
| Full bridge incidence kernel | **VERIFIED** | The fiber is exactly the full incidence-scaling action.  The reciprocal-only chart and physical bridge-multiplier recovery remain withdrawn. |
| Projective peeling and no compensation | **VERIFIED** | Positive rank-one cut blocks give local projective slices; the reduced bridge graph is a tree, so no gauge circulates.  Intrinsic extraction localizes source-relative containment blobwise without continuously selecting target parameters. |
| Primitive core and support exhaustion | **VERIFIED** | The cyclomatic/degree argument gives the cycle and four theta cores.  Strong repairs, incoming roles, rigid supports and subdivision words are structurally exhaustive under the locked convention. |
| Complete `n3` decorated directed relation universe | **VERIFIED** | The new independent generator closes the first prior blocker and exactly matches raw and merged normalized relation multisets.  Recordwise graph-to-switching-to-mask-to-Fourier-to-witness binding remains independently checked. |
| Five-port theta-2 gate | **VERIFIED** | Independent generation yields three equal-signature pairs and exactly `18+42+132` presentations: direct isomorphisms, selected-incoming presentation duplicates with transports, and marginalized-incoming restoration roots. |
| Restoration-terminal arbitrary subdivisions | **VERIFIED** | The tracked compact gate covers `144` n3 and `132` theta-2 paths, `269,730` exact relations, all four terminal classes, and a maximum of ten ports.  Nine destructive relation/transport/coherence mutations are rejected. |
| Direct-anchor arbitrary subdivisions | **VERIFIED** | The separate `62`-anchor compilation closes the second prior blocker; no non-`T` survivor or one-sided containment remains. |
| Marginal submersion and probe coherence | **VERIFIED** | Positive path-product differentials have full row rank on the open cube.  Fixed anchor transports, one-port segment placement, two-port order and one coherent `T` choice reconstruct every subdivision word. |
| Root reduction | **VERIFIED** | Every root factor reduces to a real leaf-bearing incoming boundary.  Source and target boundaries may be chosen independently; reversibility and open-cube edge splitting preserve the projective model germ. |
| Ordinary `T` local germ | **VERIFIED** | The normalized three-sunlet tensors have an exact common strict rational point and rank four.  Analytic local sections plus the chain rule preserve a common full-rank germ after embedding in an unchanged context.  Equality of complete open stochastic images is not claimed. |
| Local-to-global one-sided iff theorem | **VERIFIED** | Cut recovery, projective localization, complete directed local closure and finite-union dimension exclude nonlocal compensation and every proper generic containment.  Conversely, local `T` germs glue simultaneously through effective bridge intervals. |
| Exceptional algebraic locus | **VERIFIED** | It is a finite union of closures of rank-drop images and zero sets of observable cut/atlas factors that are nonzero on the intended stratum.  No unidentified physical parameter is projected into distribution space. |
| Structural reconstruction | **VERIFIED** | The algorithm terminates after finitely many split tests and bounded probes, returns the canonical semidirected topology modulo `T`, and explicitly does not claim physical bridge recovery. |
| Frozen weak sharpness theorem | **VERIFIED** | The four-leaf equality, strict quadratic point, both rank-eight minors, topology census, and all-`n` leaf-substitution inverse remain independent and unchanged.  Theta is not used as an `S_TC` move. |
| Manuscript mathematical scope | **VERIFIED** | The active paper states one-sided and symmetric relations precisely, separates Zariski closure from stochastic overlap, disclaims complete-image equality and bridge-parameter recovery, and uses the corrected class, root, `T`, exceptional-locus and sharpness language. |

## Exact replay and regression record

- `bash reviews/n3_universe_generator/verify.sh`: **VERIFIED**; exact fresh
  universe regeneration gave `10,826` raw and `10,466` merged relations with
  the two matching multiset hashes above.
- `bash reviews/direct_anchor_probe_closure/verify.sh`: **VERIFIED**; exact
  graph/algebra replay gave `62`, `2,642`, `18,224`, and `18,520` as above,
  with `12/12` mutations rejected.
- `python3 reviews/final_outcome_p_referee_v2/verify_review_certificate.py`:
  **VERIFIED**; it checks both repaired blockers, the compact tracked-input
  lock, manuscript scope and all mutation summaries.
- The committed compact clean-clone gate reports `50` byte-locked tracked
  inputs, `276` terminal paths, `269,730` relations and `9/9` rejected
  mutations.  No active release script consumes an untracked verbose
  `probe_extension_*` stream.

## Defects preserved

These defects were observed and are not hidden by the positive verdict:

1. `FINAL_OUTCOME.json` is absent at the reviewed commit.
2. `RELEASE_METADATA.json` is absent at the reviewed commit.
3. The active release verifier is deliberately fail-closed until this v2
   report exists and the final metadata is generated.
4. Invoking the archive-clean compact gate with system `python3` failed at
   `import networkx`; the declared project virtual environment is required.
   This is a dependency/bootstrap defect, not a mathematical failure.  The
   committed compact package has a successful fresh-archive replay using the
   project environment.
5. A venv archive replay started during this final pass was terminated at the
   user's hard stop rather than extended.  Its load-bearing tracked-input and
   semantic certificates were already independently verified; no theorem
   claim depends on that interrupted duplicate run.
6. Historical untracked `primary/certificates/probe_extension_*` files remain
   in the working checkout.  They were not read by this review and are not
   consumed by any active release script.
7. The checkout advanced from the requested commit to descendant
   `2ea63248...` only to record project research-log updates.  The verdict and
   hashes above are for the requested mathematical/release state.

The first three items must be completed in mechanical release packaging before
an archive is labelled final.  None changes the theorem or requires a new
mathematical audit.

## Submission decision

**VERIFIED.**  No load-bearing gap remains.  Outcome P is mathematically
submission-ready, subject only to generating the final release metadata,
building the stable PDF/archive, and running the already-defined clean release
scripts in the declared project environment.
