# Independent bounded directed JC relation review

Date: 2026-08-09

Scope: bounded directed local relations only.  This review makes no claim
about arbitrary port words, bridge localization, the global theorem, or the
weak-class sharpness theorem.

## Verdict

**UNRESOLVED.**  The finite directed algebraic hard cover is not closed.  No
non-`T` standard-strong collision has been found in the completed clean-room
records, but the available records do not certify the quantified atlas.

The review must not be cited as proving the local or global positive theorem.

## Corrected universe findings

| Claim | Status | Exact conclusion |
|---|---|---|
| Fixing `INCOMING -> INCOMING` is exhaustive | **FALSE** | Rooted incoming status is provenance, not a colour of a standard semi-directed relation.  Complete boundary bijections form the full group `S_p`. |
| Full target `S_p` relative action is necessary | **VERIFIED** | The preserved four-boundary witness has relative role permutation `[2,3,0,1]`, outside the incoming-fixed subgroup. |
| Requiring the target structural incoming boundary to be selected is exhaustive | **FALSE** | A selected support can marginalize the target rooted incoming boundary. |
| Zero-character structural incoming dummy is necessary | **VERIFIED** | The target completion grammar must contain `incoming_selected` and `incoming_dummy`; the dummy is not an observed tensor boundary. |
| Completion graph equals selected topology graph | **FALSE** | Dummy leaves certify a full strong completion but are absent from the selected topology. |
| `selected_retains_strong_core` means intrinsic reduced `S_TC` | **FALSE** | It records retention of the original primitive core only. |

The first two failures are preserved in
`PRESERVED_FIXED_INCOMING_FAILURE.md` and
`PRESERVED_DUMMY_INCOMING_FAILURE.md`, with machine-readable witnesses under
`counterexamples/`.

## Preserved exact computation

The clean-room implementation imports no producer graph canonicalizer,
switching engine, descendant-mask code, relation enumerator, invariant
evaluator, sign code, or separator selector.  It independently constructs
rooted completions, standard mixed graphs, displayed switchings, descendant
masks, exact JC tensors, the 84-element port-permuted invariant deck, and exact
pullbacks.

For four observed boundaries (`outgoing=3`), the retained descriptor screen
is **EXACTLY COMPUTED** as follows:

- 5 source standard topology records and 5 source signatures;
- 1,953 target structural bases in both incoming modes;
- 232 structural descriptor groups and 4,608 full-boundary descriptor-orbit
  records;
- 40 candidate target signatures and 54 necessary directed signature pairs;
- 102,426 exact zero-pullback checks;
- screen body hash
  `d2fef86e118c01732bda1772bbe174f4dbe3a29302d4d3ab5e6885f2d3bc264a`.

The provisional equal-deck binding emitted 1,014 canonical relation records:

- 5 labelled isomorphisms;
- 4 ordinary-`T` relations;
- 0 retained non-`T` equal-deck records;
- 1,005 non-core-retaining records pending support completion, comprising
  818 `incoming_dummy` and 187 `incoming_selected` records.

Only 128 of these 1,014 records have a fixed-incoming representative; 886 do
not.  Of the four ordinary-`T` records, two have a fixed-incoming
representative and two require the corrected full-boundary action.  Thus the
fixed-incoming omission changes the actual equality atlas.

These are exact outputs, not a completed theorem.  In particular:

1. `cleanroom_census.json` predates the final unique TR-family naming and is a
   stale diagnostic; its per-template subtotals do not bind the later n=3
   relation stream.
2. `screen_n3.json` fields named `full_boundary_only_candidate_pairs` and
   `fixed_incoming_candidate_pairs` are overlapping occurrence sets, so the
   word `only` is inaccurate.  The relation-level fixed/nonfixed counts above
   are the usable figures.
3. The equal-deck records still require the completed graph-derived support
   cover before any pending equality can be discarded.

## Partial sign work

Status: **UNRESOLVED**.

The resumable n=3 work cache contains 25 exact graph-derived sign attempts:
7 are certified and 18 have unresolved mixed-sign factors.  No
`strict_descriptor_audit_n3.json` was completed, and these 7 certificates do
not cover the relation universe.  They are preserved only as restartable
work, not as theorem evidence.

The first monolithic sign run exited with status 143 after about eight
minutes.  Two later reviewer-owned restarts were also externally terminated,
including a one-certificate unit after 5.7 seconds.  The reviewer sent no
signal.  See `PRESERVED_PROCESS_TERMINATION_FAILURE.md`.

## Load-bearing unresolved work

The following are **UNRESOLVED** in this review:

1. corrected clean regeneration after the final source changes;
2. complete n=4, n=5, and n=6 directed screens and exact relation bindings;
3. exact generic source and target Jacobian ranks, including upper bounds;
4. graph-derived strict or algebraic witnesses for every unequal direction;
5. restoration of every pending n=3 equality through the complete
   cycle-source/theta-target union, including marginalized incoming roles;
6. the hard-cover map from every pending relation to a completed strict or
   `T` relation;
7. source-anchored/target-permuted normalized comparison with a corrected
   producer stream;
8. the mandatory mutation suite;
9. deterministic clean-clone regeneration.

The current producer union file is empty/incomplete and was not used.

## Adversarial conclusion

No exact standard-strong, non-`T` JC counterexample was produced here.  Nor
was the positive local atlas proved.  The first theorem-level stopping point
is the missing corrected finite hard cover after the full-`S_p` and dummy-
incoming repairs.  The only defensible release status is therefore:

> **UNRESOLVED — corrected universe established; four-boundary candidate
> screen partially audited; finite directed algebraic hard cover not closed.**
