# Root/cut gate independent audit log

## 2026-08-09T18:21:34-07:00 -- audit opened

- Scope locked to Gate 1 root-collision closure and Gate 3 pointwise cut
  preservation.  Manuscript sources are read-only.
- The historical `PASS` and status fields are treated as inert strings.
- The clean-room implementation will import no project graph, Fourier,
  invariant, rank, or certificate-verification module.  Historical JSON and
  literal template data may be read as untrusted inputs.
- Existing unrelated untracked repair artifacts will not be edited or staged.
- First preserved issue: the Gate 1 class checker merges all parallel mixed
  edges before asking whether a parallel edge remains.  Its advertised
  no-parallel check is therefore vacuous.
- First standard-convention attack: 247 of the 1,493 network-bearing records
  serialized in the frozen Gate 1 certificate produce a parallel mixed edge
  immediately after the single root suppression of Englander et al.
  Definition 2.2.  Those records pass only after the audit's broader
  merge-and-resuppress normalization.
- Gate 3's frozen 359-type certificate omits the tensor encodings from its 359
  records.  The older adversarial cross-check explicitly inherits all 981
  one-active factor witnesses without reconstructing their factors.  A new
  tensor-hash join and exact factor replay is required.
- Initial completion estimate: **20%**.

## 2026-08-09T19:08:59-07:00 -- exact replays complete

- Gate 1 topology scan completed.  Of the 1,493 distinct serialized network
  representatives, 1,246 are literal-standard after one former-root
  suppression and 247 are not.  Every failure has both a parallel mixed edge
  and a local strong-tail violation; the broad reducer drops one reticulation
  in all 247 cases.  Full fixtures were retained before any other conclusion.
- The five serialized six-port common-signature pairs split into three valid
  nonisomorphic ordinary-`T` pairs and two pairs outside the literal standard
  class.  All 424 triangles in the broad serialized reductions are ordinary,
  but the historical implementation erases arbitrary triangle arrowheads and
  is not itself the definition of `T`.
- A second independent Gate 1 implementation reconstructed all 356 serialized
  separator pairs (`44+192+120`), all 50 bound tensor/invariant witnesses, and
  exact open-cube Bernstein signs.  The 50 witnesses have 48 coefficient
  arrays after parameter-name erasure.  The final JSON does not serialize the
  additional `408/1252/468` concrete target parameterizations, so those claims
  cannot be replayed from the certificate alone.
- The claimed arbitrary Gate 1 lift remains unresolved: its long-word routine
  manufactures comparison data from the very words it reconstructs, the
  nine-port JSON retains no nonmatching encodings, and the finite topology to
  arbitrary-word binding was carried out in the incompatible broad class.
- Gate 3 topology was regenerated from the five final core encodings under the
  literal convention.  The exact unions are 293 distinguished three-port
  endpoint types and 359 four-port one-active tensors.  The frozen hash sets
  match exactly.  Invalid root presentations (97 in the three-port search and
  133 in the four-port search) were preserved and excluded; every resulting
  tensor has a different literal-valid witness.
- All 293 endpoint sign records passed exact polynomial reconstruction:
  `261` have `abc-t^2>0`, while `32` have `abc-t^2=0` and `a-bc>0`.  The replay
  checked 917,916 factor-level Bernstein coefficients, including all 12 hard
  inheritance-Bernstein cases.
- All 981 crossing one-active minors were reconstructed and independently
  refactored.  Their 642 distinct irreducible factors have strict open-cube
  signs; no factor expression was inherited from the frozen atlas.
- The omitted two-active contraction reproduces the four physical
  `4 x 4` blocks, 20 nonzero minors up to sign, all four decisive minors, and
  all three stated polynomial identities.  The proof uses the physical
  `0<z<1` and never specializes `z=1`.
- An independent literal-standard run-compressed switching census checked ten
  root/nonroot cycle/theta families and found zero balanced noncut colourings
  displayed by every switching.  The accompanying restriction argument lifts
  the finite census to arbitrary port words.
- Eight destructive mutations (class, `T`, separator count/coverage, endpoint
  factor, hard Bernstein coefficient, tensor omission, and central-port
  ordering) were all rejected.
- Current completion estimate: **80%**.  Remaining work is report synthesis,
  reproducibility manifest, final verification, and isolated checkpoint.
