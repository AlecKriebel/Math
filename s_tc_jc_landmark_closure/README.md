# Sharp generic-identifiability boundary for level-2 JC networks

This directory is the active final-closure program.  It has exactly two
acceptable endpoints:

- **Outcome P:** standard semi-directed `S_TC` level-2 topologies are
  generically identifiable under open JC parameters modulo ordinary triangle
  redirection `T`, and the frozen weak-class theorem proves sharpness; or
- **Outcome C:** an exact non-`T` counterexample or proper one-sided generic
  containment inside the locked `S_TC` class.

The certified weak-class release in `../s_tc_jc_sharp_boundary/` is frozen.
Historical positive atlases and manuscripts are discovery inputs only and may
not be promoted without regeneration from primitive graphs.

No intermediate atlas, finite census, or conditional local-to-global theorem
is a terminal result.
