# Ordinary triangle move lock

Status: **VERIFIED for JC**

The only positive local move admitted in the final classification is ordinary
triangle redirection `T` as defined in `DEFINITIONS_LOCK.md`.

On 2026-08-09 the historical primary verifier and the clean-room JC release
verifier were replayed.  The independent replay regenerated byte-for-byte:

```
97097fa36e00edbf4837bbef3a255ccd756aac99136138746168ec94630df4dc
```

for `jc_triangle_redirection_release_certificate.json`.

The certificate proves that all three semi-directed orientations of the
three-port triangle have a common interior regular JC germ and that the
port-tensor equality survives arbitrary corresponding port grafting.  It
does **not** claim equality of complete open stochastic images.  All rooted
presentation moves and all non-`T` portions of the historical `R3` family are
excluded.

Reproduction command (from the historical audit tree):

```sh
env -i PATH="$PATH" PYTHONDONTWRITEBYTECODE=1 \
  AUDIT/CLEAN_ENV/bin/python \
  AUDIT/INDEPENDENT_IMPLEMENTATION/verify_jc_triangle_redirection_release.py
```
