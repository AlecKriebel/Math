# Superseded local release artifacts

This directory is the local quarantine for pre-DOI and superseded generated
release artifacts.  Its large generated contents are intentionally ignored by
Git.  They are retained only for provenance and must not be uploaded or cited.

The sole active generated proof object remains under `release_artifacts/`:

- `stc_jc_sharp_boundary_atlas_certificates_v1.1.7.tar.gz`;
- its `.sha256` sidecar;
- `CERTIFICATE_BUNDLE_ENVELOPE.json`; and
- `certificate_bundle_logs/`.

The active identities are recorded in `PERSISTENT_ARCHIVE_CHECKLIST.md`.
