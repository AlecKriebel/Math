# Superseded Outcome-P replay evidence (2026-08-13)

These files certify the earlier 18-page Outcome-P manuscript at source commit
`35291bba72f52ac800e99ea797ddad20d9852a67`. They are preserved for audit
history only. They do not certify the active manuscript, its PDFs, or the
current release tag.

`SHA256SUMS` is a frozen manifest of every preserved record other than the
manifest itself. Run `shasum -a 256 -c SHA256SUMS` from this directory to
check that the historical bytes have not changed.

The authoritative current replay evidence is distributed with the active
GitHub Release named in `release/PUBLIC_RELEASE_ASSETS.md`. Its external
envelope binds the immutable source commit, deterministic reproducibility
archive, submission PDFs, and clean quick/full/regenerate-all transcripts.
