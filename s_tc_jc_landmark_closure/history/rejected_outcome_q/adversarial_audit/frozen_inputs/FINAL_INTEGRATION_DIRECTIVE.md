# Final Integration, Bounded Omega Audit, and bioRxiv Release Program

## Mission

Continue from the local on-disk source and reproducibility package for:

> **Strong Tree-Childness Is a Sharp Identifiability Boundary for Level-2 Jukes–Cantor Networks**

Integrate the cloud-derived canonical-cleanup quotient theorem into that manuscript, perform one strictly bounded final audit of the historical Omega pair, and produce the complete final bioRxiv submission package.

The final paper must prove, for the intended literature-standard cleanup convention:

\[
\boxed{
\begin{aligned}
&\text{Every binary semi-directed strongly tree-child level-2 network}\\
&\text{is generically identifiable under open JC modulo ordinary}\\
&\text{triangle redirection }T,
\end{aligned}}
\]

with no proper one-sided generic containment, together with the certified sharpness statement that the larger weakly tree-child class is not generically identifiable modulo \(T\).

The Omega audit may strengthen the sharpness theorem. It may not delay the paper indefinitely.

Do not expand into:

- K2P or K3P;
- level greater than two;
- pointwise rather than generic classification;
- numerical parameter identifiability;
- finite-sample statistics;
- the full classification of \(W_{\mathrm{TC}}\);
- new Omega variants or Omega chains;
- additional conjectures.

This is the final research-and-publication pass.

---

## 1. Preserve the existing theorem packages

Before editing:

1. Freeze the current landmark manuscript and its complete active verifier package byte-for-byte.
2. Freeze the cloud Outcome Q package or report byte-for-byte.
3. Freeze the certified weak Theta ambiguity package byte-for-byte.
4. Freeze every historical Omega artifact required for the bounded audit.
5. Record SHA-256 hashes for all frozen inputs.
6. Create a new versioned working directory or Git branch for the merged paper.

Do not overwrite or retroactively alter any prior fail-closed package.

The final active verifier must not consume superseded or quarantined files except through explicitly named regression tests.

---

## 2. Import Outcome Q as local mathematical evidence

The Outcome Q result was produced in a separate cloud environment. Do not trust inaccessible cloud paths or summaries transitively.

### 2.1 Required local reconstruction

Either import the complete cloud source and verifier archive into the local repository or independently reimplement the short convention-closure proof locally.

The local package must regenerate:

1. the root-suppression artifact taxonomy;
2. the forced root-created zipper lemma;
3. the constructive already-simple rooting;
4. the exact JC zipper tensor;
5. the strict analytic section;
6. the cleanup-rooting and already-simple-rooting class relations;
7. the parallel-theta and root-created 2-sub-blob frontier;
8. survival of the weak Theta pair under cleanup;
9. the model-preserving quotient theorem.

No theorem in the final paper may rely only on a cloud transcript or unavailable file.

### 2.2 Locked quotient theorem

Use the following mathematical result, after local verification.

Let \(\mathcal S_2\) be the class of labelled simple mixed graphs obtained under the intended Englander–Brits cleanup convention from binary LSA-valid rooted level-2 networks that are strongly tree-child with respect to the complete cleanup rooting set.

For \(N\in\mathcal S_2\), define

\[
q(N)
\]

to be the same final labelled simple mixed graph equipped with its already-simple \(sd_0\) rooting fibre.

Prove:

\[
\mathcal M^{\mathrm{JC}}_{\mathrm{clean}}(N)
=
\mathcal M^{\mathrm{JC}}_{0}(q(N))
\]

as complete open stochastic model images.

Consequently,

\[
N\preceq_{\mathrm{JC}}N'
\iff
q(N)\preceq_{\mathrm{JC}}q(N'),
\]

and likewise for symmetric full-dimensional regular overlap.

The map \(q\) is the identity on the final mixed graph but changes the admissible-rooting fibre. Describe it as a:

> **canonical rooting-fibre reduction**

or:

> **canonical cleanup quotient**

and define that phrase precisely.

---

## 3. Integrate Outcome Q into one unified paper

Do not append the 22-page convention report as a wrapper around the existing paper.

Produce one conventional journal manuscript with:

- one title;
- one abstract;
- one introduction;
- one main-results section;
- one definitions section;
- one theorem numbering system;
- one bibliography;
- one data/code statement;
- one AI disclosure;
- one reproducibility appendix.

Remove all phrases such as:

- Outcome Q;
- frozen baseline;
- verified baseline;
- Part II;
- release theorem;
- cloud result;
- transferred theorem package;
- pipeline status;
- landmark result.

The manuscript should read as though the convention theorem and statistical theorem were developed as one coherent mathematical paper.

### 3.1 Recommended title

Retain:

> **Strong Tree-Childness Is a Sharp Identifiability Boundary for Level-2 Jukes–Cantor Networks**

Do not add “Outcome Q,” “canonical quotient release,” or similar process language to the title.

### 3.2 Recommended paper architecture

Use approximately this structure:

1. Introduction and biological motivation  
2. Network conventions and observational relations  
3. Canonical cleanup quotient  
4. Primitive level-2 factors  
5. Pointwise recovery of cut splits  
6. Projective bridge peeling  
7. Complete projective local classification  
8. Arbitrary subdivisions and coherent probes  
9. Global classification and generic reconstruction  
10. Sharpness outside the strong class  
11. Biological consequences and limitations  
12. Computer-assisted proof and reproducibility  
13. Appendices with exact algebraic data and theorem-to-certificate crosswalk  

The exact numbering may differ, but the argument must be continuous.

---

## 4. Rewrite the conventions section

Define separately:

\[
sd_0(D),\qquad \operatorname{clean}(D),\qquad SD_H(D).
\]

State exactly:

- rooted binary assumptions;
- LSA validity;
- retained reticulation arrowheads;
- root deletion;
- degree-two suppression;
- parallel-edge identification;
- treatment of rooted 2-blobs and non-leaf 1-blobs;
- admissible rootings under each convention;
- \(R_{\mathrm{TC}},W_{\mathrm{TC}},S_{\mathrm{TC}}\);
- level and blob definitions;
- ordinary triangle redirection \(T\).

Distinguish:

1. formation of the complete semi-directed topology;
2. restriction to a leaf subset;
3. construction of a displayed network;
4. construction of a displayed tree.

Do not call the conventions literally identical.

### 4.1 State the class relations

After proof, state:

\[
\operatorname{Root}_0(N)
=
\operatorname{Root}_H(N)
\subseteq
\operatorname{Root}_{\mathrm{clean}}(N),
\]

and:

\[
W_{\mathrm{TC}}(\mathrm{clean})
=
W_{\mathrm{TC}}(0)
=
W_{\mathrm{TC}}(H),
\]

while:

\[
S_{\mathrm{TC}}(\mathrm{clean})
\subsetneq
S_{\mathrm{TC}}(0)
=
S_{\mathrm{TC}}(H).
\]

Include the explicit strictness witness or move it to a concise appendix with a clear reference.

Do not let the strictness witness distract from the main topology theorem.

---

## 5. Present the root zipper cleanly

Add one original TikZ figure showing:

\[
\rho\to p,\qquad
\rho\to q,\qquad
p\to q,\qquad
p\to a,\qquad
q\to b
\]

and its cleanup to the ordinary edge

\[
a-b.
\]

### 5.1 Structural lemma

Prove that this is the only additional tree-child presentation admitted by cleanup but rejected by \(sd_0\).

Include explicitly:

- why the root children must be adjacent;
- why the adjacent lower child is reticulate;
- why the upper child is a tree vertex;
- why \(a\) and \(b\) are nonreticulate and distinct;
- why no arrowhead is lost;
- why no second cleanup step can occur.

Add the missing explicit sentence:

> If \(a\) and \(b\) were already adjacent, the head of that arc would have its existing parent \(p\) or \(q\) and a second parent, contradicting that \(a\) and \(b\) are nonreticulate.

Do not retain an unnecessary claim about iterated zipper contractions for arbitrary non-tree-child networks unless a complete induction is supplied. The theorem needs only the tree-child fibre.

### 5.2 Exact JC identity

Derive from displayed trees:

\[
\kappa
=
uv\left[
\lambda\alpha\beta+(1-\lambda)\gamma
\right].
\]

Prove that the complete normalized two-terminal Fourier tensor is:

\[
(1,\kappa,\kappa,\kappa).
\]

Then prove that every \(x\in(0,1)\) has a strict analytic preimage using:

\[
c=\frac{1+x}{2},
\qquad
m=\frac{4x}{(1+x)^2},
\]

\[
u=v=c,\qquad
\gamma=m,\qquad
\lambda=\frac12,
\]

\[
\alpha=\frac{1+m}{2},
\qquad
\beta=\frac{2m}{1+m}.
\]

Check exactly:

\[
\alpha\beta=m,\qquad
c^2m=x,
\]

\[
1-m=\frac{(1-x)^2}{(1+x)^2}>0,
\]

and:

\[
\frac{\partial\kappa}{\partial\gamma}
=
uv(1-\lambda)>0.
\]

State clearly that this proves equality of complete open two-terminal JC tensor images and therefore remains valid under contraction with any identical external context.

Do not generalize this to arbitrary parallel-edge identification.

---

## 6. State the main theorem directly for the cleanup class

The main theorem should no longer be stated first for \(sd_0\) and then transferred later.

State directly:

> Let \(N,N'\) be leaf-labelled binary literature-standard semi-directed strongly tree-child level-2 networks under the cleanup convention. Then \(N\preceq_{\mathrm{JC}}N'\) if and only if their labelled reduced bridge trees agree and each corresponding pair of nontrivial blobs is labelled-isomorphic or related by ordinary triangle redirection \(T\).

Then conclude:

- there are no proper one-sided generic containments;
- symmetric full-dimensional overlap has the same classification;
- outside a proper algebraic exceptional set, the topology is determined modulo \(T\).

The proof may invoke the cleanup quotient and then apply the already-simple classification, but the reader-facing theorem should cover the literature class directly.

### 6.1 Define the cleanup model image

Do not write \(\mathcal M_N^{\mathrm{JC}}\) ambiguously.

Define:

\[
\mathcal M_{\mathrm{clean}}^{\mathrm{JC}}(N)
\]

as the common open model image of all cleanup-admissible rooted presentations of \(N\).

Prove its well-definedness using:

1. root-zipper image equality;
2. ordinary degree-two suppression;
3. stationary reversible JC rerooting invariance.

Then identify it with:

\[
\mathcal M_0^{\mathrm{JC}}(q(N)).
\]

---

## 7. Preserve the corrected projective bridge theorem

The active manuscript must use only the full incidence-scaling action:

\[
P_u\mapsto a_{u,e}P_u,\qquad
P_v\mapsto a_{v,e}P_v,\qquad
x_e\mapsto\frac{x_e}{a_{u,e}a_{v,e}}.
\]

Retain the regression:

\[
\left(\frac12,\frac12,\frac12\right)
\quad\text{and}\quad
\left(\frac35,\frac35,\frac{25}{72}\right)
\]

have the same observable product \(1/8\).

Never reintroduce the withdrawn reciprocal-only chart.

The paper may recover:

- projective local tensor orbits;
- normalized effective bridge scales;
- topology modulo \(T\).

It may not claim recovery of:

- physical bridge multipliers;
- incident arm multipliers separately;
- every \(T\)-orientation as compatible with one fixed distribution.

---

## 8. Preserve the existing exact local-to-global proof

Do not reopen the graph-to-algebra atlas unless merging Outcome Q exposes a concrete dependency mismatch.

The local researcher should:

1. verify that \(q\) leaves every final cleaned mixed graph unchanged;
2. verify that no new blob core is introduced;
3. verify that the existing projective local atlas applies without modification;
4. verify that every local invariant remains multihomogeneous;
5. verify that the bridge tree, ports, segment words, and incoming roles are unchanged;
6. verify that ordinary \(T\) is transported exactly;
7. rerun the complete active dependency graph.

No target-only historical count may re-enter the proof.

---

# Part II — Bounded Omega Audit

## 9. Hard audit limit

The Omega audit is the final optional strengthening.

It is strictly bounded by:

- **four wall-clock hours**, or
- **one complete primary replay plus one independent replay**, or
- **one repair iteration for an implementation defect**,

whichever limit is reached first.

After that limit:

- if every acceptance gate passes, include Omega;
- otherwise quarantine Omega and complete the paper without it.

An unresolved Omega question is not allowed to block bioRxiv preparation.

Do not investigate:

- Omega-chain;
- larger Omega families;
- altered Omega cores;
- additional leaf placements;
- K2P/K3P Omega behavior;
- attempts to modify Omega to satisfy a failed gate;
- new weak-class examples.

Audit only the exact historical Omega pair.

---

## 10. Recover the exact historical Omega pair

Locate and freeze:

- the rooted source and target arc lists;
- their intended semi-directed reductions;
- the original exact JC parameter correspondence;
- the exact common point;
- Fourier-coordinate formulas;
- Jacobian minors;
- any independent verifier;
- every prior class-membership audit.

Record hashes before modification.

Use the final cleanup convention from the merged manuscript, not the historical convention labels.

---

## 11. Omega acceptance gates

Omega may enter the paper only if all mandatory gates below pass.

### Gate O1 — final topology and class membership

Independently verify that both cleaned topologies are:

1. binary;
2. LSA-valid through at least one rooted presentation;
3. level two;
4. triangle-free after complete cleanup;
5. members of \(W_{\mathrm{TC}}(\mathrm{clean})\);
6. not members of \(S_{\mathrm{TC}}(\mathrm{clean})\);
7. nonisomorphic as labelled mixed graphs.

Enumerate the complete cleanup-admissible rooting set for each topology.

Report:

- total admissible rootings;
- tree-child rootings;
- explicit non-tree-child rooting;
- exact omnian or reticulation-child witness.

Because the graphs are claimed triangle-free, \(T\)-equivalence should reduce to labelled isomorphism. State this explicitly rather than merely saying “non-\(T\).”

If cleanup identifies the two graphs or creates a triangle, Omega fails.

### Gate O2 — exact JC model relation

Rebuild both JC parameterizations from the rooted graph encodings.

Verify:

1. every displayed switching;
2. every descendant mask;
3. every Fourier coordinate;
4. the exact common point;
5. strict positivity of every edge multiplier and inheritance parameter;
6. equality of all complete Fourier coordinates;
7. equality of pattern probabilities;
8. generic dimensions of both models;
9. nonzero full-rank Jacobian minors;
10. a common regular relatively open region of full model dimension.

Equal Zariski closures alone are insufficient.

An isolated common point is insufficient.

A boundary witness is insufficient.

If the original correspondence used a convention artifact removed by cleanup, rebuild the correspondence in the final cleaned model coordinates.

### Gate O3 — triangle-free sharpness interpretation

If Gates O1 and O2 pass, prove the exact statement:

\[
\boxed{
\text{The triangle-free subclass of }
W_{\mathrm{TC}}(\mathrm{clean})
\text{ is not generically JC-identifiable.}
}
\]

Explain that this strengthens the existing sharpness theorem:

- the Theta pair proves failure over all \(W_{\mathrm{TC}}\);
- the Omega pair would show that triangles are not needed for that failure;
- strong tree-childness itself, rather than triangle geometry, is the decisive hypothesis.

Do not claim a complete classification of triangle-free \(W_{\mathrm{TC}}\).

### Gate O4 — direct all-taxa propagation

Attempt exactly one all-\(n\) extension:

- identical leaf substitution by rooted binary trees;
- using the already proved positive analytic substitution inverse.

Audit whether this preserves:

- triangle-freeness;
- \(W_{\mathrm{TC}}\setminus S_{\mathrm{TC}}\);
- nonisomorphism;
- exact overlap;
- full dimension.

If this closes directly, state the quantified all-\(n\) Omega theorem.

If it does not close immediately from the existing substitution lemma, stop. Do not design another propagation gadget.

A finite triangle-free weak-class counterexample is already sufficient to strengthen the boundary theorem.

### Gate O5 — independent replay

Use:

1. one primary implementation;
2. one independently written implementation that shares no graph reduction, rooting enumeration, displayed-tree, Fourier, number-field, or Jacobian code.

Mandatory Omega mutation tests:

- delete one admissible rooting;
- classify only the displayed rooted presentation;
- omit cleanup;
- introduce a triangle;
- swap one pendant label;
- change one arrowhead;
- replace one parameter by a boundary value;
- alter one Fourier coordinate;
- replace one Jacobian minor;
- identify the two graphs incorrectly.

Every mutation must fail.

---

## 12. Omega outcomes

Use exactly one of these.

### Omega-PASS-ALL-\(n\)

The exact historical pair is triangle-free, lies in \(W_{\mathrm{TC}}\setminus S_{\mathrm{TC}}\), has full-dimensional regular open JC overlap, and extends to every \(n\ge n_0\).

Add a principal sharpness theorem such as:

> Even the triangle-free weakly tree-child level-2 class is not generically identifiable under JC. This failure occurs for arbitrarily many taxa.

### Omega-PASS-BASE

The exact pair passes all topology and stochastic-overlap gates, but no all-\(n\) propagation is certified within the audit limit.

Add:

> The strong tree-child hypothesis is necessary even in the triangle-free level-2 class.

Do not claim an all-taxa Omega family.

### Omega-FAIL

Any load-bearing gate fails or remains unresolved at the audit limit.

Move all Omega material to:

```text
history/quarantined_omega/
```

Record the exact failed gate and proceed immediately with the existing Theta sharpness theorem.

Do not mention Omega in:

- the title;
- abstract;
- main results;
- cover metadata;
- bioRxiv submission summary.

A brief historical note in the development log is sufficient.

---

# Part III — Final Unified Manuscript

## 13. Sharpness section

The existing Theta construction remains mandatory because it supplies:

- an explicit non-\(T\) ambiguity;
- a full-dimensional regular stochastic overlap;
- an exact all-\(n\) extension;
- convention-closed failure in \(W_{\mathrm{TC}}\setminus S_{\mathrm{TC}}\).

If Omega passes, present the sharpness section in this order:

1. Theta: all weakly tree-child networks are not identifiable.
2. Omega: the failure persists even after imposing triangle-freeness.
3. Interpretation: strong tree-childness, not merely absence of triangles, is the positive theorem’s essential structural hypothesis.

If Omega fails, retain only Theta and do not weaken the established main theorem.

Do not submit the standalone weak ambiguity paper separately if its complete theorem and proof are incorporated into the unified manuscript.

Preserve that standalone package as an archived derivation module.

---

## 14. Abstract requirements

The abstract must contain, in ordinary prose:

1. the biological inference question;
2. the full literature-standard cleanup class;
3. the if-and-only-if one-sided containment theorem;
4. generic identifiability modulo ordinary triangle redirection;
5. projective rather than physical bridge recovery;
6. strong-tree-child sharpness;
7. Omega’s triangle-free strengthening only if certified.

Avoid:

- “landmark”;
- “Outcome Q”;
- “pipeline”;
- “candidate theorem”;
- “frozen baseline”;
- internal gate names;
- raw computational counts unless essential.

Keep the abstract approximately 180–250 words.

---

## 15. Introduction and novelty framing

State the prior frontier accurately:

- Englander et al.: triangle-free strongly tree-child level-2 generic JC identifiability.
- Brits et al.: level-1 full identifiability modulo triangle redirection and the broader cleanup convention.
- Currie et al.: semialgebraic triangle-model intersections and differences.
- Ardiyansyah: earlier level-2 Fourier-invariant comparisons.
- Holtgrefe et al.: weak and strong semi-directed tree-child frameworks.

State the present contribution as:

1. complete generic observational equivalence for the literature-standard strongly tree-child level-2 JC class;
2. exact classification of one-sided containment;
3. canonical cleanup quotient resolving the convention mismatch;
4. reconstruction modulo \(T\);
5. sharp failure in the weak class;
6. triangle-free weak-class failure only if Omega passes.

Do not claim:

- pointwise identifiability everywhere;
- equality of complete \(T\)-related images;
- physical parameter identifiability;
- K2P/K3P results;
- all higher-level networks;
- a complete classification of \(W_{\mathrm{TC}}\).

---

## 16. Naming and terminology

Use descriptive names only:

- canonical cleanup quotient;
- root zipper;
- root-zipper JC identity;
- projective bridge peeling;
- theta pendant-transfer ambiguity;
- triangle-free weak-class ambiguity, if Omega passes.

Do not self-name any theorem, lemma, pair, or conjecture after Kriebel.

Do not add a new conjecture in this final pass.

---

# Part IV — Reproducibility and Release

## 17. One active dependency graph

The final active proof graph must include:

1. literature cleanup conventions;
2. root-zipper structural theorem;
3. exact zipper JC identity;
4. canonical rooting-fibre reduction;
5. baseline primitive factor theorem;
6. pointwise cut theorem;
7. projective bridge theorem;
8. complete projective local atlas;
9. marginal submersion;
10. coherent probes;
11. ordinary triangle germ;
12. global synthesis;
13. Theta sharpness;
14. Omega only if all gates pass.

Every superseded claim must be outside the active manifest.

The active verifier must fail if:

- the old reciprocal bridge chart is restored;
- the paper states literal convention equality;
- a target-only atlas replaces decorated relations;
- physical bridge recovery is claimed;
- all \(T\)-orientations are called pointwise compatible;
- Omega is cited after a failed gate.

---

## 18. Clean-clone verification

From a fresh clone or freshly unpacked archive with no untracked files, run:

```bash
bash reproducibility/verify_quick.sh
bash reproducibility/verify_full.sh
bash reproducibility/verify_regenerate_all.sh
```

The commands must:

- require no unavailable cloud path;
- require no manually restored file;
- record exact Git status;
- record Python, compiler, symbolic-algebra, and TeX versions;
- record elapsed time and peak memory;
- rebuild the manuscript;
- check every active manifest;
- run Outcome Q locally;
- run Omega only if included;
- verify PDF fonts and page count;
- reject unresolved references and overfull boxes.

Preserve complete transcripts.

---

## 19. Visual and editorial audit

Render every page using at least two independent PDF renderers.

Inspect manually for:

- clipping;
- overlapping formulas;
- broken TikZ figures;
- unreadable tables;
- missing glyphs;
- incorrect theorem references;
- bibliography errors;
- outdated preprint versions;
- inconsistent notation;
- stale `sd0`-only wording;
- internal process terminology.

Run an exact search for:

```text
Outcome Q
frozen baseline
Part II
verified baseline
candidate theorem
landmark
1,686 valid completions
physical bridge multiplier recovered
all T orientations are compatible
```

None may remain in active journal prose, except historical terms inside a clearly labelled correction-history file.

---

# Part V — bioRxiv Submission Package

## 20. bioRxiv positioning

Prepare the submission for:

- **Server:** bioRxiv
- **Subject area:** Evolutionary Biology
- **Manuscript type:** New Results
- **Affiliation:** Independent Researcher
- **Primary field framing:** evolutionary-history and phylogenetic-network identifiability

Make the life-science relevance explicit in:

- title;
- first two abstract sentences;
- introduction;
- biological-consequence section;
- submission metadata.

The paper must explain what exact infinite-data sequence distributions can and cannot recover about reticulate evolutionary histories.

---

## 21. Required bioRxiv files

Produce a directory:

```text
biorxiv_submission/
```

containing:

### Main files

```text
Strong_Tree_Childness_Sharp_Level2_JC.pdf
Strong_Tree_Childness_Sharp_Level2_JC_source.zip
```

The source archive must include:

- LaTeX;
- BibTeX;
- every TikZ/vector figure source;
- any custom style files;
- build instructions.

### Supplement files

Prepare one reasonably sized upload supplement containing:

- computational-methods guide;
- theorem-to-certificate crosswalk;
- compact exact certificates;
- verification README;
- environment lock files;
- persistent repository link or DOI for the complete large archive.

Do not make bioRxiv the only host for a very large reproducibility archive.

Prepare a deterministic full archive for Zenodo or OSF and record its SHA-256. If the human author has not yet deposited it, use one clearly marked DOI placeholder in the private submission checklist, not in the public final PDF.

### Metadata sheet

Produce:

```text
BIORXIV_METADATA.md
```

with:

- exact title;
- author name;
- affiliation;
- corresponding email;
- ORCID;
- abstract;
- subject category;
- article type;
- keywords;
- funding statement;
- competing-interest statement;
- author-contribution statement;
- data/code availability statement;
- AI-use disclosure;
- suggested license options for human selection;
- suggested manuscript blurb of no more than 100 words.

Do not fabricate funding, affiliations, DOI values, or institutional approvals.

### Upload map

Produce:

```text
BIORXIV_UPLOAD_MAP.md
```

stating exactly:

- which file is the main manuscript;
- which file is supplementary material;
- which metadata goes into each portal field;
- which choices remain for the human author;
- final checks before pressing Submit.

---

## 22. AI disclosure

Retain a substantive disclosure stating that generative AI systems assisted with:

- exploratory algebra;
- implementation;
- drafting;
- adversarial review.

State that:

- several earlier automated claims failed audit and were withdrawn;
- active results are supported by exact graph-derived certificates and independent replays;
- no AI system is an author;
- the human author accepts responsibility for the final theorem, citations, code, and submission.

Do not claim that complete prompts or logs are archived unless they actually are.

---

## 23. Final bioRxiv-readiness audit

Before release, an independent reviewer must answer:

1. Does the title describe the exact cleanup class?
2. Does the main theorem apply to the full intended literature class?
3. Is \(q\) defined without circularity?
4. Is the zipper the only extra tree-child cleanup fibre?
5. Is its complete open JC image exactly one edge?
6. Does the baseline local-to-global theorem remain valid after the quotient?
7. Is the weak Theta sharpness theorem unchanged?
8. If Omega is included, is it truly triangle-free under cleanup?
9. If Omega is included, is its stochastic overlap full-dimensional and strictly interior?
10. Are all claims reproducible without cloud-only artifacts?
11. Is the manuscript free of pipeline language?
12. Does the biological framing satisfy bioRxiv scope?
13. Is every upload file present and openable?
14. Are all hashes and transcripts current?
15. Are all human-only submission choices clearly identified?

Any negative answer must be repaired before the final outcome.

---

## 24. Final deliverables

Return all of the following:

1. final unified manuscript PDF;
2. complete LaTeX/BibTeX/TikZ source;
3. complete reproducibility archive;
4. compact bioRxiv supplement;
5. `BIORXIV_METADATA.md`;
6. `BIORXIV_UPLOAD_MAP.md`;
7. theorem-to-certificate crosswalk;
8. final active dependency graph;
9. prior-work and novelty audit;
10. correction history;
11. quick verification transcript;
12. full independent transcript;
13. regenerate-all transcript;
14. PDF visual-inspection report;
15. exact Git commit;
16. archive SHA-256 files;
17. final human submission checklist.

---

## Final outcome

Return exactly one of these.

### Outcome A — BIORXIV PACKAGE COMPLETE WITH TRIANGLE-FREE OMEGA SHARPNESS

All Outcome Q material is integrated into the unified paper, the bounded Omega audit passes, Omega is included with its exact certified scope, and the complete bioRxiv package passes clean-clone review.

### Outcome B — BIORXIV PACKAGE COMPLETE WITHOUT OMEGA

All Outcome Q material is integrated into the unified paper, Omega fails or remains unresolved within the bounded audit, Omega is quarantined, and the complete bioRxiv package passes clean-clone review using the already certified Theta sharpness theorem.

### Outcome C — MAIN THEOREM RELEASE BLOCKED

Use this only if integrating Outcome Q exposes a new load-bearing defect in the main strong-class theorem itself.

Provide:

- the first exact mathematical defect;
- the maximal theorem still certified;
- the completed bounded Omega disposition;
- a corrected non-submission manuscript.

Omega failure or incompleteness alone is not a valid reason for Outcome C.

Do not return an intermediate progress report. Complete the merged manuscript and bioRxiv package with or without Omega.