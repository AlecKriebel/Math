# Supplement build

From this directory, run:

```bash
export SOURCE_DATE_EPOCH=1786924800
tectonic supplement.tex
```

Exact-byte replay was tested with Tectonic 0.16.9 and default bundle v33.
The source is self-contained apart from standard LaTeX packages supplied by
that bundle.
