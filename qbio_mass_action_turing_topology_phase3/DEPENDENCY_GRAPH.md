# Theorem dependency graph

```text
mass-action differentiation + converse reconstruction
                    |
                    v
        exact reduced homogeneous Jacobian
                    |
          +---------+----------+
          |                    |
          v                    v
conservation splitting    principal-minor scaling
          |                    |
          +---------+----------+
                    v
       fixed-J diagonal-damping theorem
                    |
                    v
       exact all-mobile iff characterization
          |             |              |
          v             v              v
  selector encoding  certificate    fixed-n projection
          |          completeness       |
          v                             v
  existential-real membership    fixed-n polynomial time

PARTITION
   |
   v
open-cube stable-family equivalence
   |
   v
row-splitting similarity
   |
   v
right-scaling elimination
   |
   v
exact row-segment mass-action realization
   |
   v
NP-hardness of stationary-Turing admissibility
```

The NP-hardness branch uses no unrestricted diagonal-stabilization theorem. It proves scaling elimination only for its explicitly lifted family. The negative singleton minor in the reduction is fixed and positive before the signed-minor convention, so homogeneous stabilizability is the only remaining YES/NO issue.
