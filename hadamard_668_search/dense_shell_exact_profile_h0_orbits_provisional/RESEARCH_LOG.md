# Research log

## 24 July 2026 PDT

- Read the completed, hash-pinned result records for `h0-p00-p07` and
  `h0-p00-p08` without modifying the running census.
- Extracted one and two retained canonical exact-profile records,
  respectively.
- Reimplemented the profile arithmetic independently of the production
  classifier and of both earlier exact-profile verifiers.
- Replayed every one of the 37 physical correlation equations for each of
  the three new profiles.  All nonzero lags vanish and zero lag is 167.
- Rebuilt the full exact 24-element
  `C6 x C2(A-star) x C2(B-star)` action.  Each new representative is
  canonical, has stabilizer order one, and therefore orbit size 24.
- Rebuilt the two earlier profile orbits in the same verifier.  Every one
  of the ten pairwise intersections among the five orbits is empty.
- Recomputed all five production digests exactly.  No discrepancy was
  found.
- Scope remains exact compressed profiles only.  The exhaustive `h=0`
  census is still running, and no labelled `LP(333)` or `H(668)` is claimed.
