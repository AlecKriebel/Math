# Rank-two Jordan obstruction research log

## 2026-07-25

- Received the unique quotient sign-class left by the constant-rank-two
  universal diagonal weight obstruction.  Fixed permutation class 607
  as the representative and verified its quotient equations, diagonal
  degrees, modular ranks, and order-two automorphism group.
- Restored the fixed coefficient `eta=+/-1` of the `z^18 J` trace term
  for the two surviving Jordan types.
- Derived a common seven-dimensional homogeneous diagonal over-code for
  the normalized nonzero-`J2` and nonzero-eigenline-plus-`J2(0)` types.
- Exhausted all 37 affine cosets using 64 information assignments per
  coset.  Binary diagonal words occur only for the inadmissible
  coefficients `eta=+3` and `eta=-3`; both genuine trace orientations
  are empty.
- Scope decision: this closes the exceptional quotient inside the
  constant symmetric rank-two exponential family.  Higher-`y`
  conjugators and unrestricted semiregular `C37` lifts remain open.
- Combined with the existing split-semisimple obstruction, the
  QR/NQR-only result for the other rational types, and the exact absence
  of an all-zero diagonal quotient in the 625-class census, this upgrades
  the constant-rank-two no-go from one certified quotient to every
  semiregular quotient class.
- Independent red-team replay confirmed the ambient-coordinate
  symmetric expansion, the seven-dimensional affine code and its
  information set, the complete `eta=+/-3` nonempty-coset spectrum, the
  emptiness at `eta=+/-1`, and the eigenvalue-normalizing decimation.
