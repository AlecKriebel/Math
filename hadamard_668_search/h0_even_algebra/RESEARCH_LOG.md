# Research log

## 2026-07-24

- Reconstructed the twelve even quadrics and six odd bilinear-affine rows
  in the canonical `21+15` half-turn basis.
- Tested common translations.  Both the even `X` centering system and the
  odd affine-term absorption system have ranks `21/22`, so neither is
  consistent.
- Computed exact associative closures of the relative polar operators.
  The `X` and `Y` pencils generate the full matrix algebras of dimensions
  441 and 225.
- Repeated the calculation after imposing the canonical asymmetric
  `y=e_0` odd fiber.  Its 15-variable restricted pencil also generates
  the full 225-dimensional matrix algebra.
- Interpretation: no simultaneous invariant-block, diagonal, or ordinary
  field-trace norm contraction exists for these pencils.  This is a
  rigorous negative result and does not exclude nonlinear elimination.
