# Research log

## 2026-07-24--25 PDT

- Resumed the arbitrary-quadratic antipodal rank-at-most-two census from
  the frozen five-profile dimension-gated certificate.
- Implemented one-orbit atomic continuation records with semantic input
  hashes, deterministic coordinate and score stream hashes, complete
  score histograms, and immediate physical replay of every exact
  second-digit point through the following digit and exact row-margin
  catalog.
- Exhausted the 13 formerly deferred canonical representative gauges:
  six dimension-17 images and seven dimension-18 images.  Together with
  the five legacy-safe gauges, this accounts for exactly
  `3,663,754,254` physical quotient states.
- Found seven exact second-digit assignments, on `orbit-01`,
  `orbit-04`, `orbit-06`, `orbit-08`, `orbit-11`, `orbit-13`, and
  `orbit-18`.  Their assignment hashes and complete physical data are
  embedded in the detached certificate.
- Replayed every assignment.  Their following-digit defects are
  `10,13,10,14,13,14,11`; none passes the following digit and none is a
  member of the exact 1,756-word row-margin catalog.
- Froze and detached the merged 18-record certificate.  The validator
  reconciles every histogram, requires the score-18 bin to equal the
  survivor list, reconstructs all 18 quotients, verifies the legacy and
  continuation semantic provenance, and directly replays all seven
  controls without reading the ignored production output.
- Conducted a critical scope audit before interpreting the result.
  Reconstructed all `18*24=432` action incidences and all 360 distinct
  shell profile images, computing only exact feature-space ranks.
- Independently cross-checked the action census with a separate pure
  Python `F_3` eliminator that does not call the quotient or project rank
  routines.  It reproduced every per-orbit distribution, the global
  incidence distribution `(14:24,15:8,16:80,17:96,18:224)`, the distinct
  image distribution `(14:12,15:8,16:56,17:96,18:188)`, and the exact
  87,815,310,840-state workload.
- Discovered that the quadratic antipodal feature law is not invariant
  under the classification action.  `orbit-02`, `orbit-04`, `orbit-05`,
  `orbit-08`, `orbit-10`, and `orbit-17` have gauge-dependent physical
  dimensions.  This invalidates any interpretation of one canonical
  census as coverage of a whole classification orbit.
- Corrected the claim to exactly 18 frozen canonical gauges.  The other
  342 action images remain unenumerated.  Their independent state
  workload is exactly `84,151,556,586`, versus `3,663,754,254` states
  already completed.
- Preserved the legacy v1 verifier and certificate byte-for-byte.  Added
  a separate scope-correction record and superseding v2 certificate
  rather than silently rewriting historical evidence.
- The 13 continuation records totaled 13,248.53 wall seconds,
  11,064.40 user seconds, and stayed below 60,866,560 bytes maximum RSS.
  The detached full replay passes in about six seconds and 41 MB.
- Current canonical certificate semantic hash:
  `963a4cb24217fa1a58afa7d7840cf965821c76177c6c6711d33862c00355dc7a`.
- Current action certificate semantic hash:
  `029ae4f1b6186435d2f1b445f88da1d7e69479b4a65bac9e179e9cb2a8aafcad`.
- No external contact, commit, push, or top-level documentation change
  occurred.
