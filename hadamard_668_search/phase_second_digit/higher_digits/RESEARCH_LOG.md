# Higher-digit research log

## 2026-07-24

- Derived the exact integral lattice criterion for every lambda digit.
  Through digit 3 is equivalent to both Eisenstein coordinates being zero
  modulo 9; digit 4 adds their sum modulo 27.
- Built direct modular CNF and CP-SAT encodings.  Bounded generic runs were
  UNKNOWN even at the abundant second digit.
- Developed incremental ternary repair for the eighteen exact quadratic
  second-digit forms.  On profile `h2-422220-0`, a vectorized tabu run found
  a full second-digit witness after 2,952,653 updates and 121.92 observed
  seconds.
- Replayed all twenty displayed coefficients exactly.  The witness has 17
  nonzero rows at digit 3 and is fixed by none of the five excluded proper
  common-multiplier supergroups.
- Exhausted all nonzero Eisenstein integers of norm at most `135^2`.
  Maximum lambda valuation is 8, so nine zero digits `0,...,8` force exact
  zero.
- Ran five 300-second exact-coordinate tabu audits at digit 3.  Best
  objectives were 9, 5, 8, 8, and 9; every status is UNKNOWN.
- The structured-family audit independently established that its unique
  digit-2 survivor fails digit 3 in five rows and is fixed by excluded
  supergroup ID8.
- A hinted six-worker exact mod-9 CP-SAT run remained UNKNOWN after 300
  seconds, 3,549,810 branches, and 475,442 conflicts.
- Tested full and relaxed Newton correction sheets, three random
  codimension-12 CDCL slices, and five codimension-12 restricted tabu
  slices.  All were UNKNOWN.
- Independently learned that digit 3 has nineteen genuine carry equations;
  the combined digit-2/digit-3 linearization has rank 36 and inconsistent
  correction at the known digit-2 witness.
- Directly attempted digit 4 for 300 seconds.  The best exact combined
  digit-2/3/4 objective was 17; status UNKNOWN.
- Publication audit: the complete-search table in the higher-digit note is
  explicitly local to the five `h=2` profiles; the repository-wide estimate
  separately covers `h=1` and `h=0`.
- Publication audit: bounded checkpoint files replay their saved points but
  omit full stochastic trajectories, and the sparse-solver JSON files are
  retained as historical benchmarks whose old model sizes need not be
  regenerated by the subsequently strengthened scripts.
- Ran a five-profile, 300-second-per-profile exact-quadratic census.  Nine
  distinct full digit-2 points were replayed: five on `h2-222222-0` and
  four on `h2-422220-1`.
- Two census points also satisfy the delayed `E1(origin)` digit-3 linear
  equation, disproving the candidate obstruction that the digit-2 variety
  forces that row nonzero.  They still leave 11 and 16 digit-3 rows
  nonzero, so they are stage-2.5 witnesses and not digit-3 lifts.
- Exact trivial-character replay found that all nine census points miss
  their profiles' compatible row-margin corpora.  The two stage-2.5
  witnesses are therefore useful local algebra probes but not viable
  partial `LP(333)` lifts.  Candidate 0's delayed row alone survives
  through digit 4 and first fails digit 5; other rows already fail digit 3.
- Exhausted all 13,065,937 affine points through Hamming radius 5 around
  the better candidate-0 stage-2.5 point.  Only the center and one
  radius-5 neighbor satisfy digit 2; the neighbor is worse at digit 3 and
  also misses the row-margin corpus.
- A 600-second delayed-hyperplane tabu run from the candidate-0 center,
  with digit-2 residual weight 30, made 287,990 updates and did not improve
  the exact objective 11 (`UNKNOWN`).
- Added exact six-phase-sum corpus membership and fixed-target selection to
  the sparse CP-SAT model, together with a digit-2-only mode.  Candidate 0's
  72-target union was `UNKNOWN` after 300 seconds; eight sampled 30-second
  target shards were also all `UNKNOWN`.
- Built a complementary permutation tabu instrument whose pair moves
  preserve one exact six-sum target by construction.  A 60-second
  candidate-0 target-0 run ended `UNKNOWN` at `(2,9)` nonzero
  first-/second-digit rows after 26,173 moves.
