# Research log

## 2026-07-24

- Derived the universal phase-adjusted four-clique decomposition of the
  case-26 characteristic-three pair-state quadratic.
- Proved both 18-dimensional quotient projections are injective and
  computed the exact ungauged work count
  `687,194,767,360`.
- Rejected dense `F_3^20` character transforms on RAM grounds and global
  component caches on exact row-count and disk grounds.
- Implemented a packed-trit C++ hash join.  It reproduced the pinned
  quotient's 62 survivors and three separated independent NumPy counts.
- Integrated the independently certified free spatial-reflection gauge
  from `../eliahou_global_reuse_math/`.  The gauge fixes one odd
  noncentral long orientation and reduces the exact principal count by
  40%, to `412,316,860,416`.
- Retained ungauged mode as a control.  On quotients `0`, `131071`,
  `262143`, and pinned quotient `260914`, gauged mode reconstructs the
  complete reflection orbits and exactly matches ungauged survivor
  counts, best witnesses, exact-candidate lists, and canonical SHA-256
  digests.  Pinned quotient: `31` representatives reconstruct to `62`
  full survivors.
- Hardened the range kernel:
  - reconstructs all 78 support bits;
  - compares exact integer polynomial values with independent
    bit-packed four-row physical correlations for every survivor;
  - checks weight 39, energy 334, content four, and mod-6 divisibility;
  - retains exact candidates and range minima;
  - hashes the canonical full survivor/residual stream;
  - emits source/model-pinned atomic JSON manifests.
- Added an eight-worker runner with an 8 GB aggregate RSS guard, a
  100 MB output guard, exact resume validation, and an aggregator.
- Added an independent NumPy range replay.  A two-range/four-quotient
  test ran, resumed with zero recomputation, aggregated, and replayed
  successfully.
- Measured a production-path single-core range of 2,048 quotients:
  `3,221,225,472` join rows in `47.9903545` seconds, 78,998 full
  survivors replayed, zero exact supports, and `17,498,112` bytes
  maximum RSS.  The literal full-case projection is 1.706 hours on one
  core; eight-worker scaling remains unmeasured.
- Initially deferred the whole case-26 census while the eight-worker dense
  h0 census was active.
- Subsequently completed all `262,144` quotient states in `256` atomic
  ranges.  The exact aggregate contains `412,316,860,416` gauged join rows
  and `10,533,216` modular survivors.  Every survivor received both an exact
  integer-polynomial check and a bit-packed physical replay.
- Found zero exact integer supports and emitted no exact candidates.  This
  closes the defined case-26 global-quotient census, not the wider H(668)
  problem.
- The canonical range-stream digest is
  `c566eef9154ebefb13bca52c5e3e931c622e7d930619e2d3f35bec09d9e27fc7`.
  The run-config and aggregate SHA-256 values are respectively
  `e47a5d236701aca230308a30c2739a3b7835d1e9d7bfb9b2ca0957025395280c`
  and
  `268adc90d99e7a045c60879fd9367910c78a3b0b93e11aab47589e743d5a5253`.
- The global best nonexact witness is quotient `123143`, central value `1`,
  pair state `369546495487`, with four nonzero normalized residuals and
  residual vector
  `[0,0,0,-36,0,0,0,18,6,0,0,0,0,0,-6,0,0,0,0,0]`.
- Independently replayed that quotient through NumPy polynomial substitution
  and the physical rows: 46 survivors, zero exact supports, and an exact
  match to the aggregate best witness.
- Added a compact tracked completion certificate and an output-independent
  verifier.  Optional `--live` verification strictly re-aggregates all 256
  ignored range manifests and checks the pinned live artifact hashes.
