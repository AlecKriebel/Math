# Historical Phase II--V verification tools (Python 3.11 or newer).
# Upper major-version bounds avoid silently accepting incompatible APIs while
# leaving patch/minor updates available on supported Python releases.
sympy>=1.12,<2
networkx>=3.2,<4
pytest>=8,<10
