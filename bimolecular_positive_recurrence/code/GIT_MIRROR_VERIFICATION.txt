LOCAL RERUN NOTE — NONCANONICAL PROVENANCE
Date: 2026-08-09

This note supersedes the earlier Version 0.2 transcript.  That transcript
described a report containing interpreter metadata and source hashes gathered
recursively from incidental files, so its claimed cross-environment digest is
not a canonical release result.  It remains in the release only as an
explicitly labelled historical provenance artifact.

Version 0.3 keeps environment metadata outside the stable report, hashes a
closed explicit source allowlist, independently validates every top-complex
certificate, and compares regenerated canonical JSON with a committed golden
file without overwriting it.

Local rerun environments:
  CPython 3.11.8 on macOS 26.5.2, arm64
  CPython 3.14.6 on macOS 26.5.2, arm64

Command:
  ./reproduce.sh

Outcome:
  38 unit tests passed under each interpreter.
  Two regenerated reports were byte-identical.
  Both matched the committed verification_report.json byte for byte under
  both interpreters, with the same report digest.
  verification_report.json SHA-256:
  08e3c57bbd792ff119def1719ad2fc5151d2e6671e203a0604ab11f22e68b84d

The canonical evidence is verification_report.json plus reproduce.sh.  This
human-readable note is excluded from the report's source allowlist and makes
no claim of external or independent expert review.
