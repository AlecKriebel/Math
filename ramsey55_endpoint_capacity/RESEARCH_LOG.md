# Publication log

## 2026-07-24 - Short-note publication decision

- The endpoint theorem and capacity profile were judged worthy of a short,
  provisional computational-combinatorics note.
- The publication claim was deliberately limited to the
  `(e(A),e(H))=(85,128)` endpoint in the hypothetical regular degree-18
  branch.
- The paper does not claim a new bound for `R(5,5)`.
- Classical feasible-cone and counting antecedents are explicitly credited.
- The public note is paired with the already released history-independent
  verifier bundle and exact archive digest.
- Packaging revision v1.0.1 was published after a repeat-run audit found that
  ordinary Python cache files and Git checkout metadata could trigger the
  strict file-set guard. The mathematical outputs are unchanged; the corrected
  release works both from a Git checkout and a clean extracted archive.
- The public v1.0.1 archive SHA-256 is
  `de541d6c7ed8be496784397ea0ee3f1b12c2b93cdbc42ba908160095c1d79cc4`.
- Prepared a permanent research checkpoint at
  `research/ramsey-number-5-5/` and a paper page at
  `papers/ramsey-endpoint-capacity/`, with the global-bound disclaimer,
  verifier release, source audit, PDF, and publication metadata linked
  together.
- The broader research program remains paused under its four recorded resume
  gates.
