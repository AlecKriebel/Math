The next phase should **stop trying to prove positive recurrence of the raw embedded jump chain**. Fast neutral reactions can generate enormous jump counts without consuming much physical time, and tight inactive coordinates need not have finite support. The clean repair is to:

1. replace the primary chart workload by a **strictly positive lexicographic scalarization** whose shells are finite;
2. run the aggregate-debt/current-target mechanism in physical time;
3. prove shellwise finite mean descent for that proper workload;
4. conclude CTMC positive recurrence through a fully stated random-time Foster theorem.

That directly addresses the remaining moving-environment issue: the earlier project had already certified the bookkeeping and finite algebra, while the unresolved question was concentration where strict sources are unavailable or rarely occupied. 

Use this directive:

```text
# Autonomous T3-2 Definitive Final-Repair Program:
# Strictly Positive Lexicographic Workloads,
# Tight-Environment Elimination,
# Physical-Time Trace Cost,
# and Shell-Adapted Foster Closure
#
# Do Not Prove Recurrence of the Raw Embedded Jump Chain

## Mission

Continue from:

    the latest T3-2 workload-reactivation candidate release;
    the certified finite atlas;
    the embedded labelled reaction-count Green theorem;
    the aggregate workload-debt theorem;
    the actual-target carrier theorem;
    the source-layer hierarchy;
    the one-active finite algebra;
    the independent adversarial audit

        /mnt/data/T3-2_workload_reactivation_adversarial_audit.md.

The latest candidate release is not certified.

The exact unresolved interfaces are:

1. a coordinate may be tight under the escaping occupation while having
   infinite support, so it cannot simply be inserted into one fixed finite
   phase;

2. rare slower reactions may create high-workload excursions followed by
   arbitrarily many fast workload-neutral jumps, so bounded slower-event
   count or zero reward on a fast trace does not by itself exclude escaping
   occupation of the raw embedded chain.

Resolve exactly one of:

### Outcome T3-2-CERT

Every finite bimolecular weakly reversible stochastic mass-action network
with:

    at most three dynamically active species;
    at most two active linkage classes;
    arbitrary positive reaction-rate constants;

is positive recurrent on every closed communicating class.

Equivalently, every closed irreducible class admits a unique stationary
probability distribution.

### Outcome C3

Give an explicit network satisfying those hypotheses, with positive
rational rates and an infinite closed irreducible class that is transient
or null recurrent.

Do not create a theorem manuscript until every gate below is closed.

## 1. Preservation and claim discipline

Preserve unchanged:

    the three independently reproduced two-active atlases;
    the deficiency-zero certificates;
    the two service architectures;
    the corrected service endpoint W_tau<=W_0-1;
    the actual-current-target residual identity;
    both conditional-activation counterexamples;
    the aggregate-debt scalar inequality;
    the exact finite Green/Poisson/Dirichlet tools;
    the existing source-degree checks.

Create a new project:

    /mnt/data/t3_2_positive_workload_final/

At project start state explicitly:

    the finite atlas is certified;
    the scalar debt lemma is certified;
    the global T3-2 theorem is not certified;
    the fixed-finite-environment and raw-embedded-return arguments are
    withdrawn.

Do not perform a priority audit before T3-2-CERT or C3.

## 2. Certified inherited facts

Treat the following as established only at their stated scopes.

### 2.1 Actual-current-target identity

If the current actual reaction target is t and the next physical reaction
has source s and target u, then

    Delta V
      =
    log[(X)_t/(X)_s]

for the marked residual-factorial potential.

A target-following episode may begin only from a target produced by an
actual reaction.

### 2.2 No conditioning on future activation

The proof may not wait for a selected future reaction, condition on it,
and then treat the following target path as uniformly negative.

Retain as mandatory regressions:

    0 -> 2A -> A -> 0;

and

    A <-> 2A,
    0 -> A+B -> B -> 0.

### 2.3 Aggregate physical workload debt

For an integer physical workload H and an excursion baseline B, define

    D=(H(X)-B)^+.

At service-trial endpoints,

    D_{k+1}
      <=
    (D_k-S_k)^+ + A_k.

If, on D_k>0,

    P(S_k>=1 | F_k)>=p,
    E[A_k | F_k]<=a<p,

then

    E tau_{D=0}
      <=
    D_0/(p-a).

No independence or genealogy is required.

### 2.4 Actual-target carrier service

A positive workload reaction creates an actual physical target. A
same-linkage return prefix from that target yields cancellation, service,
or chart exit.

The return may cancel the positive event exactly. Cancellation is not
strict descent.

### 2.5 Finite structural classification

The two-active atlas, common-invariant branch, deficiency-zero branch and
service architectures are retained without modification.

## 3. Explicitly withdrawn arguments

Do not use any of the following.

1. Tight inactive-coordinate marginals imply one fixed finite support box.

2. A box carrying 1-epsilon of an occupation may be treated as exact after
   sending epsilon to zero without a uniform error estimate.

3. A finite carrier phase may record the exact inactive-coordinate
   population when that coordinate has unbounded support.

4. Bounded expected count of slower reactions implies that slower reactions
   cannot support an escaping occupation.

5. Positive recurrence of the raw embedded jump chain is required for CTMC
   positive recurrence.

6. A zero-workload fast trace excludes high-shell occupation created by rare
   slower transitions.

7. Finite expected physical duration implies finite expected ordinary jump
   count.

The final proof should be built in physical time.

## 4. Central replacement:
## strictly positive lexicographic workload

The chart workload used for source-rate comparisons may have zero
coefficients on nominally inactive coordinates. That workload is not proper.

Construct a proper refinement.

### Positive Lexicographic Scalarization Theorem

Fix a complete physical source-rate flag and all finite workload
comparisons used in the chart proof.

Construct rational vectors

    h^(1),...,h^(r),

with r<=3, such that:

1. h^(1) is the primary chart workload already used by the source-layer
   and atlas arguments;

2. the later h^(j) refine only ties left by the previous vectors;

3. for every pair of source complexes, the first nonzero comparison in

       (h^(1).y,...,h^(r).y)

   agrees with the complete source-rate priority required by the proof;

4. every species coordinate has a positive coefficient in at least one
   h^(j);

5. the joint sublevel sets

       {x:
          h^(1).x<=n_1,
          ...,
          h^(r).x<=n_r}

   are finite;

6. every reaction increment has a bounded integer lexicographic reward
   after clearing denominators;

7. strict signs of primary workload-changing reactions are preserved;

8. source ties may be refined, but no strict source-rate comparison is
   reversed.

Preferably encode this finite lexicographic system into one positive integer
workload

    H_*(x)=h_*.x,

with

    h_{*,i}>0

for every species, while preserving every relevant reaction sign in
lexicographic order.

If one fixed scalar cannot preserve the required ordering globally, use the
finite tuple and prove the multilevel Foster theorem in Section 11.

This theorem is load bearing.

Its purpose is:

    every H_* shell is finite;
    no coordinate is hidden in an infinite “bounded environment” phase.

## 5. Gate E1:
## environment truncation is no longer an assumption

Rebuild every chart using the positive lexicographic workload.

Do not require an inactive coordinate to have finite support.

For any coordinate formerly called inactive, prove exactly one of:

1. its contribution is controlled by a lower-priority workload component;

2. growth of that coordinate moves the state to a chart of lower structural
   rank or larger active set;

3. the coordinate participates in an affine invariant;

4. the coordinate is genuinely bounded on the closed communicating class.

A tight but infinite-support environment must be handled by the workload,
not inserted into a finite phase.

### Finite shell phase

For one fixed proper-workload shell or finite joint shell, the physical
population state set is finite.

Only at that point may the phase record exact coordinate values.

All finite carrier, Green and Poisson calculations must be shellwise on
this genuinely finite state set.

## 6. Gate E2:
## uniform truncation-or-promotion theorem

Prove a theorem that replaces the previous terminal finite-box claim.

### Tight-Environment Alternative

For every divergent sequence of states or escaping physical-time
occupation, and every nominally lower-priority coordinate, exactly one of:

#### E2.1 — uniform local control

There is a finite range on which the service probability, arrival bound and
episode-duration estimates have a strict margin that survives the omitted
tail.

#### E2.2 — promotion

A positive fraction of the relevant workload or physical-time occupation
leaves every finite range of that coordinate. Promote the coordinate to the
next workload component or active chart.

#### E2.3 — lower-rank exit

Growth of the coordinate changes source support, capped availability,
linkage activity or source-rate flag and enters a lower-ranked chart.

#### E2.4 — affine invariant

The coordinate is controlled by an affine invariant positive on the alleged
escape direction.

Do not infer E2.1 merely from tightness.

If using an epsilon-box argument, supply:

    the exact tail error;
    the strict negative margin;
    a choice of box for which tail error is smaller than that margin;
    proof that enlarging the box does not destroy the margin.

If this cannot be done, use E2.2.

## 7. Gate E3:
## environment-free carrier theorem

Rebuild the physical carrier theorem without assuming one globally finite
environment phase.

For each proper-workload shell:

1. the exact state set is finite;

2. build the carrier phase from:
       actual target;
       linkage;
       path position;
       capped availability;
       exact shell state;

3. compute or bound the service probability p_x;

4. compute the slower-arrival mean a_x;

5. prove that, outside one finite global exceptional set,

       a_x<p_x;

6. prove that the inequality is stable under every source-rate cell.

### Sequence-to-uniform closure

If no finite exceptional set exists, choose a divergent bad sequence with

    a_x>=p_x.

Compactify its complete source-rate flag.

Use actual-target priority service and the carrier-race theorem to derive:

    a strict service margin;
    source-flag refinement;
    coordinate promotion;
    structural exit;
    affine invariance;
    deficiency-zero structure;
    certified service architecture;
    or Outcome C3.

This contradiction must produce the finite exceptional set required above.

Do not assume a shell-independent p in advance.

Shell-dependent positive p_x is allowed, provided the resulting physical
duration is handled by the shell-adapted theorem.

## 8. Gate E4:
## physical-time effective trace

Define stopping times using physical time, not ordinary reaction count.

A trace event may be:

    completion of an aggregate-debt excursion;
    unpaired service;
    proper-workload shell descent;
    workload-component promotion;
    source-flag refinement;
    structural rank decrease;
    entry into an invariant/product-form/service branch.

Fast neutral reactions occurring between trace events are skipped.

Prove:

### Physical-Time Trace Theorem

For every starting state outside a finite set, an adapted trace stopping
time tau_x satisfies one of:

1. the proper workload or lexicographic workload decreases;

2. the structural rank decreases;

3. the process enters a certified positive-recurrent branch;

4. Outcome C3 occurs.

Additionally:

    E_x tau_x<infinity.

No bound on the expected number of skipped ordinary jumps is required.

Prove the trace is strong-Markov compatible.

If the neutral process has a closed component preventing the next trace
event, prove that component has an affine invariant, product-form stationary
probability, or is a lower-dimensional certified branch.

## 9. Gate E5:
## trace-cost control

The prior proof did not control occupation generated by many fast neutral
jumps after a rare high excursion.

Avoid this problem by proving recurrence in physical time.

For each finite proper-workload shell S_n define

    T(n)
      =
    1+
    max{
       E_x tau_x:
       x in S_n
    }.

The maximum is finite because S_n is finite and Gate E4 supplies finite
physical means.

Do not include expected ordinary jump count in T(n).

If using a workload tuple, define the analogous shell cost on each finite
joint shell.

Prove endpoint integrability and bounded reaction overshoot.

A path may visit higher shells before tau_x; only the endpoint and physical
duration are needed, but every endpoint moment required by the potential
must be justified.

## 10. Gate E6:
## genuine descent after debt clearing

Aggregate debt clearing merely returns the workload to its excursion
baseline.

At zero debt prove:

### Baseline Descent Theorem

From every sufficiently large state, after finitely many zero-debt trials,
one of:

1. an unpaired negative transition lowers the proper workload by at least
   one integer unit;

2. a certified service architecture gives

       W_tau<=W_0-1;

3. the structural rank decreases;

4. another workload component is promoted;

5. an affine invariant or deficiency-zero branch applies.

The mean physical duration is finite.

Failed trials may create debt. That debt must be cleared before restarting.

The success probability may be shell-dependent, but must be positive at
every starting state and must yield a finite shellwise mean.

## 11. Gate E7:
## shell-adapted physical-time Foster theorem

State and prove a self-contained theorem.

### Scalar version

Let X be an irreducible nonexplosive countable-state CTMC, let K be finite,
and let H:X->N_0 be proper.

Suppose that for every x outside K there is an adapted stopping time tau_x
such that

    H(X_{tau_x})<=H(x)-1,

    E_x tau_x<infinity.

Since every H-shell is finite, set

    c(n)
      =
    1+
    max{
       E_x tau_x:
       H(x)=n
    },

and make c nondecreasing.

Define

    U(x)
      =
    sum_{j=0}^{H(x)}c(j).

Prove:

    E_x[U(X_{tau_x})-U(x)]
      <=
    -c(H(x)),

and

    E_x tau_x
      <=
    c(H(x)).

Then prove:

    finite expected physical hitting time of K;
    finite expected positive return to a selected state;
    positive recurrence.

Do not use expected embedded jump count.

### Lexicographic version

If no single proper scalar H is available, prove the corresponding theorem
for a finite workload tuple and a finite structural rank.

The construction must be explicit and must have finite sublevel sets.

Every rank-changing seam and workload-component switch must be included.

## 12. Gate E8:
## trace-to-CTMC theorem

Prove directly that physical-time trace recurrence implies CTMC positive
recurrence.

Required steps:

1. finite expected physical hitting time of a finite set;

2. construction of the CTMC trace on that finite set;

3. irreducibility of the finite trace;

4. finite expected physical duration of each finite-set excursion;

5. finite expected return to one selected state;

6. existence and uniqueness of the stationary probability.

Nonexplosion must still be proved independently from:

    at-most-linear aggregate rate of population-increasing reactions;
    finite-shell boundedness of all other rates.

Do not claim positive recurrence of the raw embedded jump chain.

## 13. Gate E9:
## one-active-coordinate repair

Rebuild the one-active theorem using the proper positive workload and
physical-time trace.

Let A be the primary active coordinate.

### Quadratic case

If 2A is a source, prove the complete drift inequality including every
reaction.

### Linear case

If 2A is absent:

1. construct the degree-one finite trace shellwise;
2. solve its exact Poisson equation;
3. retain degree-zero births at natural rates;
4. use actual targets and aggregate debt;
5. handle repeated births before service;
6. allow bounded coordinates to have infinite support globally by placing
   them in lower workload components;
7. conclude proper-workload descent, promotion, or invariant.

The exact service-token invariant

    A-sum_{D in K}D

may be used only after checking every original reaction.

A zero-mean, nonzero-variance phase must receive a Lamperti audit rather
than being called an invariant.

## 14. Gate E10:
## two-active atlas interface

Keep the certified atlas unchanged.

For each chart:

### Available linkage

Use Gates E1–E7.

### Shielded linkage

Use the certified classification:

    positive-active affine invariant;

or

    signed one-active reduction.

The signed reduction must be combined with the repaired one-active theorem,
not a fixed finite environment assumption.

### Shielded–shielded pair

Use:

    common invariant;
    deficiency-zero product form;
    one of the two service architectures.

### Distributed occupation

An occupation alternating among different faces must be split through the
finite workload/rank trace.

Inter-chart transitions remain physical trace events and cannot be lost in
an epsilon-box limit.

## 15. Gate E11:
## independent occupation verification

As an independent route, formulate the physical-time trace as a
semi-Markov or random-time occupation process.

If the Foster theorem failed, derive an escaping normalized trace
occupation whose average physical-time cost is finite and whose workload
drift is nonnegative.

Use the service-capacity inequalities to show its drift is strictly
negative.

This verification should not normalize the number of fast neutral jumps.

It must independently exclude the prior obstruction:

    rare promotion to a high shell
    followed by extremely many neutral embedded jumps.

## 16. Mandatory regressions

The repaired theorem must pass all of the following.

### 16.1 Tight but infinite environment

Construct a positive-recurrent example in which one lower-priority
coordinate has a Poisson-like infinite-support stationary distribution.

The proof must not replace that coordinate by one fixed finite phase.

### 16.2 Rare high excursion with many neutral jumps

Construct a system in which a slow reaction enters a high shell and a fast
conservative linkage makes a very large number of neutral jumps before
return.

The physical-time trace must remain integrable even if expected embedded
jump count is large.

### 16.3 Conditional activation failures

Retain both exact positive conditioned blocks.

### 16.4 Pure cancellation

A positive event and its return prefix may have zero net workload.

### 16.5 Multiple arrivals

Several positive events may occur before service. Aggregate debt must handle
them without genealogy.

### 16.6 Weighted zero layer

    H <-> 2L,
    0 <-> L.

### 16.7 Delayed drain

    0 -> A+B -> B -> 0.

### 16.8 Available–shielded pair

Use each signed one-active shielded mask.

### 16.9 Critical finite-phase chain

Zero mean with nonzero variance must be classified correctly.

### 16.10 Extreme rate separation

Use positive rational rates spanning at least twenty orders of magnitude.

## 17. Counterexample track

Search specifically for failure of the physical-time trace theorem.

Prioritize networks with:

    a slow outward reaction;
    a high-shell fast neutral subsystem;
    finite physical return but infinite embedded jump return;
    or genuinely infinite physical return;
    tight infinite-support cofactors;
    no affine invariant;
    no deficiency-zero structure;
    no certified service architecture.

Distinguish carefully:

    embedded-chain null recurrence;
    CTMC null recurrence;
    CTMC positive recurrence with an embedded-chain pathology.

Only the second is Outcome C3.

## 18. Computational deliverables

Create:

    /mnt/data/t3_2_trace_cost_final/
        STATUS.md
        theorem_statement.md
        technical_summary.md
        proof_audit.md
        independent_proof_reconstruction.md
        README.md

        manuscript/
            main.tex
            main.pdf
            references.bib

        src/
            positive_workload.py
            lexicographic_scalarization.py
            environment_promotion.py
            shell_phase.py
            carrier_capacity.py
            physical_time_trace.py
            trace_duration.py
            baseline_descent.py
            shell_foster.py
            finite_set_trace.py
            one_active_trace.py
            two_active_interface.py
            trace_occupation_dual.py
            counterexample_search.py
            independent_verifier.py

        certificates/
        examples/
        tests/
        failed_approaches/

The independent verifier must reconstruct:

    workload scalarization;
    finite joint shells;
    environment promotion alternatives;
    shellwise service margins;
    physical-time trace kernels;
    expected duration bounds;
    baseline descent;
    Foster inequalities;
    finite-set return;
    atlas interfaces.

It must not import the discovery implementation.

## 19. Acceptance criteria for T3-2-CERT

T3-2-CERT is accepted only if:

1. every coordinate is controlled by a positive workload component;

2. joint workload sublevel sets are finite;

3. tightness is never identified with finite support;

4. service margins survive environment truncation or force promotion;

5. no globally finite inactive-coordinate phase is assumed;

6. aggregate debt clears in finite physical mean;

7. zero debt is followed by genuine descent;

8. fast neutral jumps are handled through physical time, not jump count;

9. no proof of raw embedded-chain positive recurrence is required;

10. the physical-time trace has finite shellwise expected duration;

11. the shell-adapted Foster theorem is stated and proved completely;

12. the one-active theorem is rebuilt under the new environment treatment;

13. the two-active atlas is interfaced without an epsilon-box gap;

14. distributed face occupations retain all inter-chart flow;

15. CTMC positive recurrence follows directly from physical return;

16. nonexplosion is proved separately;

17. the conditional-activation and cancellation regressions pass;

18. a fresh independent adversarial audit finds no hidden finite-support or
    embedded-jump assumption.

## 20. Hard stop

If neither T3-2-CERT nor C3 is established:

1. do not create a theorem manuscript;

2. preserve the finite atlas, debt theorem and actual-target results;

3. state the smallest remaining physical-time trace theorem;

4. do not return to colored genealogies;

5. do not return to raw embedded-chain recurrence;

6. do not proceed to three linkage classes;

7. do not perform a priority audit.

## Final directive

The remaining issue is not another reaction-network classification.

It is a recurrence-interface problem.

Make every coordinate part of a proper finite-rank workload. Use the
aggregate-debt and actual-target mechanisms to obtain physical-time descent
of that proper workload. Skip fast neutral reactions rather than charging
them as embedded steps. Build a complete shell-adapted random-time Foster
theorem and conclude recurrence directly for the CTMC.

Either close that physical-time trace bridge or use its failure to construct
an exact CTMC counterexample.
```

The essential shift is:

[
\boxed{
\text{raw embedded return count}
;\longrightarrow;
\text{proper workload descent in finite expected physical time}.
}
]

That is the most direct way to repair both remaining defects simultaneously.
