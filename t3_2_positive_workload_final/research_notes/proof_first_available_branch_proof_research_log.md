# Available-branch proof research log

## 2026-08-12 PDT

- Replaced orientation/history search by the exact marked Bellman identity.
- Proved that the Q/U/C available path can be run without cap or chart exit:
  its successful endpoint (x-t+c) literally enables the comparison source.
- Isolated the only remaining one-active obstruction as a Flat0-marked B/F0
  countable inactive phase.
- Found that a uniform physical-duration bound over the inactive start is
  false (pure death gives a logarithmic hitting time).  Recorded the sharp
  (O((1+\log(2+v))^m)) moment bound, which is sufficient in a one-active
  tier.
- Drafted the cap-free B/F0 theorem.  Its key operator is the exact marked
  factorial weight on the countable phase.  A maximal source has at most one
  weight-neutral jump; the next maximal-source jump contracts, and every
  lower-source interruption has a power-small weighted contribution.
- Kept the closed no-kill alternative classwise: irreducibility forces it to
  be the whole fixed class, and a one-species binary Foster function proves
  recurrence directly.
- Replayed the corrected S-superlevel definition after initially flagging a
  possible same-active-degree descent.  For a one-active descriptor all
  inactive primitive tier weights are zero, so its D-level is exactly active
  degree.  The certified first edge exits the entire degree superlevel and
  therefore strictly lowers active degree.  The initial concern applied only
  to arbitrary descending edges and is inapplicable to the certified edge.
- Independently replayed and froze the exact 18,496-pair wrapper audit.  Its
  11,842/6,654 selector is disjoint at the immutable support-pair level, so
  the two branch potentials are never compared along a trajectory.  The
  certificate suites gave 10 passing tests, and the target rendered cleanly.
- Independently replayed the final 46,872-pair certificate and every cited
  branch scope.  The five branches are pairwise disjoint and exhaustive; the
  final theorem, nonexplosion argument, and publication render received a
  strict exact-byte pass.
- Independently audited the final global fixed-class reduction.  Physical
  path lifting makes deleted-coordinate stoichiometry constant on each
  active linkage, so its factorial factor is an exact positive rate
  multiplier.  Projection is injective within an active support, projected
  collisions merge strongly, and the image class is CTMC-conjugate to the
  original class.  This gives a literal zero-/one-/two-linkage routing with
  no Lyapunov-family seam; the global theorem received a strict exact-byte
  and render pass.
