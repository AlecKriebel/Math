# Retracted pre-mark objection to the both-available seam

**Diagnostic correction, 2026-08-12 PDT.**

An earlier draft of this note incorrectly tested the inherited “available”
classifier before introducing the actual target mark.  That objection is
retracted.

The relevant episode starts from the target \(t\) of the preceding physical
reaction.  Pathwise \(x\ge t\), so \(t\) is an enabled source even on a
C-type face where the classifier's top cofactor source is disabled.  Strong
connectivity gives a target-following path from \(t\) to a lower complex
\(c\).  The symbolic Q/U/C classifier bridge then produces a faster complex
\(q\) with \(q_C\le c_C\), so \(q\) is enabled at the successful endpoint
and \(p_c\to0\).

The exact C--C diagnostic

\[
 L_1=\{0,C,A+C\},\qquad L_2=\{2A,2C,B+C\},
 \qquad h=(1,3,0),\qquad C=0
\]

therefore does not obstruct the marked theorem.  The possible carried
sources on that face use the physical paths

\[
 0\to C\to A+C,\qquad 2A\to B+C\to2C.
\]

At the two endpoints, respectively \(B+C\) and \(2A\) are enabled faster
sources.  The proposed pre-activation access word failed, but the marked
current-target partition succeeds.

The corrected proof and its independent exact-byte audit are
“proof_first_both_available_current_target_theorem.md” and
“proof_first_both_available_current_target_independent_audit.md”.

This file is retained only so the abandoned pre-mark objection cannot be
mistaken for a live dependency.
