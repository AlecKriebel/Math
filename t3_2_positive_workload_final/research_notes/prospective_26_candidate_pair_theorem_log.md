# Research log: prospective 26-pair candidate theorem

## 2026-08-10 20:00 PDT — scope reconstruction

Replayed the claim-neutral 26-pair selector.  Confirmed 30 one-active and
94 all-active failed incidences, with no two-active failure.  Detected that
the older selector note referred to Proposition 5.2 as a discrete factorial
statement although the proposition is literally written for continuous
rate-adjusted entropy.  Treated the discrete comparison as a new proof
obligation.

## 2026-08-10 20:20 PDT — exact one-active routing

Reduced the 30 rows to 15 normalized support profiles.  All have inactive
caps `(0,0)` and two mixed linkages.  The exact split is 20 pure-active-source
direct rows, eight Family-III resistance-zero origin-service rows, and two
frozen/no-history rows.  Verified that the graph proof is orientation-free
and that the aggregate killed-kernel proof is unchanged for an arbitrary
fixed factorial linear correction.

## 2026-08-10 20:45 PDT — powered all-active repair

Derived the discrete same-state reversible decomposition.  The top drift is
negative entropy dissipation plus an `O(beta)` finite-shift error; its
second through fourth jump moments are bounded by dissipation plus
`O(beta)`.  The curvature-cofactor premise is precisely the estimate
`alpha/x_i = O(beta)`.  Combined this with the lower-linkage logarithmic gap
and the exact fourth-power binomial identity to absorb the carré and Taylor
terms.

## 2026-08-10 21:05 PDT — candidate composition

Composed passing descriptors, the 94 powered all-active descriptors, and
the 30 one-active descriptors using one rate-adjusted corrected factorial
potential and the existing all-species reflected-debt theorem.  Kept all
analytic, pair-recurrence, and global flags false pending the independent
audit.  Added focused exact-support/hash tests and began final control scans.

## 2026-08-10 21:25 PDT — audit repairs and pair arithmetic

Repaired the moving-boundary linear-correction charge to
`O(L_n+J)` and invoked the endpoint-weighted moment bound above order eight;
nested active entries are not pathwise bounded by inactive mass alone.
Removed a stray control character in the powered moment display.  Froze the
claim-neutral disjoint arithmetic: `(759,36,795)` prospective parent,
`(26,0,26)` selected with zero prior-certified overlap, and `(733,36,769)`
remaining, without changing any certification flag or authoritative count.

## 2026-08-10 21:45 PDT — independent audit passed

The independent final audit returned PASS.  Promoted only the two scoped
analytic flags and the exact 26-pair recurrence flag; the global T3-2 flag
remains false.  The audited branch has zero overlap with prior-certified
pairs and changes the authoritative remainder from `(759,36)` to
`(733,36)`, total 769.
