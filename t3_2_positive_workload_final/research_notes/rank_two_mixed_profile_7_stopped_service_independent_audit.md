# Independent audit: stopped service for the final rank-two seven

Audit timestamp: 2026-08-10 PDT.

Frozen finite payload and row fingerprints:

    payload 15920ec2ab510bab87b4e4b778cd998f7bbf7622b606bda383014dd5d6add2e3
    rows    aefff460ba993878d2961f752463cd2acd87c677fbba43cfc05408523943b98c

Audited files after the mechanical equation-tag render fix:

    theorem note  9f8622ae324ac1ea099a75dca834bbacafadd353e311cb8d20bf35d299ca00a1
    certificate   bee3003a034763efb2958ab1410a59d6bde258c1e5590925d9acdbf6baac1366
    focused tests 929b54c732d6f988546192d4307bf63c150b0760d809b5e86357104baeb0e38c

## Verdict

**PASS at the bounded seven-pair descriptor-local scope.**  No exact
orientation, rate, stopping-endpoint, or population-bookkeeping
counterexample was found.  The result remains claim-neutral in the executable
snapshot: this audit does not itself change any analytic, recurrence, pair, or
global flag.

## Localized activation

For each dormant chart, killing carrier edges which enter inactive
quadratics leaves a transient two-state carrier generator.  The resolvent
vector `q=(-Q)^(-1)1` and sufficiently small positive perturbation
`v=1-epsilon q` give strictly positive rate-weighted reward at both carrier
sources.  Falling-factorial propensities therefore give the stated

    L_T R >= c X R - K R^2,

with quadratic variation at most `C n R` in the localized wedge.  The
exponential `exp(-theta R)` survival estimate and the logarithmic drift away
from the finite lower phase are compatible with every positive fixed rate
vector; constants need not be rate-uniform.

As an independent finite stress test, all 569,898 strongly connected directed
graphs on the four carrier menus were enumerated.  Every carrier-killed matrix
had a positive determinant and every nonempty carrier subset had a directed
exit.  This includes all sparse and bidirected graphs, not only complete or
Hamilton-cycle orientations.

The fast-trial duration has an exponential tail on the `log(n)/n` scale.
Applying the stopped counting-process exponential martingale to the constant
birth clock gives an extra-birth exponential moment tending to one.  Combined
with the fixed positive trial success probability, this supplies a genuine
compound-geometric exponential moment for the total activation birth count
`K`; it does not use the withdrawn pure-geometric shortcut.  The same
localized time estimate and the death counting martingale make activation
downward shell exit superpolynomially rare.  Upward exit requires order-`N_0`
births and is exponentially rare.

## Fluid service and exact counts

On the service-zero face, every interior point enables all present
service-free sources, and strong connectivity forces a cut to a
`C`-containing target.  At a pure vertex with its quadratic source present,
that source either creates `C` or first creates the other carrier; the
interior cut then applies.  The only invariant face points are exactly the
eight certified dormant vertices.  The local PF logistic inequality excludes
approach to each such vertex from a nonvertex orbit.  Barbalat's argument and
compactness of the activation shell therefore give a uniform finite horizon
with arbitrarily large integrated service.

The window death count is controlled through its random compensator, not by a
false fixed-Poisson domination.  The pathwise bound on that compensator and
the constant-clock birth moment give uniform exponential moments.  Uniform
finite-horizon fluid convergence then yields the event-weighted lower bound
on service deaths after the exponentially small window-shell event is
removed.

The population identities are exact and exhaustive:

    N_a   = N_0 + K - D_pre,
    N_end = N_0 + K - D_pre + B_win - D_win.

On the regular event, `E[K;R] <= E K` and the truncated service lower bound
give negative mean for `K+B_win-D_win`; no conditional estimate for `K` is
used.  Activation up/down, service-window up/down, and large-`K` complements
cover every shell endpoint.  Pre-activation deaths are never removed from the
dynamics and only improve the total-population upper envelope.

## Scalar, moments, and gluing

The deterministic envelope by total population retains every top reaction
and every possible factorial reconcentration.  Its initial multinomial gap is
`O(N_0^4 log^3 N_0)`, strictly below the negative sixth-power contribution of
order `N_0^5`.  The regular-event sixth-power Taylor expansion is justified by
the uniform exponential count moment.  Downward shell exits have a
deterministic order-`N_0^6` gain; upward shell costs are polynomial and are
absorbed by their exponential probabilities.

The same count tails give endpoint and physical-duration moments of every
fixed order, hence the required choice `q>8`.  Outside the dormant wedges,
the all-active service margin and the `C`-bounded coercive-source estimate
close the pointwise drift.  The stopped wedge estimate and pointwise-good
estimate use the identical globally proper scalar.  Since total population
is bounded above by its initial value plus the constant-rate birth Poisson
process, the chain is nonexplosive and the common-potential physical-time
gluing theorem applies classwise.

## Replay and render

The stopped-service, selector, and scalar-obstruction suites pass 16 tests in
total, and the certificate reproduces the exact seven pairs, eight dormant
rows, finite row hash, payload hash, and false flags.  After moving equation
tags (3.1) and (6.11) outside their `split` environments, Pandoc plus Tectonic
produces an 11-page PDF.  Tectonic still reports one overfull box and a font
warning for `\setminus`; these are minor typesetting cleanup items, not
analytic defects.
