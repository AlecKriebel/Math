# Common-catalyst operational macro research log

## 2026-08-12 PDT

The proof was rebuilt around the exact factorization

\[
  \mathcal L_T=Y\mathcal L_Q,
  \qquad T=\{X+Y,Y+Z,2Y\},
  \qquad R=\{0,Y,Z\}.
\]

The decisive repair was to stop treating individual lower deaths as failed
activation attempts.  Complete operational attempts retain every lower and
birth clock, and all earlier births are charged only through one expected
complete ledger.  The labelled-particle construction allows lower
\(Z\to Y\) transfers (helpful) and \(Z\to0\) deaths (charged); every
\(Y\)-sourced lower event and birth ends the current active block at its
actual endpoint.

The prelude has a uniform geometric success probability and uniformly
bounded expected birth debt.  Its endpoints are a fractional workload
return, a direct-death-rich block, or a macroscopic catalyst shell.  On the
shell, the top fluid equation is a time change of the irreducible linear
chain generated by \(Q\); a physical window of order \(1/H\) supplies any
fixed expected direct-death service while adding \(o(1)\) expected births.
This yields the exact expected ledger and physical-time macro required by
the frozen workload-only Foster theorem.

The first written draft exposed two correctable defects during replay: the
fractional-return stop had been included in the literal clean-block event,
and the successful-label constants did not absorb the single distinguished
catalyst.  The frozen theorem separates the unspoiled event from the clean
endpoint and uses \(\alpha=p_*/8\), \(\rho=p_*/4\).

Frozen theorem:

~~~text
research_notes/proof_first_h111_common_catalyst_operational_macro_theorem.md
SHA-256 3662b346d9efe30cff1075fdab000c6fc0a749ce0bbbdc1b7c2b2719e2625dd5
469 lines / 17,970 bytes
~~~

Pandoc/MathJax and letter-paper Tectonic render checks passed.  The PDF has
six pages and no overfull, underfull, undefined-reference, or missing-glyph
diagnostic.  Exact bytes were handed to an independent hostile auditor.
