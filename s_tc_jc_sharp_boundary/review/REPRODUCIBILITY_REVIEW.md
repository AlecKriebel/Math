# Reproducibility review

## Verdict: PASS

The publication supplement starts from primitive graph/event definitions for
the cycle and four theta cores. It regenerates canonical ported topologies,
displayed-tree descendant-mask tensors, exact invariant signatures, and all
directed source-target pair universes. The exact theorem release separately
rebuilds the 192 residual seven-port records, all 1,686 completed targets,
root-reduction checks, the 177 endpoint and 453 crossing tensor types, the 547
pointwise-cut Bernstein expansions, the Theta common point, and the final
dependency closure.

The new automatic-triangle theorem has two separate finite implementations.
A dependency-free Python program starts from `K4` minus one edge and enumerates
all internal/external root sites, reticulation pairs, and edge orientations. A
separately written C++ program independently obtains 25 binary acyclic rooted
presentations, seven graph-symmetry orbits, and zero tree-child cases. The
structural proof reducing every multi-triangle level-2 blob to this core is in
the paper and is independently summarized in
`review/MULTITRIANGLE_STRENGTHENING_REVIEW.md`.

A separately implemented C++ reviewer recomputes the five- and six-port
directed joins without importing the primary join code. Separate Python
reviewers regenerate the seven-port completion, root reduction, pointwise cut
theorem, final synthesis, and publication-scope assertions. The clean quick
and full transcripts record the exact command sequence.

SHA-256 files are used only to control release integrity. Mathematical claims
are checked by regeneration and exact arithmetic.


## Version 1.1.1 correction replay

The corrected release rebuilds the manuscript after narrowing Theorem 9.3 to
structural reconstruction modulo ordinary triangle redirection.  The
publication reviewers now reject the superseded pointwise-compatibility
phrases and require the structural-versus-pointwise caveat.  The bibliography
check also requires the Ardiyansyah 2021 level-2 variety paper.  PDF preflight
and visual review were rerun from the rebuilt bytes; the prior visual report,
which missed visible overlaps, is not retained as evidence.
