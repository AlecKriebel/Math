# Research log

## 2026-08-21 13:27 PDT — promotion-form assembly

- Re-audited the global analytic proof against the independent adversarial
  ledger.
- Incorporated the marked/unmarked \(C/T\)-scale proof and isolated the true
  bivalent stabilizer.
- Bound the graph exclusion of the \((1,2,2)\) \(K_4-e\) component to the
  exact 25-rooting/zero-tree-child replay.
- Used an interior physical bridge split, the paired marginal section, the
  fixed-full restoration implication, the retained-arrowhead root argument,
  contextual triangle rank, incidence-correspondence genericity, the
  structural reconstruction quotient, the two-section continuous-time
  inequality, and both sides of the cherry dimension argument.
- Replaced stale finite counts with the authoritative raw-four,
  \(\theta_2\), cycle, and corrected 997-parent restoration censuses.
- Added an explicit full-probe placeholder and a guard that cannot pass on a
  one-port checkpoint.
- Left `work/final_theorem_release` unchanged.

Best-guess completion toward an unconditional theorem package: **97%**.  The
remaining 3% is the load-bearing corrected two-port completion plus its
independent replay/mutation freeze and one-pass insertion of exact hashes.

## 2026-08-21 13:39 PDT — adversarial manuscript pass

- Verified 23 frozen input files byte-for-byte and checked every available
  embedded payload hash; the frozen-only guard returned `PASS_FROZEN_ONLY`.
- Confirmed the authoritative finite partitions sum exactly to 405,216 raw
  four-port rows, 2,946,240 \(\theta_2\) rows, 13,440 cycle roots, 536,364
  cycle children, and 36,824 corrected restoration edges.
- Added the exact 934-class terminal census and the independently replayed
  22-quintic/12-quartic/2-cubic direct overlay; mutation bindings reject
  reassignment by degree family.
- Expanded the weak-sharpness proof to give the two exact graph presentations,
  rational strict continuous-time parameters, common tensor, rank witnesses,
  and both the lower and upper cherry-dimension bounds.
- Checked balanced display-math delimiters, valid JSON, valid Python syntax,
  and a clean whitespace diff.
- Confirmed the full guard exits nonzero with `PROMOTION_BLOCKED` and lists
  every still-pending probe field.

Completion estimate remains **97%** because none of these checks replaces the
unfinished two-port classification.

## 2026-08-21 15:55 PDT — unconditional promotion

- Froze the corrected full probe at primary file SHA
  `6c228e0d495fbc68b1df4ddb571558425a35e081cc22b954f232f8a3458991d3`
  and semantic payload
  `674853fa730c4f54b9ba264d539a51591c8b926ad444195e68df086c26f83825`.
- Independently reconstructed 176 anchors, 29,964 one-port rows, 2,107
  two-port parents, and 544,571 two-port rows.  The clean-room graph/full-map
  audit payload is
  `65160636abfa33de47136a222081ac70bd7b6fae0e029b7a7c379e2d8653df74`.
- Confirmed 32,729 reverse marginals, 1,760 coherent two-port triangle
  equalities, zero new triangles, zero unresolved/incoherent records, and
  12/12 targeted mutations rejected.
- Filled every machine probe value, removed only the now-discharged
  computational conditions, retained every `D_plus`, regularity,
  strong-tree-child, genericity, and exact-data hypothesis, and added the
  final probe hashes/census to Appendix A.
- Re-ran the quantifier audit and promotion guard.

Best-guess completion toward the unconditional principal-domain theorem
package: **100%**.  No mixed-sign claim is made.

## 2026-08-21 16:02 PDT — bibliographic adversarial correction

- Corrected the topology-source author initials to A. K. Englander,
  M. Frohn, E. Gross, N. Holtgrefe, L. van Iersel, M. Jones, and
  S. Sullivant.
- Retained the v4 posting date because it is locally bound by the frozen
  topology-direction certificate and theorem note, rather than inferred from
  memory.
- Re-ran the full promotion guard after the correction.

Completion estimate remains **100%**.

## 2026-08-22 12:29 PDT — probe byte-stability rebind

- A clean full regeneration showed that the probe summary's embedded runtime
  made its byte hash nondeterministic even though its mathematical payload and
  all 574,535 rows were unchanged. Runtime is now emitted only operationally;
  the stable primary certificate SHA-256 is `93de7b0d...` with the same
  payload `674853fa...`.
- The refreshed independent graph audit passed every row and all 12 mutations;
  file SHA-256 `47f527dd...`, payload `4492860f...`. The structural/algebra
  replay and promotion placeholder were rebound, and the promotion guard still
  returns the identical canonical PASS census.
- The theorem remains unconditional `K2P-SAME`. The temporary 99% completion
  estimate reflects only the fresh full replay and final submission archive.
