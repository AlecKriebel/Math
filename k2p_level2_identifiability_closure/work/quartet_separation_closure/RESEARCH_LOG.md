# Research log: convention-aware quartet repair

## 2026-08-24 — Referee defect reproduced and repaired

The printed `G/T` quartet bodies were checked against the declared
`(0,C,G,T)/(1,s,g,s)` convention and found false. The corrected `C/T` bodies
have exact pullbacks `0` versus `P(1-g_I)` and `0` versus
`2P(1-g_I)`. A symbolic Klein-four verifier and eight convention/formula
mutations now bind the printed proof to the declared spectrum.

Completion estimate for the local quartet-semantic repair: **100%**.

## 2026-08-24 — All promoted terminal references rebound

The independent streaming binder checked 4,414,710 terminal references and
888 per-layer certificate IDs in the raw4, theta2, theta2 dummy-restoration,
cycle, restoration, and probe layers. There are zero missing or dangling
certificates, all authoritative ledgers retain their prior hashes, and 11
terminal-specific mutations are rejected. No topology category, restoration
parent, probe result, rank result, or census changed.

Completion estimate toward the full post-referee submission repair: **70%**;
remaining work is canonicalizer/parameter-transport qualification, final
release rebinding, PDF QA, and the clean full replay.

## 2026-08-26 — fail-closed quartet mutation qualification

The eight semantics attacks and twelve terminal-binding/composed graph attacks
were retained, but their broad marker/hash acceptance was replaced by clean
production baselines and exact diagnostic, exception-type, and exit-code
contracts. Negative controls now prove that unrelated diagnostics, tracebacks,
imports, timeouts, signals, other exits, PASS tokens, and stale artifacts do
not qualify a rejection. Both reports use caller-owned external output,
pre-import stale cleanup, source collision guards, and atomic publication.

The final canonical semantics suite passed 8/8 in 2.93 seconds with
69,419,008-byte maximum RSS (the independent external run took 3.10 seconds
and 69,894,144 bytes). The final canonical terminal suite passed 12/12 in
36.18 seconds with 156,942,336-byte
maximum RSS. External and canonical reports were byte-identical. Refreshing
the clean baselines changed only manuscript/upstream provenance bindings and
derived certificate payloads; all six formulas, 288 transports, 21 displayed
pairs, 4,414,710 terminal rows, 888 certificate IDs, layer censuses, graph
guards, and classifications are unchanged.

Completion estimate for the bounded quartet mutation-evidence repair:
**100%**, pending release-wide resealing.
