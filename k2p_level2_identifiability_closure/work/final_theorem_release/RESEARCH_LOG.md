# Research log

## 2026-08-21 10:24 PDT — integration started (10%)

- Scoped the unified release to the principal `D_plus` theorem, strict
  continuous-time corollary, and weak-class sharpness theorem.
- Required quick/full modes, a byte-stable lock, explicit optimized-mode
  rejection, and cross-layer mutations.

## 2026-08-21 10:35 PDT — adversarial promotion revocation (25%)

- The adversarial reviewer falsified the coherent-probe topology oracle.
- Of 115 reviewed one-port rows labelled tree--sunlet, the exact relation is
  `none` for only 39, labelled-isomorphic for 36, and ordinary-triangle for 40.
- Froze `direct:raw4424` with source insertion 3 and target insertion 0 as a
  mandatory regression.
- Broadened the hard block to every tree--sunlet-derived row in the four-port,
  theta2, 997-restoration, cycle, and probe packages pending independent
  full-map pullback/restriction certification.

## 2026-08-21 11:05 PDT — fail-closed harness implementation (55%)

- Added an explicit-check, no-`assert` replay of the common ordinary-triangle
  tensor, rank-nine split, and tree--sunlet sign factor.
- Added nested-manifest validation, cross-layer semantic censuses, literal
  probe-transport restriction checks, and final-document gates.
- Added isolated quick/full orchestration that avoids writing into source work
  packages.
- Added re-signed mutations for omitted raw rows, false rank exclusions,
  missing children, wrong parents, broken transports, and reassigned
  cubic/quartic/quintic/quadratic certificates.
- Current best estimate: 55% complete toward a promotion-grade integration
  package.  The estimate may fall if the corrected topology audit changes the
  primitive ledgers materially.

## 2026-08-21 11:12 PDT — P0 partition revocation (35%)

- Mixed-first restriction replay found that 0 of the 16,974 legacy four-port
  `tree_sunlet` rows has an actual tree-versus-ordinary-sunlet restriction on
  the claimed triple.
- Revoked the complete 377,382-row topology partition and every direct,
  restoration, and probe census whose completeness depended on those rows.
- Changed the integration contract: promotion now requires full-map algebraic
  reclassification of every legacy row, regenerated downstream ledgers, and a
  truth-certificate hash embedded in each regenerated package.
- Completion estimate decreased from 55% to 35% because the central finite
  universe must be regenerated; the integration machinery itself remains
  substantially implemented.

## 2026-08-21 11:38 PDT — dynamic replacement contract (42%)

- Removed the hard-coded 997-parent promotion assumption.  Added a signed,
  path-configurable contract in which all 405,216 raw IDs are covered once,
  quartet exclusions are preserved, and restoration-parent count is derived
  from an exact class ledger whose members agree with the raw projection.
- Required a terminating forest with zero missing parent/child, zero
  unresolved records, zero cycles, and coherent transport on every edge.
- Kept the preliminary 16,702-rank/88-quadratic/184-restoration split as
  explicitly incomplete provenance only.

## 2026-08-21 11:46 PDT — full-map rescue and mutation hygiene (58%)

- Bound the adversarial whole-map rescue of all 16,974 four-port rows: exact
  strict source sign, identically zero target pullback, eight polynomial
  classes, and zero exact-isomorphism/triangle conflict.
- Bound the corresponding 2,528-row theta2 rescue: exact source zero, strict
  target sign, 85 polynomial classes, and zero conflict.
- Isolated byte replays passed in 19.746 seconds (four-port) and 71.603 seconds
  (theta2), total 91.358 seconds.  The corrected contract now requires the
  reason `full_map_Ti_strict_sign` and binds each row to its ordered truth-row
  hash; rooted tree/sunlet reasons are forbidden.
- Found and repaired a mutation-suite hygiene bug: hard-linked temporary
  clones allowed a write to reach the tracked direct overlay.  Restored its
  exact file SHA-256
  `d39ef18b8a58383c4f35aa9cc8fffc69d87928c77a27ddeb60c39b067e24fe21`,
  changed clones to independent copies, and added a `try/finally` source hash
  guard.  The regression rejected the cubic reassignment in 11.3 seconds and
  left the source byte-identical.
- Best estimate is now 58% toward a promotion-grade integration package.  The
  main remaining integration work is the regenerated raw/class/forest binding,
  restoration/cycle/probe whole-map truth gates, mutation reports, and final
  theorem-document promotion.

## 2026-08-21 12:03 PDT — restoration-forest counterexample (49%)

- The adversarial full-map audit falsified at least one of the historical 646
  restoration sign leaves.  For `s0:c407:t2794:p1230`, incoming role,
  insertion 3, triple `(1,2,4)`, all three direct `T_i` pullbacks vanish
  coefficientwise on both full five-port maps; the exact graph relation is
  nevertheless `none`.
- Revoked forest completeness and removed the old forest replay and
  missing-child test from promotion evidence.  The signed locator now requires
  a full-map repartition of all 646 rows, new algebraic obligations where
  necessary, a rebuilt forest, and replacement mutations.
- The raw-four v2 correction remains independently valid.  Its derived
  four-port parent census is recorded only as a raw class-ledger fact until the
  replacement child forest closes.
- The isolated authoritative raw-four mutation suite regenerated byte-for-byte
  in 26.800 seconds with 9/9 rejected and zero source-file drift.
- The unified blocked mutation diagnostic completed in 89.890 seconds.  All
  nine currently runnable conceptual gates rejected with zero survivors.  The
  exact absent gates are the corrected-restoration missing-child mutation,
  probe wrong-parent/broken-transport mutations, and the unified `raw4424`
  truth-oracle mutation.  Correct-baseline direct rejection output hashes begin
  `88b21ce0` (cubic), `5945d5b6` (quartic), and `d8abc637` (quintic).
- Completion estimate decreased from 58% to 49%.  This is a real finite-
  restoration gap, not an integration or performance issue.

## 2026-08-21 12:11 PDT — restoration gap reduced to eight (54%)

- Exact full-map repartition gives 606 valid sign rows and 40 zero/zero rows.
- Thirty-two zero/zero rows retain one dummy; all 256 second children close as
  248 quartet exclusions and eight whole-map `T_i` exclusions.
- Eight fully restored five-port relations remain.  Their exact graph relation
  is `none`, source rank is 15 or 16, target rank is 18, and the quadratic
  search returns none.  The proof-first route is to lift known four-port
  marginal certificates; cubic search is explicitly not part of the current
  release route.
- Completion estimate rises to 54%, conditional on exact certificates for
  those eight and regeneration/mutation of the forest.

## 2026-08-21 12:13 PDT — theta2 whole-map overlay independently frozen (56%)

- Bound the independent theta2 replay (payload `871656d3...`): all 2,528 rows,
  85 sign classes, exact graph relation `none`, source zero, strict target
  negative, and zero unresolved.  Recorded replay runtime is 44.671 seconds.
- Bound its mutation report (payload `8a361d4b...`): 10/10 rejected, including
  omitted/reassigned rows, missing presentation, wrong orientation, Bernstein
  corruption, graph-relation corruption, and optimized mode.
- Removed theta2 from the legacy-partition blocker.  Its old rooted reason is
  now interpreted only as row selection for the whole-map overlay.
- Completion estimate increases to 56%; restoration remains the dominant
  finite obstruction.

## 2026-08-21 11:42 PDT — authoritative composite contract frozen (57%)

- Replaced overlay-only promotion semantics with two mandatory byte-stable
  primitive composites.  Raw-four must materialize all 405,216 raw IDs with
  the exact five-category presentation census; theta2 must materialize all
  2,946,240 raw IDs and bind every dummy-restoration descendant.
- Bound the raw-four canonicalization census: 1,472 terminal presentations
  form 934 classes with multiplicities `{1:680,2:150,4:71,5:14,6:7,8:12}`;
  2,540 restoration presentations form 997 parents with multiplicities
  `{1:424,2:112,4:449,8:12}`.  These are independently derived invariants,
  not enumeration controls.
- Added fail-closed checks for canonical gzip JSONL bytes, exact raw IDs,
  source/target/permutation presentations, evidence bindings, matched rank
  lower/upper certificates, class membership roots, descendant transports,
  independent byte regeneration, mutation coverage, and zero rooted fields.
- Kept restoration, cycle, and probe layer sizes dynamic.  A corrected cycle
  audit now closes all 7,452 base and 300 restored rows; the 24 reopened rows
  use a different full-map triple with source zero and Bernstein-strict target
  sign.  Cycle promotion still awaits independent replay and mutations.
- Syntax and derived-census checks pass.  Completion estimate rises only to
  57%: the contract is ready, but both composite ledgers and all corrected
  downstream packages are still absent.

## 2026-08-21 13:02 PDT — cycle, probe-input, and restoration v3 integrated (72%)

- Hash-bound the corrected cycle promotion certificate, both authoritative
  ledgers, independent promotion and whole-map replays, and the 12/12
  mutation report.  The clean package covers 13,440 base rows and 536,364
  fixed-full children with zero rooted fields or unresolved rows.
- Hash-bound probe-input contract v2, its independent replay, and the 20
  negative mutations plus optimized-mode positive gate.  Its claim boundary
  remains input-only: 176 anchors, 2,206 sites per side, and 29,964 first
  source--target pairs do not constitute probe closure.
- Hash-bound restoration v3 file SHA-256 `43bd2be5...`, provenance crosswalk
  `80033ce3...`, replay `24fa2e61...`, and 13/13 mutation report
  `7513e55d...`.  The clean forest has 997 canonical parents, 2,540 physical
  roots, 36,568 first children, 256 second children, 36,824 edges, and 36,792
  exact-separator leaves, with zero missing children, cycles, or unresolved
  records.
- Added independent derivation of canonical-parent, member-root,
  class-membership, child, edge, transport, and leaf hash roots.  Kept raw
  presentation membership separate from physical class-to-member edges.
- Added the independent global analytic audit as a theorem-layer dependency:
  `AUDIT.md`, the graph-derived component-scale replayer, and certificate
  payload `ce8f4e68...`.  It validates the marked/unmarked incidence-scale
  ranks and excludes the sole degree-two stabilizer from the retained class,
  conditional on finite closure.
- The safe structural integration diagnostic passed in 4.22 seconds.  The
  only signed blockers are the two complete composite ledgers, full probe
  classification/coherence, and the unified cross-family release package.
- Reconciled the composite interface with the moving producer without locking
  its hashes: one shared generator, support module, independent verifier, and
  mutation runner now have one locator role each.  Both current summaries
  pass the strengthened structural contract in 0.24 seconds, including the
  exact hash-root algorithms, directed source-lower/target-upper rank gate,
  terminal registry binding, and cubic/quartic/quintic/quadratic mutation
  coverage.  They remain unbound until the producer reports a final freeze.
- Best estimate is 72% toward a promotion-grade integration package.  This
  estimate excludes theorem promotion itself, which remains fail-closed until
  the moving producer packages freeze and the final documents are reconciled.

## 2026-08-21 13:19 PDT — corrected primitive composites frozen (84%)

- Hash-bound the final corrected-composite package, including its shared
  generator/support/verifier/mutation executables, both canonical gzip
  ledgers, both summaries, both independent replays, both mutation reports,
  the terminal registry, package manifest, and release-contract replay.
- The authoritative raw-four ledger contains every one of 405,216 raw IDs
  exactly once.  Its summary payload is `9cbb689a...`, its independent replay
  payload is `1a51d5ff...`, and all 14 mutations reject with payload
  `63eca71e...`.
- The authoritative theta2 ledger contains every one of 2,946,240 raw IDs
  exactly once.  Its summary payload is `6109900c...`, its independent replay
  payload is `0e80f8c4...`, and all 12 mutations reject with payload
  `18eaed98...`.  Its descendant ledger derives 56 restored roots, 864
  children, and 832 leaves.
- Cross-checked the 934-class terminal registry against the raw-four
  presentations and restoration v3 input.  The directed rank convention is
  source-lower versus target-upper throughout; no rooted restriction reason
  occurs in either promotion ledger.
- Added an outer mutation gate that revalidates the located raw-four 14/14 and
  theta2 12/12 reports as one cross-layer unit.  It explicitly binds omission,
  false-rank, missing-child, wrong-parent, broken-transport, and reassigned
  quadratic/cubic/quartic/quintic cases to the exact ledger and summary bytes.
- The safe structural diagnostic now passes with raw-four, theta2,
  restoration v3, corrected cycle, probe-input v2, and the independent
  analytic audit all bound.  The only finite-release blockers are the full
  probe/coherence package and the unified cross-family certificate, replay,
  and mutation suite.
- Best estimate is 84% toward a promotion-grade integration package.  The
  estimate remains below theorem closure because the missing probe and
  cross-family gates are load-bearing, and final theorem text cannot be
  promoted before both pass.

## 2026-08-21 15:40 PDT — corrected two-stage probe frozen (94%)

- Bound the completed probe package: 176 primitive anchors, 29,964 one-port
  rows, 2,107 one-port equality parents, 544,571 two-port rows, and 32,729
  reverse marginals, with zero unresolved or incoherent records.
- Bound all 67,741 exact transports, 4,379 parent restrictions, 638 quartet
  certificates, 156 direct `T_i` certificates, and 118 Bernstein-strict
  polynomials. The producer replay and 15/15 mutations pass.
- Bound the independent primitive-graph audit file SHA-256 `879a397a...`,
  semantic payload `65160636...`, and 12/12 adversarial mutation payload
  `58006ed7...`. Its clean-room audit covers every row and independently
  reconstructs every parent and graph transport.
- Best estimate rose to 94%; only the acyclic unified cross-family release and
  final theorem-document promotion remained.

## 2026-08-21 16:12 PDT — unified finite universe frozen (98%)

- Materialized the acyclic five-family certificate with file SHA-256
  `31e31f37...` and payload `559c6d9b...`. It cross-binds raw4, theta2,
  restoration v3, cycle, and both probe stages without binding its own
  downstream reports.
- The independent replay has file SHA-256 `42032edf...` and payload
  `2cd32140...`. Downstream files are locator-bound but excluded from their
  own source fingerprint, eliminating a self-referential hash cycle.
- All 22 unified temporary-copy mutations reject with file SHA-256
  `7beee4e4...`, payload `9ec91bd9...`, and zero survivors. Coverage includes
  omitted raw rows, false rank, missing child, wrong parent, broken transport,
  reassigned quadratic/cubic/quartic/quintic and probe `T_i` certificates,
  reversed order, global-triangle incoherence, the `raw4424` regression,
  source writes, and optimized Python.
- The signed locator is `FROZEN`, all five family statuses are `PASS`, and its
  payload is `adf5eed3...`. No finite mathematical blocker remains.

## 2026-08-21 16:48 PDT — unconditional theorem release qualified (100%)

- Bound the bibliography-corrected promotion manuscript SHA-256
  `1c4aedd7...`, quantifier audit `425a041b...`, completed probe binding
  `b3ae68a5...`, and deterministic promotion guard `6e3e301c...`.
- Independently derived the promotion probe combined root
  `7868fed6f8e0c10fcb9740da8ffdcb7f64ea68939c99cba6f364da4cfd90bf50`
  from the one-port, parent-inventory, and two-port ordered roots.
- The ordinary referee replay passed all standard layers in 321.725 seconds.
  The promotion guard itself took 0.269 seconds and the unified independent
  replay 9.526 seconds on this machine.
- The expanded outer adversarial suite passed all 20 conceptual gates in
  261.824 seconds with zero survivors, including six independent promotion-
  document/binding mutations.
- Promotion status is unconditional `K2P-SAME` on the principal `D_plus`
  domain, with the strict continuous-time and `4n-3` weak-sharpness
  corollaries. No mixed-sign statement is made.
- Best estimate: 100% complete toward the requested theorem closure and
  promotion-grade reproducibility package.

## 2026-08-21 18:05 PDT — final clean-copy binding audit (99%)

- Updated the authoritative manuscript binding to SHA-256 `6cd54064...`; the
  completed probe binding and promotion guard remain unchanged.
- Recursively bound the direct referee package's 15-file `INPUT_LOCK`,
  including the atlas compiler, descriptor/rank tables, and package verifier,
  so a clean copy cannot read an uncommitted runtime input.
- Added a machine-checked historical-artifact registry for all eight
  proof-like files found by the final scanner. Five are stale, revoked, or
  superseded narratives; the old restoration certificate, replay, and
  depth-one narrative are quarantined as revoked runtime provenance. Every
  row binds exact bytes, forbids promotion authority, and names its corrected
  replacement. The corrected restoration-v3 forest/replay remain the sole
  promotion inputs.
- Added three adversarial registry mutations targeting promotion of a revoked
  file, removal of its authoritative replacement, and omission from scanner
  coverage. The outer conceptual mutation census is now 23.
- The first independent clean-copy audit found one packaging-only caveat: the
  referee commands assumed the repository virtual environment. The release now
  contains its own locked requirements file, the README gives project-root
  bootstrap commands for exact `networkx==3.5` and `sympy==1.14.0`, and every
  entry point checks that both packages import with those versions before it
  qualifies any theorem layer. The tested runtime is Python 3.14.6.
- Best estimate is temporarily 99% while the final lock, clean-copy quick
  qualification, affected mutation suite, and independent sharpness re-audit
  are rerun. No mathematical finite-universe blocker is open.
