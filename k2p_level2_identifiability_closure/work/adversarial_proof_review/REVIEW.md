# Independent adversarial review

## Current verdict

**Analytic bridge/marginal/gluing layer: PASS.**

**Pointwise directional topology layer: PASS.**

**Four-port 997-obligation restoration forest under the corrected fixed-full
implication: PASS.**

**Primitive cycle fixed-full gate: PASS**, through the independently replayed
package `work/cycle_three_port_closure`.

**Corrected physical probe input universe: PASS**, with 176 path records and
all 29,964 first-probe source/target attachment pairs bound in schema v2.

**Global K2P-SAME promotion: pending the corrected all-primitive one-/two-port
probe output and its independent mutation replay.**  The current global proof
must not say that rank-upper and theta2 closure are the only remaining gates;
cycle restoration and coherent probes are also load-bearing dependencies.

## Load-bearing findings

1. The complete positive bridge fibre has exactly one paired-C/T incidence
   scale and one G incidence scale at every endpoint.  Rank-one uniqueness,
   K2P symmetry, the component normalizers, and bridge-tree peeling leave no
   further positive gauge or holonomy.
2. The physical local product requires endpoint-near-identity/residual/
   endpoint subdivision.  An ambient normalized tensor slice need not be
   physical.  The patched proof makes this distinction and passes exact
   replay.
3. Paired marginal restriction has an analytic physical section in
   \(\mathcal D_+\), and simultaneous bridge gluing works in both
   \(\mathcal D_+\) and the strict continuous-time cone.
4. An abstract selected relation cannot be lifted unconditionally.  The exact
   counterexample in `unconditional_lift_counterexample.json` forces the
   corrected logic: fix one full containment first and restrict those same
   actual networks.  Source openness is enough; target openness is neither
   proved nor needed.
5. Different displayed-quartet sets have disjoint strict physical K2P images,
   by the zero-versus-strict-positive invariants of Englander et al. v4.  This
   validates every quartet-deck directional exclusion, including a tree model
   versus a larger network model; generic variety distinction alone would not
   have sufficed.
6. The existing 997-parent forest did not cover omitted-role iso/T terminals,
   theta2 dummy anchors, or dummy-bearing cycle roots.  Treating prose probes
   as a substitute was not valid.
7. A general first-child quartet repair for cycle roots is false.  Exactly 132
   fully restored rows retain equal topology and require algebra.
8. The complete cycle package closes all 5,964 roots through 536,364 physical
   completions, with 535,920 quartet exclusions, 300 direct full-map
   (T_i) exclusions,
   132 exact quadratics in 54 descriptor-pair classes, and 12 exact labelled
   isomorphisms.  All 36 cycle physical anchors have unique transports.
9. The revoked 172-anchor probe used an incomplete internal-arc grammar and
   topology-first terminal oracle.  The corrected v2 input has 176 uniquely
   identified path records, including four recovered ordinary-triangle
   anchors, and includes every pendant, reticulation-incoming, and
   root-suppressed mixed edge.  Its independent replay verifies 176 exact
   parent relations, 352 root-half equivalences, and 2,206 sites per side.

## Evidence

- `audit_certificate.json`: exact analytic/topology replay, status PASS.
- `mutation_certificate.json`: 12 adversarial analytic mutations rejected.
- `topology_direction_certificate.json`: clean displayed-set census and
  directional theorem binding.
- `unconditional_lift_counterexample.json`: exact false-lift counterexample.
- `work/cycle_three_port_closure`: complete generator, independent verifier,
  raw ledgers, proof, strict-domain witnesses, and mutations.
- `probe_input_contract.json`: corrected 176-record/29,964-pair input universe.
- `probe_input_independent_verification.json`: independent exact replay, PASS.
- `probe_input_mutation_certificate.json`: 20/20 mutations rejected, optimized
  mode PASS.

The final verdict will be updated only after the corrected full probe package
consumes v2, classifies all 29,964 A+p pairs and every required A+p+q child,
proves exact parent restriction, returns zero unresolved/incoherent records,
and survives independent omitted-anchor, wrong-parent/order, triangle, cache,
and certificate-reassignment mutations.
