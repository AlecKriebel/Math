VERSION 1.1 GIT PROVENANCE
Date: 2026-08-10 (America/Los_Angeles)

Release tag name:
  bimolecular-positive-recurrence-v1.1

Fully validated release-content commit:
  fb40ab750ef2840f9ef58a5bb9267af57e79f2b5

Parent main commit used to start the separate Version 1.1 package:
  e3c11ff4c8e200fbaf8dc81e14854ad168e183d4

Temporary annotated tag object used for the recorded clean-clone replay:
  43420326cc67c53ffc47f7894290ef2cf467cfb3

The exact replay is in CLEAN_CLONE_TRANSCRIPT.txt. It cloned the temporary
annotated tag into a new directory, created a fresh CPython 3.14.6 virtual
environment, installed the Version 1.1 verifier, passed all 57 tests,
reproduced the canonical report twice byte for byte, rebuilt all three
15-page PDFs twice byte for byte with Tectonic 0.16.9, checked the PDF and
source hashes, generated and verified a complete 63-file candidate manifest,
and ended without Python bytecode caches in the package tree.

After the transcript and final metadata were installed, the two manifest
copies and deterministic ZIP archive were regenerated. The public Version
1.1 annotated tag was then placed on that final metadata commit, and the
complete verifier, PDF, manifest, report-copy, and archive/tree checks were
repeated from that immutable tag. The remote CI record is the public console
record for the final tagged replay.

A Git commit cannot literally contain its own hash: adding the hash would
change the commit. Accordingly, the immutable public tag is the authoritative
resolver for the final release commit. Verify it with:

  git rev-parse bimolecular-positive-recurrence-v1.1^{commit}
  git show --no-patch --format=fuller bimolecular-positive-recurrence-v1.1

No GitHub Release, DOI, preprint upload, journal submission, or external
contact was created for Version 1.1.
