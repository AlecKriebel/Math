# Version 2 independent semantic review

## 2026-09-21T14:23:35.751177+00:00 — first adversarial checkpoint

Estimated completion of the semantic review: 40%. This review is in progress; it does not certify unbuilt drafts. Reviewers do not edit production files, and no broad build was launched by this reviewer.

### Exact scope and success criteria

The new work must genuinely connect independently stated finite-dimensional complex Hilbert-space operators and tensor-product density operators, of local dimensions at most two, to the existing matrix model. It must also connect arbitrary input-dependent finite outcome types to cardinal-indexed tables and prove general finite stochastic output processing preserves the convex PVM class. Inputs may remain canonical `Fin` counts, as explicitly authorized by the root task. Proof artifacts and actual production compilation are required; aliases of target definitions and unchecked coordinate assumptions do not settle correspondence.

### Stochastic processing: mechanism accepted, final compilation receipt pending

`StochasticFamily` independently requires real nonnegative row probabilities and row sums one. Its distribution over full deterministic selectors is the product of all rows. `StochasticProcessing.Selector` is one pair of whole Alice/Bob functions indexed by every target-input/source-label row; it is not selected separately for each observed input pair. Its weights are nonnegative, sum to one, and reconstruct both local channel entries as marginals of the same selector. `behavior_decomposition` asserts equality of the entire ambient behavior table for arbitrary `p`, a stronger algebraic statement than equality of one Bell score.

Each deterministic branch invokes the existing actual PVM-coarsening map, permitting mergers and zero-padding. The finite convex combination therefore stays in `convexPVM`. There is no false claim that arbitrary stochastic processing preserves `rawPVM`. The generic `StochasticChannel.decomposition` covers arbitrary finite source and target types; the physical behavior API currently uses `Architecture` and its canonical `Fin` labels.

Boundary audit: empty source has one empty selector of weight one; nonempty source with empty target admits no normalized channel; empty-to-empty is valid; zero-probability entries make the entire affected selector weight zero. Physical source measurements on the nonzero qubit Hilbert space cannot have an empty outcome set, so empty channels do not fabricate target physical strategies. Input-selection maps also correctly exclude selecting an input from an empty source input set. No positivity or nonempty-alphabet premise is silently inserted into the general decomposition.

Remaining evidence: owner is completing the actual module build and contracts after a documented doc-comment/`omit` parser repair. An earlier `stochastic_build.log` was still a failure when inspected, and is not counted as successful evidence. A pointwise arbitrary-label stochastic wrapper, if included in the release claims, should be an explicit composition/contract through the finite-label behavior equivalence rather than merely a prose observation.

### Finite labels: source definitions accepted, final APIs pending

`FiniteLabels.POVM` and `PVM` are independent effect-family records indexed by the source type. `Strategy` independently combines these measurements and a state; its behavior applies the actual Born trace. The encoding and decoding transport effects through `Fintype.equivFin` and prove inverse laws. A whole-table real linear equivalence transports raw ranges and convex hulls. This is substantive correspondence, not defining source physics to be the image of a target embedding.

Empty outcome sets are handled by impossibility of a normalized measurement on the nonzero qubit space, while input-dependent finite labels are allowed. Both-zero-input strategies retain a state and vacuous measurement families. The root explicitly authorized `Fin m`/`Fin n` input labels; no arbitrary-input-type theorem is claimed by this review.

### Hilbert source and isometric compression: construction accepted provisionally

`HilbertCoordinates` defines positivity from the given local Hilbert inner product; the tensor density operator will act on the actual algebraic tensor product. Because the pinned Mathlib lacks the needed algebraic tensor-product Hilbert instance, a tensor Hermitian form is built from arbitrary source orthonormal bases. The draft proves its pure-tensor inner-product formula and invariance under changes of both ONBs by tensor induction. This is an acceptable canonical tensor-product form construction if those proofs compile and the final source state uses it independently of the target matrix embedding. The separate `basisMatrix_positive` theorem derives coordinate PSD from source positivity.

`IsometricCompression` embeds by `V A V†`, adds `I−VV†` to one selected outcome, proves positive/idempotent/orthogonal completion, and preserves trace and Born pairing through tensor isometries. This handles the real missing dimension-one PVM normalization issue: merely zero-padding every effect would sum to a projection instead of the target identity. The complement assignment is the right repair.

Outstanding falsification checks: trace-one states must exclude a zero-dimensional local factor; selected outcomes must be derived from source normalization plus nonzero local dimension rather than assumed; tensor form must agree with physical product inner products and be basis independent; trace must be the actual endomorphism trace; tensor measurement action must be `TensorProduct.map` rather than a target-matrix definition; both POVM and PVM behavior must be preserved; basis choices and isometry choices may alter the padded target measurement but cannot alter represented probabilities. The final at-most-two theorem must state the actual union/model scope accurately, rather than incorrectly equating raw one-dimensional behavior sets with raw qubit behavior sets.

### Manuscript deterministic-reset positivity: accepted

The revised `prop:positive-multipliers` in `paper/main.tex` uses `S=PᵀQP` and the genuine coefficient reset `R_j`. The identity `R_j u=u` cancels the normalization multiplier; null unchanged rays cancel all terms except `2 λ_j r_jᵀ S u`, with the same sign and factor as Lean's `DeterministicGap.stationary_gap`. The pairing is strictly positive in the strict physical residual domain. Resetting one whole Bob input is a physical deterministic replacement, leaving one nondeterministic input. Its finite hidden-variable construction and subsequent deterministic refinement place the whole behavior in the PVM convex hull. The separator's strict score gap rules out λ_j≤0, including the zero boundary. Positivity of all five coefficients and spanning of the first four rays imply Λ is positive definite. No semidefinite dual-attainment or determinant-pullback lemma is required in this replacement proof.

The neighboring general duality, full smooth-manifold, and full Hessian/inertia statements are explicitly labeled manuscript-only. Their presence is not counted as formal endpoints of this review.

### Required before final approval

All new production modules, independent contracts, and complete axiom queries must compile in the fresh release run. Final source definitions must meet the outstanding Hilbert boundary/correspondence checks above. Evidence must identify the current source and exact environment, and clean-extraction verification must use those same checked sources. This note will be updated after final proofs/APIs are available.

## 2026-09-21T14:25:14.215287+00:00 — compiled finite-label/stochastic checkpoint

Estimated review completion: 65%. `StochasticProcessing` now has a successful production build log, anonymous stochastic contracts covering arbitrary types/empty channels/zero entries/whole-table decomposition, and selected axiom output using only the three standard axioms. The previous parser-failure evidence is superseded by that actual success; it was never treated as a proof. FiniteLabels owner reports the production module and `FiniteLabelContracts.lean` pass. Source and contract review confirms genuine records and inverse transport, both raw-range directions, complete table equality, both convex-hull identities, arbitrary universe-polymorphic outcome labels, and empty-outcome/empty-input boundaries. No nonempty or decidable-equality premise has entered the finite-label equality.

The root has commissioned `FiniteStochastic` as an explicit arbitrary-label processing wrapper, removing the outstanding prose-only-composition concern. Its review remains pending production source completion.

The Hilbert source has now been inspected directly: its density is an endomorphism of `E ⊗[ℂ] F`; `born` uses the actual `TensorProduct.map` of local effects and `LinearMap.trace`; local effects use positivity under the given Hilbert inner product. The source structures and coordinate theorems are separate. Trace-one rules out zero local finrank by the joint matrix trace on an empty product index. The final isometric representation and source-to-target behavior clients remain pending.

## 2026-09-21T14:28:35.041763+00:00 — forward/reverse Hilbert and label-composition review

Estimated review completion: 80%. The actual forward Hilbert representation code now derives `aliceSelected`/`bobSelected` from the source state's positive local finrank and local POVM normalization. No nonempty-output premise has been added to either strategy endpoint. Its state embedding, local complement-completed effects, and whole-table POVM/PVM equalities are in production source. The matrix compression proof permits assigning the complement even to a previously zero effect: the embedded state has no population in the added subspace, so Born probabilities are unchanged. This is the correct boundary behavior.

`HilbertIsometry` additionally constructs an actual complex linear isometry into `EuclideanSpace ℂ (Fin 2)` and proves coordinate, inner-product, norm, and injectivity properties. `HilbertReverse` constructs genuine dimension-two source Hilbert operators/density from fixed-qubit matrices via inverse basis representation, derives source positivity using the independent PSD equivalence, and proves source Born equality. This supplies the appropriate reverse direction without falsely claiming every fixed dimension-one space realizes all qubit behaviors. Final compilation of the reverse and composed clients is still being coordinated.

`FiniteStochastic` has been independently reviewed: weighted source/target-label probabilities are defined directly, then a separate theorem proves the cardinal-encoding commuting square. Its whole-table selector decomposition and deterministic physical PVM branch transport agree with the already accepted stochastic semantics. Actual final contract evidence is being gathered by the owner.

An explicit combined arbitrary-Hilbert/arbitrary-outcome-label source adapter has been assigned. This closes the remaining potential gap between two separately general interfaces; this review will inspect its independent source records, exact behavior transport, and main equality/simulation client when available.

The revised manuscript cone-circuit lemma was also adversarially checked: sign-preserving perturbation along a proper-subset dependence creates a smaller nonzero relation; pointedness rules out a one-sign relation; support minimality therefore gives a linear circuit with `N−1=rank R≤3`. Disjoint extreme rays after cancellation exclude a singleton side, forcing four rays, a two-versus-two split, and dimension three. No extra cone closure assumption is needed.

## 2026-09-21T14:33:25.681610+00:00 — composed physical source review

Estimated review completion: 90%. `HilbertSimulation` production build has completed successfully, and `HilbertContracts.lean` passed with zero diagnostics according to its owner. I inspected the contracts: they independently expose the actual tensor endomorphism/trace/positivity fields, exclude zero local dimensions and empty measurement alphabets, assert actual complex linear isometry and full POVM/PVM representation, check the reverse representation and raw-set equalities, and invoke finite simulation with actual Hilbert projective branches. An explicit normalized one-dimensional source state proves the smaller-dimensional interface is nonvacuous. The eight inspected selected dependency closures contain only `propext`, `Classical.choice`, and `Quot.sound`; the final runner must still check the full regenerated public inventory.

`HilbertSimulation.rawPOVM` and `rawPVM` quantify over an independent bundle of actual complex Hilbert spaces of dimension at most two. The reverse direction uses the genuine dimension-two Hilbert space. Hence its raw-set and hull equalities have the appropriate union-of-dimensions scope; no invalid fixed-dimension-one surjectivity claim occurs. Its finite simulation consumes the matrix main theorem and converts every complete branch back to actual Hilbert projective measurements.

The new `HilbertFiniteLabels` production source has now been inspected. Its outcome-indexed effects are actual endomorphisms of the arbitrary source spaces, the state remains an actual source tensor operator, and its behavior directly evaluates the source Born trace. Cardinal encoding is separately defined with inverse measurement and strategy laws; composing encoding, the checked Hilbert embedding, and label decoding preserves the complete original labeled table for both POVMs and PVMs. The at-most-two-input simulation endpoint has only the stated dimension/input bounds, with no nonempty-output assumptions. The current finite mixture returns actual arbitrary-label matrix projective strategies, and a checked source-level reverse constructor preserves every branch; the owner is adding explicit source-model raw/hull and Hilbert-branch clients before final validation.

The current runner explicitly requires all seven contract files, including `FiniteStochasticContracts.lean` and `HilbertFiniteLabelContracts.lean`, and discovers additional contracts recursively by exact relative path. Preliminary verifier probes written during development are superseded by the reviewer's forthcoming final probes. Production-source layout guards and package-manifest handling are being checked separately by the verifier reviewer. No final clean-run/package certification is implied by this semantic checkpoint.

## 2026-09-21T14:37:41.966452+00:00 — final semantic verdict

Semantic-review completion: 100%. The new bridge and closure source mathematics is accepted within the stated scope. `HilbertFiniteLabels` has now rebuilt successfully, all thirteen independent anonymous contracts passed with exit zero, and all fifteen inspected endpoint/reverse dependency closures contain only the three standard axioms. The contract-only missing `ComplexOrder` scope was repaired and the successful rerun supersedes the failed initial attempt; production statements were unchanged. The production log ends in `Build completed successfully`, the final contract log is empty, and the selected axiom log records the exact declarations checked.

The final combined endpoint `finite_source_projective_simulation` returns actual projective endomorphism strategies on complex C² with the original arbitrary finite labels, one finite common index, nonnegative weights summing to one, and equality of the entire source Born table. The independently defined raw Hilbert/finite-label ranges also have both exact equalities to the fixed matrix ranges, yielding the ordinary-hull equality for at most two inputs per party. Empty source labels and zero local dimensions have explicit impossibility clients. No conclusion is supplied as a premise, and no representation is obtained merely by defining the physical source to be a target image.

`MODEL_CONVENTIONS.md` and `CERTIFIED_COVERAGE.md` were also checked for mathematical interpretation. They correctly distinguish source operators from their representations, dimension-union equality from false fixed-dimension-one equality, zero effects from complement-completed effects with identical probabilities, actual whole-strategy mixing from inputwise mixing, and convex stochastic closure from raw closure. The documentation owner was asked to name the latest combined source-branch endpoint and combined contract explicitly.

Remaining release conditions are evidentiary, not an unresolved mathematical gap in these bridge statements: complete the fresh integrated production build and regenerated full public axiom audit; run all seven required contracts from the frozen source snapshot; independently validate the clean-extraction reproduction receipts and exact archive manifest; retain the documented pinned compiler/Mathlib and standard-axiom trust assumptions. The separate verifier review tracks these package conditions, including its last symlink-directory manifest edge-case repair. This semantic review does not certify every unused auxiliary manuscript derivation: the expressly retained coverage limits remain in the coverage map. No broad Lean build or production/source-script mutation was performed by this semantic reviewer.
