# arXiv fallback package

Recommended categories: primary `q-bio.MN`, secondary `q-bio.QM`, cross-list `nlin.PS`. Build `main.tex`; the supplement is a separate source/PDF. The bundle includes the generated bibliography file and all figures/tables. Nothing has been posted automatically.
