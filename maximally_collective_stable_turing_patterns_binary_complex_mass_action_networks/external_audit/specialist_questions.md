# Targeted specialist-audit questions

## Reaction-network and Turing specialist

1. Is the SCC exhaustion complete for every proper principal species set, including the hypersurface `b=2a` where one edge vanishes?
2. Does universal Hurwitz stability of every smaller principal Jacobian block justify the stated all-spectrum localization order `n-1`?
3. Is the complete positive-realization family `A_m(a,b)H` derived without omitting alternative positive equilibria?
4. Is the one-bad-minor theorem interpreted correctly as a stationary positive-real-eigenvalue result, with wave instability explicitly left separate for arbitrary diffusion vectors?
5. Is the upper endpoint `n-1` positioned accurately relative to Satnoianu–Menzinger–Maini and Anma–Sakamoto–Yoneda?

## PDE bifurcation and stability specialist

1. Is the physical equilibrium-scaling transformation `z=Hx`, with `D_phys=H Delta`, correct?
2. Is the fixed-integrated-mass Lyapunov–Schmidt setup correct when the conservation vector is semipositive rather than positive?
3. Are the `1/4` and `1/2` Fourier factors in `w_0`, `w_2`, and the cubic coefficient correct?
4. Does the gauge change `w_0(L)=w_0^ref+tau rho` correctly impose the physical mass constraint?
5. Do sectoriality, the fixed-mass spectral gap, and the principle of linearized stability justify local exponential asymptotic stability in `H^1`?

## Symbolic and algebraic specialist

1. Is the order-`(n-1)` omission-minor table complete, including the two zero omissions?
2. Is the strict `lambda>=0` monotonicity argument in the one-bad-minor theorem valid under exactly the stated hypotheses?
3. Are the equality cases in the 34-, 35-, 77-, and 84-term half-plane certificates correct?
4. Is the four-factor second-harmonic recurrence exact at both endpoints of the chain?
5. Is the all-dimensional cubic comparison `N_m(L)>1/200` logically sufficient and correctly normalized?

No packet has been sent automatically. The request is for theorem-specific scrutiny, not a general endorsement.
