# Finite revision and verification list

Applies to commit `6f68ad3e795c239e452206c84ce4ce331386a094`. These are referee requests; no manuscript repair was made during this review.

1. **Close certificate-term validation (R1).** Reject duplicate monomials and require exact actual/expected support and row counts in each affected checker. Preserve exact coefficient checks. Add extra-term, missing-term and both duplicate-order negative controls. Confirm the minimal aggregate rejects the supplied mutants, then regenerate all distributed verifier copies and bundles.
2. **Repair fraction-table spacing (R2).** Change the generator or scoped table style, regenerate the 77/84-term tables, and rebuild canonical and journal supplements. Inspect each affected page at readable scale; check that page 15's `91125`/`4420871` witness is separated. Update contents and dependent archives/hashes after final pagination.
3. **Repair two determinant-proof sentences (R3).** Replace the cycle-cover count with the existing Schur-complement proof reference; order the upstream chain fragment, triad and downstream fragment correctly in both main and supplement. No determinant formula changes.
4. **Make S9 independently checkable (R4).** Include a reaction/Hessian-derived cubic calculation and exact simple-onset/complement/higher-mode checks for m=3 on `0<epsilon<=1/1000`. The two independent implementations in this audit supply a concrete starting point. Add the short Routh explanation to the supplement and derive the verifier’s currently hardcoded cubic from the reactions.
5. **Correct the literature-comparison row (R6) and consider scope clarifications.** Credit Conradi–Mincheva–Uecker's numerical nonlinear branches and stable segments instead of unqualified “no” entries. Optional prose improvements: say “restricted well-mixed Jacobian”; state the fixed-interval meaning of Turing bifurcation; avoid equating a whole Pareto frontier with one minimax value.
6. **Run the final package checks with sufficient storage.** Require all standalone tests including the real TeX-dependent control, all ordinary verifiers and optimized-Python controls, full portable replay, deterministic bundles/manifests, three detached source builds, fresh rendered-text comparisons, and visual inspection of changed PDFs. Record exact outputs and distinguish skipped/failed checks from passes. The current audit's 28-pass/1-skip suite and interrupted TeX campaign do not close this item. If asserting full historical provenance replay, also supply its five external lineage archives and complete that separate route; the portable current-result route does not require them.
7. **Resolve version references (R5).** Use an actual immutable reference containing the corrected final package. If a new release is deliberately chosen, create it after checks and use its actually assigned DOI if applicable. Do not describe the absent v1.0.9 tag as already frozen, and do not use v1.0.8's DOI to identify different corrected files.
8. **Finalize author facts and upload review.** The author supplies accurate funding, competing interests and exclusivity declarations, finalizes the cover letter, and checks the submission portal's assembled preview. No external communication or upload was performed by this audit.

Closure means the listed defects are repaired, the final corrected artifacts pass the specified checks, and factual declarations are supplied. It does not mean a guarantee of journal acceptance, an exhaustive literature-priority proof, global PDE stability, or a stronger theorem than the paper states.
