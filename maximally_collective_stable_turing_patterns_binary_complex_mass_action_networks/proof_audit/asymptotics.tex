% Scaling consequences used in the flagship.
At unit equilibrium the exact linear diffusion infimum is $8(m-2)$, while the improved stable construction has contrast $23(91m-183)/63=\Theta(m)$. For the physical trade-off family, $\chi_D(L)=rac{23}{63}91(m-2)L$ and $\chi_H(L)=rac{91(m-2)-1}{91(m-2)L}$. At $L=1/\sqrt{3(m-2)}$, both are $\Theta(\sqrt m)$. The universal product lower bound implies $\max(\chi_D,\chi_H)>\sqrt{8(m-2)}$, proving exponent optimality only.
