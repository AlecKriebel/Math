# Root report cross-review

Timestamp: 2026-09-08T04:32:30Z. Completion: 100%.

I cross-read the root `REFEREE_REPORT.md` against the retained certificate repair, exact coefficient comparison, scalar experiment, PDF evidence and mathematical scope review. Its C1 closure, counts and nonblocking scalar disposition are supported. No substantive correction is required.

- The C1 witness and its repaired behavior are exact. There are 21 cases, 63 direct reader/generator calls, two actual regeneration rejections preserving three outputs each, 26 matching unpacked implementations and 12 matching bundled readers.
- All 218 current coefficient rows are checked exactly and unchanged from v1.0.10; all five current table copies match. The distinction between a formatter and an exact mathematical equality check is preserved.
- The Boolean witness, affected row, generated TeX, three direct-reader acceptances, full symbolic acceptance, 19-page PDF, full-profile PDF acceptance, and empty selected-warning set match saved evidence. The scientific-string variant is correctly limited to direct reader/generator testing.
- The reported numeric polynomial remains unchanged, while the generated noncanonical display is wrong. Every shipped coefficient remains correctly encoded and rendered. Low-priority input-format maintenance is a proportionate conclusion; the evidence does not justify withholding the current preprint.
- The proposed canonical rendering or consistently enforced rational-string contract directly addresses this edge. It neither requires a mathematical revision nor creates a broad new schema obligation.
- The report correctly denies having tested a complete altered-release refresh and correctly states published-hash containment.

The parent has already identified the one scope paragraph to make explicit before finalization: source auditing was rerun with both `release/` and `submission/` present and all conditional checks passed; the unchanged-table freshness check rejects the mutant before regeneration. Both statements are now supported in `SCALAR_RENDERING_RESULTS.json` and `SCALAR_RENDERING_NOTE.md`.

One optional precision edit: describe the C1 consumers as “both mathematical readers” rather than “both independent mathematical readers.” Their distinct entrypoints are tested, but these call counts do not establish fully independent implementations. This does not affect the repair conclusion.

This bounded cross-review does not independently certify claims owned by the release, metadata or other mathematical lanes. No reruns, source edits or Git actions were performed.
